# Implementation documents — index

This directory holds **implementation-ready specifications** for the work that
remains open across the project's active plans and analysis notes. Each file is
a self-contained brief for **one coherent task set**: a junior developer can
clone the repo, open exactly one document, and implement it end to end (code +
tests + docs) with no other context.

> **Provenance.** These 31 documents were generated on 2026-07-18 by extracting
> every open item from the 12 active plans in [`../plans/`](../plans/) and the
> analysis notes in [`../analysis/`](../analysis/), verifying each item's status
> against the working tree (git history + file/symbol presence) rather than the
> source doc's own claim, then grouping the survivors by workstream. 225 tasks
> across 31 docs. Items the plans marked open but the repo showed already landed
> were dropped; contradictions between sources were resolved once and recorded.
>
> **Later additions.** The directory has since grown past that batch: work
> discovered *while implementing* — a defect an existing doc's validation
> surfaced, or a gate that turned out to be reachable — is written up as a new
> doc in the same format and added to the table below, flagged with the date and
> what found it. The counts in this paragraph describe the 2026-07-18 batch and
> are deliberately not restated as the directory grows; the table is the
> inventory.

## How to use these

1. Pick one document (the table below groups them by area and flags
   dependencies). Read it top to bottom — every task carries **Goal / Steps /
   Code / Tests / Docs / Done-when**, exact repo paths, the existing pattern to
   mirror, and the commands that prove each step.
2. Read [`_conventions.md`](_conventions.md) once — it states the repo-wide
   rules every doc relies on (build/test entry points, the version-pinning
   dependency rule, changelog/docs conventions) so the individual docs don't
   repeat them.
3. If a claim in a doc disagrees with the code, **re-verify before
   implementing** — the tree moves; the docs were verified on 2026-07-18.

## Binding cross-document positions

The source plans contradicted each other (and occasionally the code) in 12
places — e.g. whether Zen 3 can open AMD branch records (it cannot in this tree;
the floor is Zen 4), whether the `trace_call_auto` completeness bug is fixed (it
is), which macOS CI image is current (`macos-15-intel`, not the retired
`macos-13`). Every one of those is resolved once in
[`_positions.md`](_positions.md), and every document here already conforms to
it. Read it before editing any doc so a change doesn't reintroduce a refuted
claim.

## The documents

Grouped by workstream. **Depends on** lists only *hard* build/code dependencies
(a doc that cannot start until another lands); soft "coordinate with" couplings
are noted inside each doc's *Task order & parallelism* section, not here.

**Status** tracks task completion as `done/total`; update the cell as tasks
land. Legend: ☐ not started (`0/N`) · ◐ in progress · ☑ complete (`N/N`, code
landed) · ✅ verified (`N/N`, exercised end-to-end on the target — not just unit
tests; see the repo's verify-before-done rule). A doc reaches ✅ only once its
hardware/credential-gated legs have actually run, or are recorded as gated.

## Working process for implementing agents

Implementation and validation are split across agents. If you are **implementing**
a doc, follow this loop so the status table always reflects reality and every step
is on `main`:

1. **Mark in progress, then push.** Before writing any code for a task, set the
   doc's Status cell to `◐ done/total (T<n> wip)` in the table below, commit that
   change alone (`docs(implementations): mark <doc> T<n> in progress`), and push to
   `main`. This claims the task so a second agent does not double-work it.
2. **Implement.** Do the task end to end — code + tests + docs + `CHANGELOG.md` —
   per the doc's Steps. Format through the pinned path (`make docker-fmt` or
   `make fmt CLANG_FORMAT=clang-format-18`).
3. **Mark complete, then push.** Bump the Status cell (`◐ done+1/total`, or `☑ N/N`
   when the doc's last non-gated task lands), and commit the implementation together
   with the status bump (`<type>(<area>): <task summary> (<doc> T<n>)`). Push to
   `main`.
4. **Hardware/credential-gated legs.** Where a task's *validation* needs silicon or
   credentials this host lacks (e.g. Zen 3 BRS, a bare-metal PT box, publish creds),
   implement everything implementable behind the gate, mark the task complete with a
   `(gated: <reason>)` note in the Status cell, and record the exact gate in the
   commit message. **Validation of these legs is a separate agent's job** — do not
   silently self-skip a leg that this host *can* run.

`✅ verified` is set by the **validating** agent, not the implementer: it is
reached only after the doc's live legs actually run green on the target hardware.

Everything below started ☐ not started — these are freshly authored specs.

### AMD hardware tracing
| Document | Tasks | Status | Depends on |
|---|---|---|---|
| [amd-ibs-backend-honesty.md](amd-ibs-backend-honesty.md) — IBS backend honesty, record sizing, ABI guards, validation gate | 7 | ✅ 7/7 (**live-verified 2026-07-20 on the Ryzen 9 9950X Zen 5 dev box** by a validating agent — the doc's only gate (AMD Zen `ibs_op` silicon) run both ways, 0 `not ok` each: **positive paths** via `make docker-hwtrace-privileged` (CAP_PERFMON bypasses paranoid=4) — `test_hwtrace` 1..658 with the AMD LBR live tests running, `test_ibs` 1..84 with **zero `# SKIP IBS`** lines, `ibs_probe` `IBS-Op/IBS-Fetch AVAILABLE` via the real-open path (T1), `# whole-process: 3/3 worker functions covered` + `# system-wide: 3/3`, `phase5` callchain survey + `record-bound` (112 / ≥1192 / ring-usable) all `ok` (T3), and the regression signal `# call_auto escalate: backend=3 insns=77 (escalated off the LBR window)` → escalation fires, **not** a `truncated=0` regression (T2); **honesty/blocked path** via `make docker-hwtrace-ibs` (paranoid=4, no CAP_PERFMON) — `ibs_probe` prints `BLOCKED — EACCES` and the 5 live tests self-skip with the real reason, never AVAILABLE (T1). Run recorded in [amd-hardware-validation.md](../amd-hardware-validation.md) "Recorded runs". T4-T7 are off-AMD-verifiable and also green in the live plan) | — |
| [amd-branchsnap-lbr-docs.md](amd-branchsnap-lbr-docs.md) — branchsnap depth fix, LBR tiling validation, freeze-probe cleanup, Zen 3 BRS story | 8 | ◐ 7/8 (T1/T4/T5/T6 landed; T2/T3/T7 landed & **live-verified 2026-07-20 on the Ryzen 9 9950X Zen 5 dev box** by a validating agent — `make docker-hwtrace-codeimage` (CAP_BPF+CAP_PERFMON, real LbrExtV2, EXIT 0, **zero self-skips**, 35 ok): T2 `use15`→15 in-region insns/`!truncated` + `use16`→16 insns/`truncated`; T3 the a89a1cb multi-exit rewrite holds on BOTH exits live (`want=1, other=0, truncated=0` down each path, neither 0x05/0x09 offset needed fixing — analysis doc note dated); T7 CALL-target #DB non-eviction `preamble_in_tiles=1 tile_truncated=0` PLUS the anti-vacuity **mutation-flip observed** (env-gated drop of the preamble PC → check 3b `not ok` while the never-reached negative control stays green) — branchsnap.c/branchtile.c/amd-tracing-plan.md now state all three boundary shapes (ret exit, tail-`jmp` exit, CALL-target checkpoint) measured, not assumed. The 7 landed tasks are now live-validated; only **T8 remains BLOCKED**: needs Zen 3 Family 19h silicon — arm must not merge untested per CLAUDE.md, so the doc cannot reach 8/8 here) | — (soft: shares the Zen 3 story with the sibling above) |
| [../plans/amd-review-followup-2-plan.md](../plans/amd-review-followup-2-plan.md) — round-2 AMD review follow-ups (IBS export-OOM honesty, doc-drift cluster, cleanups, MSR-rung symmetry, HWDBG into IBS/cascade) — the plan post-dates the 2026-07-18 extraction and is itself implementation-ready (defect + code:line + acceptance per item), so it is tracked directly: authoring a mirror doc would duplicate 22 KB of spec and create exactly the drift its own T2 exists to close | 6 | ◐ 5/6 (**T1+T2+T3+T4+T5 landed 2026-07-23 on the Zen 2 dev box.** T1: all four IBS lanes (`survey_pid`/`survey_process`/`window_end`/`survey_fetch_pid`) now capture the export return and report `EUNAVAIL` on OOM instead of a complete-looking empty survey (mirrors the SW-clock lane); `asmtest_ibs_test_set_export_fail` seam + a pure contract check run everywhere, and the LIVE leg ran under real IBS capture on this Zen 2 — injected export OOM → `EUNAVAIL`, no edges beside it (`test_ibs` ok 67/68). T3: `g_open_errno` resets on every capture entry (stale-`ESRCH`-after-success pinned live, ok 66); both live survey drains gained the seam parser's short-SAMPLE `h.size` floor (closes F7's last two sites); retired-freeze-gate comment rewritten. T4: preferred option — the MSR rung commits a NONEMPTY truncated partial as `HW_OK`+`truncated` like the fast tier (header contract honoured); genuinely-empty still falls through; decision extracted to `asmtest_trace_auto_msr_commits` (src/trace_auto.h seam) and pinned by pure checks in `test_hwtrace` (live narrow-config leg needs LbrExtV2 → Zen 4+ box, recorded gated). T5: `ASMTEST_HWDBG` now reaches `ibs_backend.c` (probe outcomes once, `perf_event_open` errno, near-full/LOST, export-OOM decision) and `trace_auto.c` (per-rung commit/skip + final mechanism); same getenv-once gate, no new env var. T2 doc drift: (a) round-1 plan, (e) validation checklist+plan hygiene, (f) asmspy residual — verified ALREADY closed by the 2026-07-21/22 wave; (b) parity-matrix Matrix 7/8 Zen 3 split from Zen 4+ (LBR never opens on BRS-only silicon), (c) Matrix 1 marks MSR-direct + IBS shipped (BRS the only forward-look), (d) SUPERSEDED banner + zero-IBS-code correction on the 2026-07-09 orphan page — fixed now. Evidence: `make docker-hwtrace-privileged` GREEN on this Zen 2 (hwtrace-test + ibs-test, live IBS, LBR self-skips as expected), `test_ibs` 1..85 / 0 failed live, `make docker-fmt` clean. **T6 remains** (standing Zen runner): its one-shot leg already ran green via self-hosted-ci-runners T3 [run 29897214772]; the standing-runner deployment decision is owned there — nothing separately actionable here. **macOS portability fix (2026-07-23, validating agent):** T4's new commit-decision seam broke the macOS hwtrace build — `examples/test_hwtrace.c` declared `asmtest_trace_auto_msr_commits` inside the Linux/x86-64 perf_event block while the pure `test_msr_commit_decision` calls it on every host → implicit-declaration error under `make WERROR=1 hwtrace-test`; prototype hoisted above the guard (the definition in `src/trace_auto.c` is unconditional and links on Darwin). `make WERROR=1 hwtrace-test` 149/149 native on the Intel-mac host, the full macOS-Intel contract green at the same tree. See CHANGELOG [Unreleased] Fixed) | — |
| [ptrace-blockstep-tracer-correctness.md](ptrace-blockstep-tracer-correctness.md) — int3 si_code, rep-prefix, SP-aware step-over, IBS pre-cover | 8 | ✅ 8/8 (T1 [all 3 drivers], T2, T3, T4, T5, T6 landed & verified: `make docker-hwtrace` 436/436 (+4 for T4's re-entrant call-out differential — confirmed red, with the correct-invocation result flipping to the inner one, when the depth check is reverted), `docker-hwtrace-jit-dotnet-bcl`/`-jit-java` green on real CoreCLR/HotSpot call-out step-overs, `make docker-docs` clean; T7 code landed: `asmtest_bs_precover_build`/`_free` (`include/asmtest_blockstep_internal.h`) memoizes `blockstep_reconstruct`'s decode; **correction to the prior gate note**: on this session's host/lane BTF is *not* masked — `make docker-hwtrace-privileged` ran T7's LOOP_X86 differential + hostile-leader legs live (not skipped): `probe_calls 81 -> 5 (hits 19)`, all green; T8 done & live-verified: `ASMTEST_TRACE_IBS_PRECOVER` (0x4) wired into `asmtest_trace_call_auto`'s block-step rung (`src/trace_auto.c`, new `build_ibs_precover`) — forks a bounded ~30ms warm-up child, surveys it out-of-band via `asmtest_ibs_survey_process`, builds+installs a T7 precover table around the one block-step call, any survey/build failure degrading silently to the plain rung; `make docker-hwtrace` 550/550 (bit-as-no-op path: on this AMD host, which lacks LbrExtV2, `asmtest_trace_call_auto`'s rung 1 always resolves to the in-process SINGLESTEP HWTRACE backend before the cascade ever reaches block-step, so the with/without-bit differential correctly asserts byte-identical output rather than a shrink — a structural cascade fact, not a T8 defect); `make docker-hwtrace-privileged` (CAP_PERFMON) 561/561 with the live mechanism actually engaged (not a no-op): a direct live-IBS-survey-primed block-step differential shows `probe_calls 101 -> 0 (hits=25, leaders=2)`; both counts reproduced natively on this Zen 2 host outside Docker too; `make check` clean. **Independently re-validated ✅ 2026-07-22 (validating agent) from clean main (0f13e58) on this AMD Zen 5 box (Ryzen 9 9950X — note: the "Zen 2 host" phrasing above is a stale mislabel; this dev box is Zen 5):** `make docker-hwtrace` **634 passed, 0 failed** carries every ptrace-blockstep assertion — T1 int3 si_code (`ok 420-424` oop forwarded-SIGTRAP-once, `ok 438-439` block-step int3 truncates), T2 rep-prefix (`ok 440-445`), T3 SP-aware step-over (`ok 541-548` software-int3 + hardware-bp call-out, in-region only / not-a-false-return), T4 re-entrant call-out differential (`ok 549-552` — result is the OUTER invocation's value, R's post-call offset recorded once at outer depth, depth-aware), T7 precover (`ok 447-457`), T8 IBS-precover bit byte-identical (`ok 219-225`); `make docker-hwtrace-privileged` (CAP_PERFMON) **667 passed, 0 failed** with the **live AMD IBS mechanism actually engaged** — `ok 241-246` + `# blockstep ibs precover (live): probe_calls 101 -> 0 (hits=25, leaders=2)` and `# precover LOOP_X86: probe_calls 81 -> 5` (a real IBS-Op survey measurably cut branch-probe decode calls, not a no-op); `make docker-hwtrace-jit-dotnet-bcl` traced a **real CoreCLR BCL method** (`Console.WriteLine`, tier-2, 41 B) via `PTRACE_SINGLEBLOCK` (16 insns, 3/0); `make docker-hwtrace-jit-java` traced a **real HotSpot JIT method** (`Hot.asmtjit`, tier-0, 104 B) via block-step (10 insns, 3/0); `make check` **54/0**; `make docker-docs` -W clean.) | — |

### Data-flow tier
| Document | Tasks | Status | Depends on |
|---|---|---|---|
| [dataflow-producer-correctness.md](dataflow-producer-correctness.md) — gap barrier, sub-register aliases, undefined flags, F2 record-and-inject | 8 | ✅ 8/8 (T1, T2, T3, T4 done & live-verified; T3 on the Zen 5 box — `make docker-dataflow-attach` 498/498, `dataflow-blockstep-test` 180/180, all 0 skips; adversarially reviewed — one confirmed MAJOR finding (32-bit GP writes weren't modeled as zero-extending the full 64-bit container, fabricating a stale-writer edge for the upper half) fixed with a teeth-checked regression test before landing; T5 done & live-verified on the Zen 5 box — DFB_IMP_HWREC split out of DFB_IMP_OTHER, `region_scan` reports `hwrec_off[4]`/`nhwrec`/`hwrec_overflow` (capped at the 4-slot architectural DR count), new local `dfb_arm_hw_bp`/`dfb_clear_hw_bps` DR0-3 plumbing (mirrors ptrace_backend.c/asmspy_engine.c without widening the backend's single-slot API), `capture_blockstep` arms one slot per scanned site and absorbs a DR6 hit with one single-step to snapshot the real post-retirement boundary (`info.hw_hits`); forward-pass-only per the doc — `step_block` still refuses to inject (T6's job), so verdicts are byte-for-byte unchanged (probed `cs_regs_access` first: Capstone 5.0.1 already reports the complete write set for all 5 hwrec mnemonics, so no producer-local supplement was needed, unlike `syscall`); `make dataflow-blockstep-test` 185/185 (was 180/180, +5: 2 scan-level + 3 live forward-pass, including a forced-replay case proving a real DR hit with `hw_hits=1` while the verdict still truncates exactly as before, and the force_singlestep=1 Done-when check `hw_hits==0`); `make docker-dataflow-attach` 506/506 across all 8 suites, 0 skips; T8 done — the named upstream sentinel (`run_avx_tcg_sentinel_case`, near the `uc_vec_width` probes) stands up two raw `uc_engine`s and asserts VEX-256 `vaddps ymm` returns `UC_ERR_INSN_INVALID` and VEX-128 `vpaddd xmm0,xmm1,xmm2` still drops VEX.vvvv (xmm0 comes back old-xmm0+xmm2, not xmm1+xmm2); trigger confirmed NOT met (Unicorn 2.1.4, qemu/VERSION 5.0.1); doc pointer grepped in both the suite and the `insn_is_vex_evex` gate comment; no pin/gate/seeding change shipped; `make dataflow-blockstep-test` 186/186 (+1), `make docker-dataflow-attach` 507/507 across all 8 suites, 0 skips; T6 done & live-verified on this Zen 2 host — `step_block`'s DFB_IMP_HWREC arm now injects the boundary's recorded write set (generic over `c->cur`'s Capstone-reported records via a new `uc_gp_container` Capstone-id→Unicorn-container map, mirroring `gp_value`'s read-side grouping — no per-mnemonic table needed) and terminates the block there, exactly like `syscall`/`int 0x80`; `region_scan`'s `injectable` widened to admit HWREC (subject to the existing 4-slot DR0-3 cap — a 5th+ site sets `hwrec_overflow` and names the reason `hwrec-overflow`); new opts hook `no_hw_record` skips arming, reproducing the pre-T6 fail-closed truncation on demand; `blind_rdtsc` now replays with real injected values (its rax/rdx overwrite no longer matters) and stays the witness for the per-step decode via `no_hw_record` rather than `force_replay` (naturally injectable now); `sc_then_cpuid`→`sc_then_sysenter` and `imp_vec`/`imp_cpuid`'s two single-step-fallback assertions were updated (`force_singlestep` pins the fallback path; is_pure() separately confirmed unchanged) since cpuid alone is now injectable, not merely gated; new live fixtures `hwrec_cpuid` (byte-identical vs oracle), `hwrec_rdtsc2` (two-site monotonicity, same-capture independent-oracle value check), `hwrec_rdrand_jc` (CF injection proven via same-capture self-consistency — not vec_compare, since rdrand draws a genuinely random value per forked capture), `hwrec_coldpath` (per-block claim: an unreached site costs nothing, `hw_hits==0 injected==0` yet `pure==1`), and a live run of `hwrec_5site` (overflow fallback, reason=`hwrec-overflow`); `make dataflow-blockstep-test` 191/191 (was 186/186, +5), stable across 5 consecutive runs; T7 done & live-verified on this Zen 2 host — new `asmtest_blockstep_extent_t` (`{off,len}`, blob-absolute) + opts `extents`/`nextents` (NULL/0 = today's whole-region sweep, unchanged); `region_scan` split into a per-extent inner sweep (`region_scan_extent`, one `cs_insn` allocation shared across all extents) whose verdicts aggregate across extents, called once per extent instead of once over the whole buffer — bytes outside every extent are never fetched, so an embedded island between two extents costs nothing; `run()` validates extents sorted/non-overlapping/inside `[region_off, code_len)` before any tracee spawns (`DF_BLOCKSTEP_EINVAL` otherwise) and converts blob-absolute to region-relative in a scratch array scoped to one call; the public `is_pure`/`is_replayable`/`is_injectable` classifiers stay whole-blob by design (extents are a `run()`-only capability, documented as such); new fixture `island_sse` (the existing `island` fixture's exact byte shape, with legacy-SSE `paddq` swapping in for `island`'s VEX-128 `vpaddq` — same 4-byte length, so all offsets are unchanged — since a genuine VEX-128 stays gated by the encoding rule regardless of extents and can never demonstrate T7's OWN claim) proves the positive case: WITHOUT extents it desyncs exactly like `island` (fail-closed, reason="decode", the negative control); WITH extents hopping the island's 2 data bytes it is byte-identical to the single-step oracle with stops cut 5→2; `make dataflow-blockstep-test` 199/199 (was 191/191, +8), stable across 5 consecutive runs; `make docker-dataflow-attach` 520/520 across all 8 suites, 0 skips; `make check` 54/54; `make docker-fmt-check` clean on both touched files; `make docker-docs` clean. **macOS portability fix (2026-07-20, validating agent):** two build breaks that only surface compiling `dataflow-test` on macOS-Intel — `examples/test_dataflow_ptrace.c` called five `test_window_*` fns unconditionally in `main` but defined them only inside the `__linux__ && __x86_64__` guard (added the missing `#else` stubs, like every sibling); and `examples/test_dataflow_blockstep.c` (T4's F2 `sc_pread` fixture) used Linux-only `memfd_create` unconditionally though the suite runtime-self-skips off Linux via the ENOSYS probe (added a compile-only non-Linux stub). `make dataflow-test` now builds + self-skips clean on macOS, `docker-fmt-check` clean. **Third macOS pass (2026-07-20, validating agent):** the same file's nine call-out/versioned `static const` fixtures (used only inside its `__linux__ && __x86_64__` block) also drew `-Werror,-Wunused-const-variable` under `make WERROR=1 dataflow-test` on macOS — guarded their definitions to match their use; `make WERROR=1 dataflow-test` builds + self-skips clean on macOS, Linux x86-64 WERROR compile re-verified, `docker-fmt-check` clean. See CHANGELOG [Unreleased] Fixed. **Independently re-validated ✅ 2026-07-22 (validating agent) from clean main (d8ff8ef) on this Zen 5 box:** `make docker-dataflow-attach` all 8 suites **0 not ok** (`dataflow-blockstep-test` **1..209**), with the load-bearing assertions green — T3 the 32-bit-write zero-extend discriminator `ok 32` (write rax→write eax→read rax produces NO stale-upper-half edge — the confirmed-MAJOR fabricated-edge fix), T5/T6 hwrec injectable `ok 61-67` (syscall/int0x80/rdtsc/cpuid/rdrand/hwrec_multi all give the forward pass a real DR post-retirement boundary), T7 extent ownership half-open `ok 4/7/8`.) | — |
| [dataflow-bindings-slice-codeimage.md](dataflow-bindings-slice-codeimage.md) — def-use/slice surface + code-image arg across bindings | 4 | ✅ 4/4 (T1, T2, T3: T2 wraps `defuse`/`forward_slice`/`backward_slice` (by-pointer seed) + the record-append surface in all seven remaining bindings — Ruby (Fiddle pack), Lua (LuaJIT ffi struct), Zig/Rust (extern/repr(C) struct), Go (cgo dlsym), Java (FFM MemorySegment), .NET (raw IntPtr, sidesteps the non-blittable-bool problem); T3 adds the counted TAP assertions over that surface to all seven `test_dataflow.<lang>` suites — a hand-built r10→r11→r12 register chain (`forward_slice(0)`/`backward_slice(2)` both `{0,1,2}`, register-only so it runs even where live-attach self-skips) and, over the shared live `df_chain` capture, the **memory** def-use edge (step1 store → step2 load) these seven could never slice before T1's by-pointer seed: `forward_slice(0)` and `backward_slice(4)` both `{0,1,2,3,4}`, excluding the trailing `ret`; anti-vacuity checked by temporarily inverting `put_mem`'s `is_write` in `src/dataflow_operands.c` (scratch build, reverted before commit, `git diff` clean) — the register-only chain checks stayed `ok` while both memory-edge checks went `not ok` (`forward_slice(0)` collapsed to `{0,1}`, `backward_slice(4)` to `{2,3,4}`), then confirmed `ok` again after rebuilding the revert; all seven `docker-dataflow-<lang>` lanes green at their new 40/40 (was 36/36), 0 skips, 0 failures (Ruby/Go/Rust/.NET cross-checked locally too); T4 done & live-verified — **widened to all ten bindings** (T4's own text names python/dataflow.py and node/dataflow.js alongside the seven, unlike T1-T3's seven-binding-scoped slice surface): a `CodeImage` wrapper (`asmtest_codeimage_new`/`track`/`now`/`bytes_at`/`free`) and `ValueTrace.attach_pid_versioned` land in Python, C++ (RAII), Node (mirrors the existing hwtrace-binding class), Ruby, Lua, Zig, Rust, Go, Java, and .NET; `attach_jit` no longer unconditionally passes NULL/null/nil for the versioned-decode `img` (C++ already threaded it structurally — this adds the first real-img test); every binding's live test tracks a recorder over a real victim's published region and decodes an `attach_pid_versioned` capture through it (result/step-count tied to that run's own args); all ten `docker-dataflow-<lang>` lanes green with the new assertions (`libasmtest_dataflow` self-test + live-img-thread checks), 0 skips — the container's soft-dirty page tracking was live (not a no-op `# SKIP`) in every lane, so this is the software tier's actual codeimage path, not a gated stand-in; no hardware/credential gate applies to this doc. **macOS portability fix (2026-07-20, third pass, validating agent):** the shared `bindings/dataflow_victim.c` (built by every `dataflow-<lang>` lane) unconditionally pulled Linux-only `<sys/prctl.h>` + `prctl(PR_SET_PTRACER,…)`, and `bindings/ruby/dataflow.rb` used Ruby-3.0 endless-method syntax that fails to parse on the Ruby 2.6 stock macOS ships (violating the gemspec's own `required_ruby_version >= 2.6`) — guarded the prctl to `__linux__` and rewrote the endless methods as classic single-line defs; `make dataflow-cpp-test`/`-python-test`/`-ruby-test` build + self-skip clean on macOS, `docker-dataflow-ruby` live-attach re-verified green on Linux. See CHANGELOG [Unreleased] Fixed.) | — |
| [dataflow-f4-object-identity.md](dataflow-f4-object-identity.md) — real object identity via GCBulkType/Node/Edge | 6 | ✅ 6/6 (T1: `asmtest_objid_*` + `test_dataflow_objid` 27/27; T2: post-capture `GCCANON_MOVE_POST` feed, `docker-gccanon-attach` 37/37 with `post_moves`>0 every phase; T3: EventPipe GCBulkNode/Edge/Type `gccanon_dumper`, live run nodes=15660 index_gaps=0 reason=Induced, `System.Int64[]` joined; T4: snapshot-space spike GO=POST (`GCBulkNode.Address==NewRangeBase`; dump GC's 15001 ranges all post-witness, 0 pre) + findings note; T5: alias fixture (LOH-compaction slide + accumulate-no-reuse so the traced object reaches the post-detach snapshot) + heap-snapshot objid join lane, `docker-gccanon-attach` green x2 — selftest 1..5, numeric 1..10/1..11/1..11 (37 landed, unchanged), alias 1..6 (false store/load edge FORGED by address identity, SEVERED by object identity, no collateral); numeric-phase objid calibration scoped out (SOH churn vs its 37 assertions — alias phase carries the live objid proof, T1 unit proves preservation); T6: retired the `likewise deferred` comment (points at the increment-4 block), CHANGELOG `[Unreleased]` Added entry, F4 plan UPDATE block + snapshot-space findings cited, `make docker-docs` clean, `make dataflow-test` 209 green. **Independently re-validated ✅ 2026-07-22 (validating agent) on this Zen 5 box:** `make docker-gccanon-attach` **0 not ok** — selftest 1..5, the three numeric phases 1..10/1..11/1..11, and the alias phase's live proof: `ok 4` NEGATIVE CONTROL (without canonicalize the store→load edge is missing), `ok 5` **THE POSITIVE — OBJECT identity SEVERS the false edge** (the dead store re-keys `X|NOOBJ`, the live load keys the node), `ok 6/7` no false alias forged (a slot-reusing object keeps a DIFFERENT key), `GCCANON_SUMMARY rc=0 post_moves=795 obj_edge=0 fail=0`; T1's `test_dataflow_objid` unit ran green inside the same lane (`ok 14` object identity severs the false edge).) | — |
| [dataflow-pt-replay-tier.md](dataflow-pt-replay-tier.md) — F5: PT + code-image + Unicorn-replay value tier | 5 | ✅ 5/5 (**T4 live leg live-verified 2026-07-21 on a bare-metal Intel PT box (Core i7-8559U) — `make dataflow-pt-live` 1..29 all pass, exit 0, stable ×3, via `--cap-add=PERFMON` in `asmtest-dataflow-attach`: both `live(20,22)`/`live(200,1)` foreign-pid captures ran (not skipped), the PT-decoded path == the single-step oracle path, and the F5 replay value trace is byte-identical to the emulator L0 with ZERO single-steps of the target. Surfaced the shared Intel-PT decode defect — see [intel-hardware-validation.md](../intel-hardware-validation.md) + CHANGELOG Fixed — plus a victim-liveness fix so the decoder can read the foreign victim's own `.text` through `attach_end`. Earlier text follows.** T1-T3 out-of-band PT+code-image+Unicorn-replay producer `src/dataflow_pt.c` + T5 F5 equivalence suite/`dataflow-pt-test`/`docker-dataflow-pt` landed & exercised green on this AMD Zen 2 host — NOT self-skips: `make dataflow-pt-test` 13/13 native (T1 byte-identity + raw memcmp + perturbed-path control, T3 purity/replay gates, T5 3 def-use edges + 4-step backward slice, + 2 block-step layout guards), `make docker-dataflow-pt` 19/19 with the synthetic decode→rebase→materialize→Unicorn-replay path byte-identical to the emu oracle for both taken(42) and not-taken(200) fixtures (`libipt-dev` added to `Dockerfile.dataflow-attach`, noble 2.0.6), `make docker-dataflow-attach` regression green (`dataflow-blockstep-test` 199/199 unchanged), `make dataflow-pt-live` correctly fails-not-skips (exit 2), `make docker-docs` clean. **Doc correction:** T2's `base_ip_out` on `asmtest_pt_decode_window` already landed via intel-pt-whole-window-substrate — reused, not re-added. **T4 wiring-complete 2026-07-20 (gated: bare-metal Intel PT silicon only — its second gate is now resolved):** the sibling **intel-pt-attach-foreign-pid** landed ☑5/5, so its foreign-pid capture (`asmtest_hwtrace_pt_attach_begin/track/poll/end`) is in the tree. `examples/test_dataflow_pt.c`'s live case was rewritten from a dead-code stub (`#if defined(ASMTEST_PT_FOREIGN_CAPTURE)` — a macro NEVER defined, so the body could never run even on PT silicon) into a REAL runtime-probed body: `asmtest_hwtrace_available(ASMTEST_HWTRACE_INTEL_PT)` gates it, and where PT is present it forks a deterministic victim, PT-captures ONE in-region invocation with ZERO single-steps of the target, subtracts the region base from the decoded absolute IPs to recover the offset path, replays it through F5's `_replay_path`, and asserts the value trace matches the emulator L0 AND the `force_singlestep` block-step oracle's executed path + GP result (both walks, taken + not-taken). The `test_dataflow_pt` link rule now pulls `HWTRACE_OBJS` (F5 CONSUMES the capture — position 9 honored, adds no capture code of its own) + `-ldl -lpthread` + `OPENCSD_LIBS`; two new block-step-oracle **layout guards** run on every host (caught here, before any PT box trusts the re-declared structs). **No compile-time gate any more** — the capture symbol links on every host, so the live body is reachable the instant a PT box runs it. Verified here: `make docker-dataflow-pt` **19/19** (WERROR-clean full-HWTRACE_OBJS+libipt link, T2 both walks, live half `# SKIP … no intel_pt PMU`), `make dataflow-pt-test` 13/13 native, `make dataflow-pt-live` (ASMTEST_REQUIRE_PT=1) fails-not-skips (exit 1), `make docker-fmt-check` clean. Live oracle-match leg stays GATED on bare-metal Intel PT + `perf_event_paranoid<0`/CAP_PERFMON — no reachable box; ✅ DONE 2026-07-21 — `make dataflow-pt-live` ran green on bare-metal Intel PT (see the ✅ note at the head of this cell)) | **intel-pt-attach-foreign-pid** (☑5/5, satisfied) |

### Intel PT & CoreSight hardware trace
| Document | Tasks | Status | Depends on |
|---|---|---|---|
| [intel-pt-whole-window-substrate.md](intel-pt-whole-window-substrate.md) — PT whole-window capture substrate, STRONG ladder, inline ctor | 5 | ☑ 5/5 (**C substrate live-verified 2026-07-21 on a bare-metal Intel PT box (Core i7-8559U) — `make hwtrace-pt-live` 1..632, 631 passed 0 failed, exit 0, stable ×4 (`WERROR=1` clean), via `--cap-add=PERFMON` in `asmtest-hwtrace`: the whole-window self-capture inits on real silicon, the decoded stream covers the TAKEN + not-taken walks (real TNT follow, `0xe` skipped/run), a 4 KiB AUX ring truncates, and the capture-side address filter + anon-region decode-time fallback both work. This was the FIRST run of the PT tier on real silicon and it surfaced a load-bearing decode defect (the code image covered only the target region, but a real capture's TIP.PGE lands in the caller → `-pte_nomap` → zero instructions) now fixed — see [intel-hardware-validation.md](../intel-hardware-validation.md) + CHANGELOG Fixed. T4's `new AsmTrace(HwBackend.IntelPt)` inline-ctor LIVE arm was EXERCISED here (libipt temporarily added to the dotnet image + `hwtrace-dotnet-test` under `--cap-add=PERFMON`): it ARMS and captures on real silicon (`ok AsmTrace(IntelPt): armed on Intel PT silicon — captured 863180 instructions`, PT window EXACT) — BUT the managed multi-threaded PT suite is non-deterministically racy: a run either SIGSEGVs (createdump: signal 11 SI_KERNEL, `addr (nil)` — a NULL deref, at a run-varying point) or flakes a JIT-timing compose check. The race is in the CONCURRENT T10/T11 ambient-hop / stitched-trace path (only live once libipt is present), NOT the single-threaded decode this change fixed; it needs dedicated concurrency work. The `libipt-dev` dotnet-image add was reverted (don't ship a racy privileged lane); **RESOLVED 2026-07-23 — the race is FIXED and `libipt-dev` is back in the dotnet image ([dotnet-managed-pt-concurrency-plan.md](../plans/dotnet-managed-pt-concurrency-plan.md), all of T1–T5).** It was NOT the managed-single-step hazard: `eu-stack` on the createdump cores put the fault in asm-test's OWN code — `asmtest_pt_read_codeimage` ← libipt `pt_insn_next` ← `asmtest_pt_decode_window` ← `asmtest_hwtrace_pt_hop_close` — an unsynchronized **code-image use-after-free**, the `JitMethodMap` EventPipe thread `realloc`ing `img->regions`/`r->vers` inside `asmtest_codeimage_track` while per-tid PT hop decodes walked those same arrays in `asmtest_codeimage_bytes_at`. `asmtest_codeimage_t` now carries a mutex (held across the array walk/append only, never across a decode, so the header's borrowed-bytes contract is unchanged), and `AsmAmbientStitchedTrace` orders its hop lifetime against Complete/Dispose. Measured on the i7-8559U PT box: **crash 7/7 → 0/8**; `make hwtrace-dotnet-test` under `--cap-add=PERFMON` **5/5 PASS (`ok=229`)**, the T4 inline-ctor arm live (`AsmTrace(IntelPt): armed on Intel PT silicon`) and the T10/T11 ambient chain now genuinely capturing (`ambient: >=2 stitched slices captured (3)`); PLAIN `docker-hwtrace-dotnet` still `1..225` (PT self-skips on **permission**, not a missing lib); new `docker-hwtrace-dotnet-ambient-stress` lane `1..576` over 25 iterations; `docker-hwtrace`, `docker-dataflow-attach` (incl. live `dataflow-pt-live` 1..29), `docker-fmt-check`, `docker-docs` all green. Marked `☑` (implementer-measured) — an independent **validating** agent stamps `✅`. See [intel-hardware-validation.md](../intel-hardware-validation.md). Earlier text follows.** T1–T5 landed & Docker-verified on this AMD host: `make docker-hwtrace` 556/556 (T1 pt_aux_* one-arm refactor + pt_begin/end_window pair; T2 begin_window PT arm + `base_ip_out`; T3 WEAK/STRONG ladder + runtime decode-trust probe, resolves to WEAK here, `pt_window_trusted`==`codeimage_available` in the libipt image), `hwtrace-dotnet-test` 191/191 (T4 `new AsmTrace(HwBackend.IntelPt)` inline ctor: `PtWindowCtx`/`Kind.PtWindow`, self-skip names the PT gate, leaked-scope finalizer clean), `docker-fmt-check` + `docker-docs` clean; T5 `test_pt_live_selfjit` + `asmtest_hwtrace_pt_set_filter` + `make hwtrace-pt-live` landed & self-skipping — `hwtrace-pt-live` **fails-on-no-PMU verified in-container** (`not ok`, Error 1). **LIVE PT capture GATED: bare-metal Intel PT + `perf_event_paranoid<0`/`CAP_PERFMON`** (no reachable box exposes `intel_pt`); `make hwtrace-pt-live` ran green on a bare-metal Intel PT box 2026-07-21 (see the head of this cell); `make hwtrace-dotnet-test`'s inline IntelPt arm still self-skips there until the dotnet image carries libipt. **macOS portability fix (2026-07-20, validating agent):** `asmtest_hwtrace_pt_begin_window`'s non-Linux `#else` arm returned `ASMTEST_HW_ENOSYS`, but the tier's one classifier (`hw_classify`) reports INTEL_PT as `EUNAVAIL` on any host without libipt — so `test_pt_window_pair_selfskip` failed on macOS. Fixed to return `EUNAVAIL` (matches the Linux `!available` arm); `make hwtrace-test` 143/143 native on the Intel-mac host, `docker-fmt-check` clean.) | — |
| [intel-pt-attach-foreign-pid.md](intel-pt-attach-foreign-pid.md) — PT attach-to-foreign-PID capture, facade dispatch, HV/EPT frontier | 5 | ✅ 5/5 (**live foreign-pid PT capture (T1/T2/T4) live-verified 2026-07-21 on a bare-metal Intel PT box (Core i7-8559U): the `asmtest_hwtrace_pt_attach_*` capture is consumed by `make dataflow-pt-live` (1..29 all pass, exit 0, stable ×3), which forks a victim, PT-captures ONE in-region invocation over the foreign pid with zero single-steps, and matches the single-step oracle byte-for-byte. Surfaced the shared PT decode defect (caller-coverage → `-pte_nomap` → zero instructions) fixed with the live-memory caller fallback + a victim-liveness fix; see [intel-hardware-validation.md](../intel-hardware-validation.md) + CHANGELOG Fixed. Earlier text follows.** T1/T2/T3/T4/T5 landed & green on this host; live foreign-pid PT capture hardware-gated. **T3** runtime-enabled jitdump attach — `docker-hwtrace-jit-dotnet-attach-jitdump` recovers `Program::Add` via CoreCLR `EnablePerfMap` over the diagnostics IPC socket [no `DOTNET_PerfMapEnabled`], `docker-hwtrace-jit-java-attach-jitdump` recovers `LHot;asmtjit(II)I` via `jcmd JVMTI.agent_load` of a **new in-tree attach-capable JVMTI jitdump agent** [`examples/jvmti_jitdump_agent.c`] — **doc correction**: linux-tools `libperf-jvmti.so` has no `Agent_OnAttach`, so the doc's prescribed jcmd-attach of it is refused; 5/5 + 4/4 ok. **T1/T2/T4** foreign-pid PT capture — `asmtest_hwtrace_pt_attach_begin/track/poll/end` (opaque `asmtest_pt_attach_t`, `src/hwtrace.c`) on the ONE `pt_aux_open` arm [pid>0, aux_watermark=aux/4, per-task, `obj_hint` HW filter S_ISREG-gated]; incremental `[aux_tail,aux_head)` de-wrap drain + foreign code-image recorder pairing [T2] + `asmtest_pt_decode_window` dispatch/absolute-rebase/truncation/software IP post-filter [T4] — one decode, not two. Portable PMU-free `asmtest_pt_aux_dewrap`/`asmtest_pt_ip_postfilter` unit-tested (7 checks, ok 136-142) + `test_pt_attach_selfskip` EINVAL/EUNAVAIL envelope; `make docker-hwtrace` **603/603**, `check-bindings-parity` OK [6 new `ALL`-exemptions], `docker-fmt-check` clean. Live foreign-pid capture gated on bare-metal Intel PT + CAP_PERFMON + same-uid ptrace. **T5** — Xen `vmtrace`/DRAKVUF `altp2m`/`libxdc` HV-EPT design record [`docs/internal/analysis/2026-07-19-pt-hypervisor-ept-frontier.md`], linked from the plan's Phase 2 deliverables; verified no-code invariant (no `src`/`mk`/Dockerfile ref). **✅ DONE 2026-07-21** — the live foreign-pid PT capture legs [T1/T2/T4] ran green on a bare-metal Intel PT box (Core i7-8559U) via `make dataflow-pt-live` (which consumes this capture); see the ✅ note at the head of this cell. The legs need bare-metal Intel PT + CAP_PERFMON + same-uid ptrace, absent on the AMD dev boxes. **Adversarial review** (26-agent workflow) then fixed a real gated-path bug — the software IP post-filter ran AFTER the absolute rebase, double-adding `base_ip` — plus `.NET` diag-IPC robustness [`WNOWAIT` so the caller's kill+reap can't hit a recycled pid; `SO_RCVTIMEO`/`SO_SNDTIMEO` so a wedged runtime self-skips instead of hanging] and the make-level `.PHONY`; re-verified `docker-hwtrace` 603/603 + dotnet-attach 5/5. **macOS portability fix (2026-07-20, validating agent):** two defects in T1's `test_pt_attach_selfskip` / `asmtest_hwtrace_pt_attach_begin` that only surface when the hwtrace suite is built on macOS (no Linux agent could): (1) `examples/test_hwtrace.c` had `<unistd.h>` only under `#if defined(__linux__)`, but `test_pt_attach_selfskip` calls `getpid()` on *every* host → macOS compile error — moved the include to the unconditional POSIX block; (2) `pt_attach_begin`'s non-Linux `#else` returned `ENOSYS` vs the tier's `EUNAVAIL` self-skip contract (`hw_classify`) → `test_pt_attach_selfskip` failed on macOS — fixed to `EUNAVAIL`. `make hwtrace-test` 143/143 native on the Intel-mac host, `docker-fmt-check` clean.) | **intel-pt-whole-window-substrate** |
| [coresight-live-decode.md](coresight-live-decode.md) — CoreSight live OpenCSD decode tree (AArch64 board-gated) | 5 | ◐ 1/5 (T1 landed & verified: `docker-hwtrace` now installs `libopencsd-dev` (noble 1.4.1) so `cs_backend.o` builds `-DASMTEST_HAVE_OPENCSD` exactly as the CI `hwtrace` job — build parity closed; `mk/native-trace.mk` scopes `OPENCSD_LIBS` to the module-present arm and appends `-lopencsd_c_api` (the C API lib the Debian `.pc` `-lopencsd` omits — the T3 tree will call `ocsd_create_dcd_tree` etc.), empty on a libopencsd-free host so no regression. `make docker-hwtrace` **615/615 + codeimage 60/60 + python 12/12, 0 failed** with the 3 hwtrace link lines carrying `-lopencsd_c_api` (dev symlink `libopencsd_c_api.so`→`.so.1` present, no undefined `ocsd_*` refs), CoreSight tier still self-skips (no `cs_etm` PMU; `present()` unchanged). **T2–T5 board-gated**: bare-metal AArch64 CoreSight board (ETM/ETE source + TMC-ETR/ETB/TRBE sink, lowered `perf_event_paranoid`); OpenCSD has no packet encoder so per the house "no untested hardware code" rule the decode tree + parity acceptance land together as one board-validated series — no reachable board here) | — |

### Intel Pin / SDE oracle lanes
| Document | Tasks | Status | Depends on |
|---|---|---|---|
| [pin-sde-future-isa-lane.md](pin-sde-future-isa-lane.md) — SDE future/absent-ISA test lane | 8 | ✅ 8/8 (T1 done: `scripts/third-party-digests.txt` `intel-sde 10.8.0` pin + `scripts/fetch-sde.sh` (fetch/verify/extract, echoes SDE_HOME; digest tamper-gate proven — wrong digest → exit 1, no kit left) + vendored `licenses/intel-sde-10.8.0/` (LICENSE.pdf + third-party-programs.txt + `pin licensing/`) + `refresh-thirdparty-digests.sh` emits the intel-sde line (URL-template reconstruction + digest recomputed = pinned value) + `licenses/README.md` row & test-lane-only note; `sde64 -help` → `Version: 10.8.0`. **T2+T3 done & green in-container** (`make docker-sde`): `Dockerfile.sde` builds a pinned, in-image-digest-gated SDE 10.8.0 + APX-capable GAS (binutils 2.46.1, `as --version` + APX probe pass) + NASM 3.02 + libunicorn; binutils/nasm digests added to the manifest + refresh emitter; `docker-sde` rule + `make help`. `mk/sde.mk` (`include`d) gates ARCH-first (x86-only real skip) then SDE-presence (fetch-pointer skip), and its `sde-null-test` proves transparency: native and `sde64 -future` TAP **byte-identical** on `test_arith`/`test_capture`/`test_mem`. Both self-skips + the live null test also verified locally on this x86-64 host. **T4 done & live-verified**: `asmtest_cpu_has_apx()` (CPUID leaf 7 subleaf 1 EDX[21]=APX_F) in `include/asmtest.h`+`src/asmtest.c`; `examples/apx_basic.s` (EGPR r16-r19 / REX2 lea / NDD add / EGPR-carry) + `examples/test_apx_basic.c` (4 cases, each gated on the APX probe), `SUITE_EXCLUDES`; `mk/sde.mk` `sde-apx-test` (assembler-capability probe → skip-honest on pre-APX GAS; else runs ONLY under SDE, greps TAP to fail if any apx case still skips) chained into `sde-test`. **The lane's reason to exist, proven on this NON-APX Zen 5 host**: bare `test_apx_basic` → all 4 `# SKIP APX not available` (CPUID gate closed on real silicon — no vacuous pass); `sde64 -future -- test_apx_basic` → all 4 `ok`, 0 skipped (emulated CPUID opens the gate, EGPR/NDD/carry execute under emulation). `make test`/`make check` unaffected (54/54); clang-format-18 clean. **T5 done & live-verified**: `mk/sde.mk` `sde-avx512-test` (chained into `sde-test`) runs the EXISTING `test_simd` under SDE and asserts `simd.avx512_adds_eight_doubles_512bit` AND `simd.avx2_adds_four_doubles_256bit` are BARE `ok` (anchored regex rejects `# SKIP`). Gate proven SDE-CPUID-driven on this AVX-512-native host: under `sde64 -hsw` (AVX2-only chip) the avx512 case reports `# SKIP AVX-512 not available`, under `sde64 -future` it runs `ok` — the un-skip is real, not host-AVX incidental. **T6 done & live-verified**: `examples/test_sde_crosscheck.c` (3 cases: gp_add, gp_flags [result+CF+ZF], sse_vec [4 f32 lanes]) modeled on test_emu.c; `mk/sde.mk` link rule + `sde-crosscheck-test` (unicorn-gated) chained into `sde-test`, `SUITE_EXCLUDES`. Runs the binary TWICE — bare (native vs Unicorn) AND under SDE (SDE vs **Unicorn-inside-SDE**); both 3/3 ok locally, confirming Unicorn's TCG runs correctly under SDE and the two engines agree on baseline ISA; clang-format-18 clean. **T7 done & live-verified**: `tools/sde_mix_report.c` parses SDE `-mix`'s global `*total` into `asmtest_trace_t.insns_total`, prints via `asmtest_trace_report()`, then echoes the `*isa-set-*` histogram; `mk/sde.mk` build rule + `sde-mix` (report, NOT chained into sde-test, SDE-gated). Verified: `make sde-mix` → `coverage: ... 226502 instructions` + 23 ISA-sets incl. AVX512F/BW; error paths (missing/malformed file) exit 1 naming the file; WERROR=1 clean, clang-format-18 clean. **Doc correction (empirical, SDE 10.8.0)**: the mix has NO `*isa-set-APX` line — SDE's XED tags the REX2/NDD-promoted legacy MOV/ADD/LEA as their BASE isa-set (I86), matching the pin-xed-trace-tier sibling's XED finding; the future-ISA-emulation proof in the histogram is instead the AVX512F/BW sets (which an AVX2-only CI host cannot run bare), plus T4's direct bare-#SKIP-vs-future-ok proof. **T8 done**: container-first `sde` job in `.github/workflows/ci.yml` (ubuntu-latest, `make docker-sde`); user guide `docs/guides/tracing/sde-testing.md` + toctree/backend-table rows in `docs/guides/tracing/index.md` (`make docker-docs` **-W clean**); `CHANGELOG.md` `[Unreleased]` Added entry; `make help` lists `sde-test` + `docker-sde`. **Final end-to-end re-verify GREEN on this x86-64 host**: a clean-rebuild `make docker-sde` (SDE/binutils/nasm layers re-run against the updated manifest) → in-image `GNU assembler ... 2.46.1` + `NASM version 3.02`, then `sde-null-test` TAP-identical, `sde-avx512-test` avx2+avx512 ran, `sde-apx-test` 4/4 no-skip, `sde-crosscheck-test` 3/3 both runs, `== sde-test: all SDE sub-lanes passed ==`, exit 0. NO hardware/credential gate fires — SDE emulates future ISA on any x86-64 (the lane's point). **Cross-host CI confirmation**: the new GitHub `sde` job ran GREEN on `ubuntu-latest` for commit `68ac9d3` ([run 29682108997](https://github.com/wilvk/asm-test/actions/runs/29682108997)) — the job log shows binutils 2.46.1 + NASM 3.02 built from source in the runner, then `sde-null-test` TAP-identical, `sde-avx512-test`, `sde-apx-test` 4/4, `sde-crosscheck-test`, `== sde-test: all SDE sub-lanes passed ==` — a genuine run of the full lane, not a skip. **Independently re-validated ✅ 2026-07-22 (validating agent) on this Zen 5 box** (which lacks APX silicon — the point of the lane): `make docker-sde` **0 not ok** across all sub-lanes — `sde-apx-test: all apx.* cases ran green under sde64 -future (no CPUID skip)` `ok 1-4` (EGPR through r16-r19, classic callee-saved preserved, NDD add, carry-CF), plus the crosscheck sub-lanes (`1..3`/`1..3`, 0 failed) — future-ISA instructions execute under SDE emulation rather than self-skipping on the absent silicon.) | — |
| [pin-xed-trace-tier.md](pin-xed-trace-tier.md) — XED-decoded Pin trace tier + shared pintool substrate | 9 | ✅ 9/9 (**live-verified 2026-07-22 by a validating agent on the Ryzen 9 4900HS (Zen 2) dev box: `make docker-pintool` GREEN, exit 0, 34 ok / 0 not ok — PIN-2 parity 11/11 (`pin/single-step insn+block offsets byte-identical` AND `pin/DynamoRIO insn+block offsets byte-identical`), APX decode 2/2 (`xed_classify_apx`: 3 EGPR/REX2 insns), plus the sibling pin-probe suite 21/21 in the same lane; the APX-execution halves stay hardware-gated as the doc records (`# SKIP pintool-apx: host CPU lacks APX (hardware gate)`) — recorded as gated, no reachable APX-F silicon. Earlier text follows.** T1 done: `scripts/fetch-pin.sh` fetches/verifies/caches the pinned Pin 4.2-99776-g21d818fa2 gcc-linux kit + vendors its license, digest recomputed twice, tamper-gate proven; T2, T3 landed: `Dockerfile.pintool` (in-image `fetch-pin.sh` digest gate + DynamoRIO for the T7 arm) + `docker-pintool`; `mk/pintool.mk` (x86-64/Linux arch gate mirroring `mk/cli.mk`), `pintool/{makefile,makefile.rules}` out-of-kit stub derived from the kit's MIT-SPDX scaffolding, null `asmtest_pintool.cpp` — built locally (`make pintool-tool`), loads + runs `/bin/true` rc=0, arch gate prints `1..0 # skipped`; **T4, T5, T6 landed & locally verified on this x86-64 host** against the pinned 4.2 kit — `pintool/pintool_shm.h` fixed offset-only channel + `examples/pin_trace_workload.c` (exports `asmtest_trace_begin/_end`, W^X ROUTINE, `result=7109`, `nm -D` marker present, native run leaves `insns_total=0`, `include/` clean); the real `asmtest_pintool.cpp` (RTN_FindByName marker gating, runtime block-head derivation, append/dedup/truncate discipline) + `examples/pin_trace_validator.c` + `pintool-test` → `make pintool-test` **9/9 ok** incl. `pin/single-step insn+block offsets byte-identical`, insns `[0,3,6,c,11,0,3,6,c,e,11]`, blocks `[0,0x11,0xe]`, totals 11/3/4; tool proven additive (plain `pin --` ⇒ zero counters); files clang-format-18-clean; **T7 landed & verified** — `make docker-pintool` **11/11 ok** incl. the full three-way parity `pin/single-step insn+block offsets byte-identical` AND `pin/DynamoRIO insn+block offsets byte-identical` (PIN-2's exit criterion), with the in-image `fetch-pin: verified` digest gate firing. **Doc correction:** `Dockerfile.pintool` builds Capstone from pinned source (the doc said omit it) — `asmtest_hwtrace_available(SINGLESTEP)` gates on the disassembler for block normalization (`src/hwtrace.c` decoder_present), so without Capstone the single-step parity arm could only self-skip in-container; per CLAUDE.md the dependency is added where the work runs. **T8 landed** — `pintool/pin_apx_fixture.h` hand-encoded REX2/EGPR (`mov/add r16,rsi; ret`) APX routine + `examples/pin_apx_decode.c` (ungated XED decode assertion), `pin_apx_workload.c`, `pin_apx_validator.c` (positive complete-trace + fork-isolated DR negative control), `pintool-apx-test` appended to `pintool-test`. **Doc correction:** the 4.2 kit tags a REX2-promoted legacy MOV/ADD as iclass MOV/ADD, ext BASE — not the `XED_EXTENSION_APX*` enums the doc expected — so the "genuinely APX" proof uses XED's purpose-built `xed_classify_apx()` (kit-verified: 3/4 insns APX). `make docker-pintool` runs the ungated decode assertion green (2/2 ok) on the non-APX CI host; **APX-silicon execution halves (Pin complete-trace + DR #6226 decoder-error negative control) GATED: no reachable APX-F CPU** — a real hardware gate (running r16/r17 code is a #UD), self-skips with the printed reason. **T9 landed** — CHANGELOG `[Unreleased]` Pin-lane entry, both stale "VNNI still breaks" claims corrected (analysis + plan docs, and a pre-existing CHANGELOG occurrence) to cite DR #5440 closed 2022-04-25 (PR #5444) with PIN-2's case resting on APX #6226 alone; `git grep "VNNI still break"` now hits only the spec meta-refs; `make docker-docs` clean. **Full lane green: `make docker-pintool` 11/11 parity + 2/2 apx decode.**) | — |
| [pin-probe-mode-capture.md](pin-probe-mode-capture.md) — Pin probe-mode argument/return capture | 7 | ✅ 7/7 (T1 substrate reuse; T2 `include/asmtest_valtrace_shm.h` + `examples/pin_probe_victim.c`; T3 `pintool/probe_capture.cpp` probe-mode entry/exit capture; T4 4 KiB pointed-to-buffer capture via `/proc/self/maps` mapped-range validation (**doc correction:** `PIN_SafeCopy` is JIT-mode-only); T5 per-target skip with reason via the `RTN_Size` probe-floor (**doc/Pin correction:** Pin 4.2 `RTN_IsSafeForProbedInsertion` is permissive; `RTN_InsertCallProbed` returns BOOL not VOID); T6 `examples/pin_probe_validator.c` + `make pin-probe-test` (in `docker-pintool`, `--cap-add=SYS_PTRACE`); T7 data-flow guide safety-posture section + CHANGELOG. **`make docker-pintool` GREEN in-container**: pin-xed PIN-2 11/11 + apx 2/2 AND pin-probe-test 21/21 — capture 12/12 with the LIVE independent ptrace producer agreeing (`ptrace rdi=11 rsi=22 rax=133 == pin`), badptr 5/5, skip 4/4 (`tiny`->TOO_SHORT); `make docker-docs` + `make check` 54/54 clean. **No hardware/credential gate** — Pin runs on this AMD host. **✅ live-verified 2026-07-21 by a validating agent on the Ryzen 9 9950X (Zen 5) dev box: `make docker-pintool` GREEN, exit 0, 34 ok / 0 not ok — pin-xed PIN-2 11/11 (pin/single-step + pin/DynamoRIO insn+block offsets byte-identical) + apx decode 2/2 AND this doc's pin-probe-test 21/21 (capture 12/12 with the LIVE independent ptrace producer agreeing `ptrace rdi=11 rsi=22 rax=133 == pin`, badptr 5/5 zero-length-record-not-fault, skip 4/4 `tiny`→TOO_SHORT). The one `# SKIP pintool-apx: host CPU lacks APX (hardware gate)` belongs to the sibling pin-xed doc's APX-execution halves, not this one.**) | **pin-xed-trace-tier** |
| [pin-libdft-taint-oracle.md](pin-libdft-taint-oracle.md) — libdft64 differential oracle for the DR taint tier | 7 | ✅ 7/7 (T1-T7 landed & verified on this x86-64 host. **T1:** `scripts/fetch-libdft.sh` git-commit-pins libdft64 @ `20804d5` + captures its Columbia BSD license; two manifest rows (3.20 `pin` + `libdft64`); tamper gates proven. **Doc/script corrections:** `fetch-pin.sh` did NOT serve the 3.20 kit as-authored — the 3.20 tarball has no `external` dir segment and ships its license as `licensing/EULA.txt`; two minimal, backward-compatible fixes let the one script serve both kits. **T2:** `Dockerfile.taint-oracle` + `docker-taint-oracle`. **Doc correction:** libdft64 + Pin 3.20 build cleanly on the tree-wide **ubuntu:24.04** under PinCRT/g++-13 (so no need to pin 20.04, which also lacks `libunicorn-dev`). **T3:** `pintools/libdft_oracle/oracle.cpp` — the libdft64 Pintool: seeds libdft's tagmap from the app markers (resolved by name), watches the 3 sink kinds in the fixture region (branch→flag-defining reg, call→rdi, rep-movs→rcx on first rep iteration), projects hits onto `at_taint_hit_t` over a POSIX shm channel (`include/asmtest_taint_oracle_shm.h`, fstat-gated to dodge a create/ftruncate SIGBUS) in the DR client's exact shape; + tool makefiles. Compiles+links under PinCRT/g++-13 against `libdft.a`, loads + runs under Pin 3.20 on this host's 6.17 kernel. **T4:** `examples/taint_fixtures.h` (fixtures moved VERBATIM from dr_taint.c — the DR lane proves no byte changed) + `examples/taint_oracle_modes.h` (shared per-mode config) + `examples/pin_taint.c` (drives the fixtures natively under `pin -t oracle.so`, fills the shm channel) + `pin-taint-test` (`mk/native-trace.mk`). Verified: `make pin-taint-test` 10/10 ok (seeded/sink/callarg/memlen each done=1 + expected libdft hit at off 0x10/0x6/0x3); `make dr-taint-native-test` byte-identically green (56/56) after the refactor. **T5:** `examples/taint_oracle_diff.c` — runs each fixture mode through BOTH the DR client (one DR lifecycle per process, like dr_taint.c) and libdft64 (fork pin over pin_taint) and asserts the sink sets are multiset-equal on `{off,ea,kind}`+tainted-bit, classifying divergence (DR-bug=RED / libdft-gap=named-skip / modelling-diff); `dr-taint-oracle-test` lane + `Dockerfile.taint-oracle` CMD; CHANGELOG `[Unreleased]` entry. all 10 `DR sink set == libdft64 sink set [ORACLE DIFF]` pass — sink kind1@0x10, callarg kind2@0x6, memlen kind0@0x3, plus propagation-only + negative controls all agree byte-for-byte. **T6:** `examples/taint_simd_fixtures.h` (XMM/SSE `simd_copy`/`simd_sink` moved VERBATIM from dr_taint_simd.c — `dr-taint-simd-test` 45/45 proves no byte changed) routed through the oracle as INFORMATIONAL modes: the diff prints `ok N # SKIP libdft-partial-sse-avx` with the DR/libdft delta (simd-copy agree; simd-sink DR=1 libdft=0 diverge — libdft's tool can't reproduce the SSE→GP→`test`/branch hit) and NEVER fails on it; the lane header enumerates all reserved gap tokens (`libdft-partial-sse-avx`, `libdft-gap-avx512-zmm`, `-eflags`, `-implicit-flow`, `-x87-fpu`, `-ternary-insn`). **`make docker-taint-oracle` GREEN: 62/62 ok (10 GP asserted + 2 SIMD named skips)**. **T7:** `taint-oracle` CI job (`.github/workflows/ci.yml`, x86-64 Linux, no macOS leg), `make help` line, CHANGELOG `[Unreleased]` entry finalized. **No hardware/credential gate applies — this host runs Pin, so the whole lane executes. ✅ live-verified 2026-07-21 by a validating agent on the Ryzen 9 9950X (Zen 5) dev box: `make docker-taint-oracle` GREEN, exit 0, 62 ok / 0 not ok — all 10 GP `DR sink set == libdft64 sink set [ORACLE DIFF]` assertions agree byte-for-byte (sink kind1@0x10, callarg kind2@0x6, memlen kind0@0x3, plus propagation-only + negative controls), and the 2 SIMD modes render as the reserved named skips (`simd-copy` DR=0/libdft=0 agree, `simd-sink` DR=1/libdft=0 diverge, both `# SKIP libdft-partial-sse-avx`, never a failure).**) | **pin-xed-trace-tier** |

### asmspy CLI
| Document | Tasks | Status | Depends on |
|---|---|---|---|
| [asmspy-cli-enhancements.md](asmspy-cli-enhancements.md) — TUI hot-edge drill-in, syscall-arg content decode, coverage gaps | 9 | ✅ 9/9 (all landed, `make docker-cli` cli-smoke PASS; T1-T4 sockaddr/ioctl/fcntl/futex/stat decode, T6-T9 group-stop/negative-n/aggregation/breadth smokes all CI-green. T5's live mode-7→Enter→mode-9 drill-in is pty-driven + AMD-IBS-gated so its interactive leg is manual-only — its DECISION logic (`asmspy_edge_drill`, 6 checks) is unit-tested in `test_autoregion`, which this doc also fixed to actually RUN in the smoke (was built-but-never-executed). Not yet ✅: T5's live drill-in needs a manual pty run on an AMD IBS host. **macOS portability fix (2026-07-20, 4th pass, validating agent):** `mk/cli.mk` gated `cli`/`cli-smoke` on ARCH only (`x86_64 aarch64 arm64`), not OS — so on macOS-**x86_64** (a supported arch) `make cli` fell through and HARD-FAILED compiling the Linux-only asmspy (`process_vm_readv`/`pthread_timedjoin_np`/`<elf.h>`/`<linux/futex.h>`/`<sys/prctl.h>`) instead of self-skipping; asmspy is a Linux ptrace/proc tool with no macOS body (macOS's stepper is the Mach tier). Added a Linux OS gate checked *before* the arch gate, mirroring the arch-gate idiom; `make cli`/`cli-smoke` now self-skip cleanly on non-Linux, and `make docker-cli` (in-container `UNAME_S=Linux`) falls through and builds asmspy + drives the full smoke as before. See CHANGELOG [Unreleased] Fixed. **Flaky arg-decode assertion — since FIXED (2026-07-20):** that same docker-cli run surfaced the syscall arg-decode smoke (`cli_smoke.sh:1541`) failing "no '...' anywhere". Root cause is NOT base-image drift (the earlier guess) but a NONDETERMINISTIC assertion: the only undescribed syscall in the stream was the incidental `restart_syscall` the kernel emits only when a signal interrupts a blocking call inside the 400-event window — it flaked to 0 matches (measured 1-of-N under Docker-Desktop's LinuxKit kernel). Fixed by having `argdecode_victim` make one DELIBERATELY-undescribed syscall (`sysinfo`, absent from asmspy's `arg_shape` table) so the honest unknown-arity `…` renders every iteration, and asserting on that specific call — a strengthening, not a weakening (the doc forbids weakening the assert). `make docker-cli` cli-smoke PASS restored end-to-end (verified). See CHANGELOG [Unreleased] Fixed. **T5 live-validated ✅ 2026-07-22 on this AMD IBS Zen 5 box (Ryzen 9 9950X):** drove the interactive TUI via a pty inside a `--cap-add=PERFMON --security-opt seccomp=unconfined asmtest-cli` container (host `paranoid=4` needs the cap) via a Python `pty` driver (scripted keystrokes + screen-scrape) — filtered the process list to `./build/auto_victim`, mode `7` opened the **live** AMD IBS-Op hot-edges view (`7 edges 2697/33340 branch/total samples THROTTLED` — NOT the IBS-unavailable self-skip), `space` froze it, navigated to the call-entry edge `grind_forever+0x1b → entered_often`, `Enter` drilled in → the data-flow view titled `… data flow of entered_often @ 0x… (pid 200, from hot edge)` **captured** it (`10 steps · 24 records · ret=… · entered_often`, full trace incl. the `imul rax, 0x9e3779b1` body + DEF-USE panel), and `ESC` returned to the still-frozen hot-edges table. **Doc correction from the live run:** step 7 / Done-when said "`b` returns to the frozen table" — but in the *captured* data-flow view `b`/`f` are the backward/forward **slice** keys; the real return key is **`ESC`** (the drill caller ignores `run_dataflow_view`'s return and redraws the frozen sample view). Fixed in the doc. Also confirmed: two ncurses-driving gotchas for any future TUI validation — reverse-video is SGR `0;7` (not bare `7m`), and with `keypad(TRUE)` the arrow keys are application-mode `ESC O B` (not `ESC [ B`; sending the latter reads as a bare ESC → quit). This was the doc's only ✅-gating leg (its interactive AMD-IBS pty run); with it green the doc is ✅ 9/9.) | — |
| [asmspy-aarch64-support.md](asmspy-aarch64-support.md) — single-step engine abstraction + NT_ARM_HW_WATCH watchpoints | 7 | ☑ 7/7 (**T7 CLOSED 2026-07-22 — the answer is a FOURTH outcome the task did not offer, and the arm64 `cli` leg is now GATING.** T7 asked whether AArch64 watchpoints actually fire on the runner, allowing {hit / slots-absent skip / armed-but-silent skip}. A standalone probe measured none of them: `NT_ARM_HW_WATCH` **slots=4**, `NT_ARM_HW_BREAK` **slots=6**, `debug_arch=8` — and `PTRACE_SETREGSET` on either returns **ENOSPC**. Slots are reported; the reservation is refused, so nothing arms and nothing can fire. Same fact kills the `--trace --tid=` frontier item T2 exposed (per-thread `NT_ARM_HW_BREAK` entry breakpoint) — one host property, two symptoms. Three changes: **(1)** asmspy names the measured reason instead of listing suspects (`asmspy_hwdebug_reason()` records it at the arm site → `# SKIP --watch: … host reports 4 watchpoint slots but refused to reserve one: No space left on device`; the old text guessed "qemu / seccomp / permission", all three wrong here — absent regset vs zero slots vs unreservable slots send an operator to three different places); **(2)** the smoke's `--trace --tid=` block takes the host-capability split `--watch` already had — named skip on AArch64, **strict on x86-64** — while still asserting what survives it: an unarmable entry must be an attach failure, never a false "never executed"; **(3)** a `--watch` success line ("value + PC captured, per-thread arming confirmed") that printed **unconditionally, including on the skip path**, now says which happened — that is exactly how a skip reads as a green. **Result: `make WERROR=1 cli-smoke` PASSES end to end on the arm64 runner** (two named skips, zero failures) and on x86 (`make docker-cli` PASS, every watch/`--tid` assertion still enforced), so **`continue-on-error` is removed — the leg gates on both arches**. Remaining gate is honest hardware: validating a real debug-register HIT needs an AArch64 host whose kernel will reserve a slot (bare metal, or a hypervisor that virtualises debug state). **T2 CLOSED 2026-07-22 — and the recorded diagnosis was wrong in a way that hid a much worse bug.** The open item read "the `--stream --tid=` single-thread filter never lands in the worker's hot function … a throughput/scheduling behavior, not a decode bug". Measured on the runner: **`--tid` is innocent** (on a fresh victim it returns a full single-thread trace, victim alive; `--tid` twice is fine too). The **whole-process `--stream` control run that precedes it was KILLING the target**, and `--tid` merely found the corpse. Root cause, reproduced in ~40 lines with asmspy out of the picture: on AArch64 a step armed on a thread parked in a blocking syscall **survives `PTRACE_DETACH`** — `ptrace_disable()` = `user_rewind_single_step()` + `user_disable_single_step()`, i.e. it SETS `SPSR.SS` and clears only `TIF_SINGLESTEP` — and fires as a fatal kernel `SIGTRAP` at `svc+4` when that syscall returns, **200-400 ms after asmspy exits**. Resume matrix on the parked leader: no-step ✔ survives / `PTRACE_SINGLESTEP` ✘ dies / `PTRACE_CONT` ✔ survives. So `asmspy --stream <pid>` was fatal to **every** multi-threaded arm64 target — and the smoke's own detach-survival tripwire could not see it, because it checked `kill -0` *immediately* and the victim is still alive then. **Fixed:** `step_resume()` resumes a thread poised on an `svc` with `PTRACE_SYSCALL` (call runs, thread stops on the way out, stepping resumes there — nothing armed across a blocking call, thread not dropped from the trace); `PTRACE_O_TRACESYSGOOD` + the existing `syscall_stop_is_entry()` classify the resulting stops, AArch64 only, x86 stop stream byte-identical. Two further defects the fix exposed, both fixed: the teardown drain stepped threads already *inside* a syscall (pc past the `svc`) which hung asmspy unkillably through `EINTR` — now guarded by `/proc/<tid>/syscall` — and its single wait could consume phase 1's queued `PTRACE_EVENT_STOP` instead of the step trap, leaving the step armed (x86 didn't care; `clear_trap_flag` disarms). Teardown-only fixes were tried on hardware and **refuted**: once armed inside a blocking call the step cannot be revoked (`PTRACE_CONT`+re-INTERRUPT ✘, `GETREGSET`/`SETREGSET` after the TIF clear ✘). **Verified live:** whole-process `--stream`/`--graph`/`--tree` each leave the victim ALIVE (all three killed it before) and a following `--tid` run returns 60/60 lines; x86 `make docker-cli` **cli-smoke: PASS**; the survival tripwire now polls ~2 s. **One self-inflicted regression on the way, caught by the gating x86 leg and fixed (1e454a9):** the new in-syscall drain guard was applied on both arches, but on x86 a thread inside a call is exactly the queued-#DB case the drain exists to service (x86 rewinds `rip` onto the `syscall` insn, so `at_syscall_insn` already covers the blocking shape) — skipping it left the trap queued, killed the victim after detach, and produced the SAME `--tid` symptom on x86. The guard is now AArch64-only. **T7 still open + one new frontier item:** with the kill gone the arm64 smoke runs ~1400 lines further and now fails at `--trace --tid=<runner>` (region engine's NT_ARM_HW_BREAK entry breakpoint — a different, pre-existing gap that was simply unreachable before), so the cli arm64 leg stays `continue-on-error`. T1/T3/T4/T5/T6 landed & host-exercised green on this x86-64 host; T2/T7 code-authored behind the arm64-CI gate. New `cli/asmspy_arch.h` register/step/watch arch seam + AArch64 `bl`/LR call-frame semantics + per-arch syscalls/`--log` + `NT_ARM_HW_WATCH` DBGWCR/DBGWVR/BAS control-word encoder, threaded across `cli/asmspy_engine.c`; build gate widened in `mk/cli.mk`; native `ubuntu-24.04-arm` leg added to `.github/workflows/ci.yml` (YAML-valid); user docs in `docs/guides/tracing/asmspy.md`. **Host-testable cores ran GREEN (not skipped):** `make docker-cli` cli-smoke **PASS** incl. `test_arch: PASS` (arch seam + the AArch64 DBGWCR/BAS encoder unit test, which validates the watchpoint control-word math even on x86-64 where no AArch64 watchpoint can arm) and the x86 `--log`/int3/detach-survival regression intact; `make docker-docs` clean (Sphinx -W); `make docker-fmt-check` clean (clang-format 18). **GATED (arm64 CI VM):** the AArch64 *behavioral* legs — single-step/detach-survival (T2, PSTATE.SS kernel-owned), register readback, `bl`/LR frame identity, `--log` syscall-name decode, native arm64 cli-smoke, and live watchpoint arm-and-fire (T7) — validate only on the native `ubuntu-24.04-arm` runner; qemu-user cannot substitute (`asmtest_ptrace_available()==0` under emulation). A validating agent runs the arm64 CI leg to close T2/T7 and reach ✅. **T2/T7 mostly live-validated 2026-07-20/21 (validating agent) — one item under on-hardware triage.** Drove the arm64 cli leg live across several rounds, fixing real x86 assumptions each time: **(r1)** `SYS_stat` compile error → `argdecode_victim` uses `newfstatat` on arm64, smoke asserts the exact per-arch failed-stat rendering; **(r2)** both jit victims' `HOT_CODE` was x86-only → A64 twins (assembler-cross-checked, SIGILL-at-startup otherwise), **5 hardcoded `ASMTEST_ARCH_X86_64` decode sites in `cli/asmspy.c` bypassed the `ASMSPY_HOST_ARCH` seam** (`--trace`/`--procs`/`--dataflow` rendered x86 mnemonics for arm64 bytes — the log showed `inc dword ptr [rbx…]` in an aarch64 process; `--stream` already used the seam), `--dataflow main` bounded-entry-wait now accepts the honest producer self-skip; **(r4)** the PLT scanner hardcoded `R_X86_64_JUMP_SLOT` + a 1-slot PLT0 → arm64 `R_AARCH64_JUMP_SLOT` + 2-slot PLT0 (verified on a real arm64 ELF under qemu: 1224 `@plt` stubs at objdump-exact addresses, was 0); **(r5)** `dup2` has no arm64 syscall → glibc routes to `dup3`, added a `dup3` SHAPE + arch-split assertion; **(r6)** `--procs` re-seize after `--stream`'s single-step detach raced on the loaded runner → fresh per-retry victim. `watch_decode_dir`'s x86 constant left as-is (its ends-at-RIP variable-length scan is x86 trap semantics by design, unreachable on the runner — watchpoints hypervisor-silent). **Green live on the arm64 runner:** build + `--stream`/`--log`/`--graph`/`--tree`/`--procs`/syscall-arg-decode/PLT-naming/`test_arch` watch-encoder unit. ~~**One OPEN item (cli arm64 leg now `continue-on-error`, x86 leg still gating):** `--stream --tid=` single-thread filter never lands in the worker's hot function on the loaded shared runner while the whole-process `--stream` control sees it fine — the single-tid and whole-process step loops are code-identical bar which threads are seized, so this is a single-thread-step throughput/scheduling behavior on a contended vCPU, not a decode bug; qemu can't reproduce ptrace to triage locally. Left for a validating agent on real arm64 hardware — the leg is staged non-gating meanwhile (T1 hwtrace-arm64 precedent).~~ **↑ REFUTED 2026-07-22 — see the head of this cell.** The reasoning that made it look like a throughput quirk ("the two step loops are code-identical bar which threads are seized") was correct and pointed the wrong way: because they ARE identical, the difference was never in the `--tid` run at all — it was in what ran BEFORE it. A hypothesis that explains a symptom by the properties of the failing command is worth one experiment on a FRESH target before it is written down as a finding.) | — |

### Single-step & block-step tiers
| Document | Tasks | Status | Depends on |
|---|---|---|---|
| [inproc-btf-block-step.md](inproc-btf-block-step.md) — W3: in-process BTF branch-granular single-step | 6 | ✅ 6/6 (`make docker-hwtrace-msr` live-verified by the implementer on this Zen 2 host — both fixtures byte-identical to the single-step baseline, 10/10 stable runs, zero truncation; `make docker-docs` clean. **Independently re-validated ✅ 2026-07-22 (validating agent) on this AMD Zen 5 box (Ryzen 9 9950X; the "Zen 2 host" above is a stale mislabel — the MSR/BTF path needs only the host `msr` module, present here at `/dev/cpu/0/msr`):** `make docker-hwtrace-msr` (`--privileged` + host `msr`) **684 passed, 0 failed** — `ok 270` in-process BTF complete-capture block partition `{0, 0x11}` matches the single-step/PT/AMD/DynamoRIO oracles, BTF pairing `ok 39-48` (gap-free 3-trip loop, ret/back-edge pairs, `decode_stitched` reconstructs all 11 insns, dual-guard ambiguity records NO pair — never guesses), MSR spec-filter `ok 34-38`, windowed block-step survives a mid-block fault `ok 528`. **Same-day cross-box confirmation (validating agent, Ryzen 9 4900HS — a true Zen 2, `msr` module loaded): `make docker-hwtrace-msr` 738 ok / 0 failed with the BTF legs LIVE — `# in-process BTF: rc=0 result=42 insns=5 truncated=0` (stream `[0,3,6,c,11]`, blocks `{0, 0x11}`, ok 242-246) and the 62-insn loop fixture complete past the 16-entry window (`insns=62 truncated=0`, ok 247-250); so the BTF arm is proven on BOTH the msr-capable boxes, with and without `amd_lbr_v2`. Run recorded in [amd-hardware-validation.md](../amd-hardware-validation.md) "Recorded runs".**) | — |
| [macos-oop-mach-stepper.md](macos-oop-mach-stepper.md) — macOS out-of-process single-step via Mach exception ports | 7 | ✅ 7/7 (**VALIDATED live on the target host 2026-07-20** — macOS 14.7.5 / Intel Core i7-8559U, the `intel-macos-x86_64-de7ec54c` box the doc's Constraints name — by an independent agent, not the implementer: `make mach-stepper-test` **25/25, exit 0, zero self-skips**, reproduced across two consecutive runs. Every live leg green: `trace_call` leaf (ret 20+22=42, stream `[0x0,0x3,0x6,0xc,0x11]`, complete) + the 62-insn loop fixture (no depth ceiling); `trace_attached` byte-identical to the in-process single-step stream; `run_to` on BOTH breakpoint paths — software-`int3` AND hardware-DR0 debug register — each resolving the in-flight call and recovering its trace. `task_for_pid` authorized via the ad-hoc `com.apple.security.cs.debugger` self-sign (`scripts/codesign-debugger.sh`), no sudo. Native baseline also brought green on this host: `make hwtrace-test` 143/143 (in-process stepper), `make check` 57/57, `make test` 0 failures, `make docker-fmt-check` clean. **Validating on real macOS-Intel silicon surfaced + fixed 3 pre-existing macOS defects in _sibling_ code** — see the Fixed notes on [intel-pt-attach-foreign-pid](intel-pt-attach-foreign-pid.md), [intel-pt-whole-window-substrate](intel-pt-whole-window-substrate.md), and [code-review-plausible-triage](code-review-plausible-triage.md); none were in this doc's own `mach_backend.c`/`asmtest_mach.h`.) | — |
| [aarch64-ptrace-single-step-validation.md](aarch64-ptrace-single-step-validation.md) — AArch64 ptrace stream validation + binding fixtures | 6 | ✅ 6/6 (**live-verified 2026-07-20/21 on the GitHub `ubuntu-24.04-arm` runner** — Azure Cobalt 100 / Neoverse-N2 real silicon, the doc's designated validation vehicle. All six tasks done; the two gating live legs green & stable across ≥3 consecutive runs. **T5 COMPLETE:** `hwtrace-bindings-arm64` runs the Go + Java wrappers' A64 ptrace fixtures LIVE (green 2×), plus the `hwtrace-arm64` python host step; the seven wrappers that gate their whole hwtrace suite on the x86-only in-process single-step backend self-skip on arm64 (running their ptrace section live is a per-binding follow-on, recorded). **T6 COMPLETE:** plan + native-tracing/hardware-tracing guides flipped from "pending real hardware" to validated-on-arm64, CHANGELOG `[Unreleased]` Added entry, `make docker-docs` clean. The one hardware-gated sub-leg — the forced-hardware `run_to` FIRING — measured **ARMED-BUT-SILENT** on this hypervisor (T2), recorded as a self-hosted-runner leg. **T1 COMPLETE** — `hwtrace-arm64` runs the whole ptrace tier LIVE on `ubuntu-24.04-arm` (run a53b5df: **205 ok assertions, zero unexpected skips**, attach/proc-resolve/run_to/callout all live) and is now GATING (`continue-on-error` dropped, floor 200). The hardening flushed out **5 real arch defects, all fixed**: `frame_insns_eq` `-Werror`; `test_debug_logging` on the x86-only SINGLESTEP backend; `test_stealth_window_inline` + `test_register_idempotent` x86-fixture SIGILLs; and the hang that ate two full runs — the AArch64 callout BLOB **clobbered x30** (its `bl H` overwrote the child's return address, so the region's `ret` jumped back to base+4 and tracer+tracee spun in-region to the 15-min timeout; root-caused from the run-29739614837 log via the unbuffered TAP tail + orphan pids). The fixture is now AAPCS64-correct (stp/ldp LR save/restore, bytes assembler-cross-checked) — `ok 90-93` live. Also: the assert step's skip-allowlist was replayed OFFLINE against the full log before it ever gated (3 honest x86-only skips added by name). **T2's measured answer (the doc's open research question): NT_ARM_HW_BREAK is ARMED-BUT-SILENT on this runner** — slots reported, arm accepted, debug exception withheld by the hypervisor; the forced-hw call-out self-skips with that exact named reason (a REAL hardware gate per the doc's Constraints — bare-metal moves to self-hosted-ci-runners). **Doc correction:** `docker-hwtrace DOCKER_PLATFORM=linux/arm64` can't build the baseline — `libipt-dev` is x86-only apt; the native arm64 runner path [no libipt] is correct. **T2** `NT_ARM_HW_BREAK` arm-and-fire probe (`probe_arm64_hwbp`, `examples/test_hwtrace.c`) replaces the stale "hardware breakpoints are x86-64 only" callout skip — forks a throwaway tracee, arms slot 0 with the backend's `0x1e5` control word, and classifies FIRES / NO_SLOTS / ARMED_SILENT hang-proof; `ci.yml` allowlist gains `NT_ARM_HW_BREAK` + a `::notice` recording the measured outcome. Probe compiles `-Werror`-clean against real aarch64 kernel headers (verified in an arm64 container) and runs hang-proof under qemu [NO_SLOTS]. **T3 COMPLETE** — `native-oop` ptrace features row in `tools/asmfeatures.c` (x86-64: available:true complete:true trace_insns=4=truth; arm64 3=3) + arm64 `bench-record`/artifact steps in the `benchmarks` job [`ASMTEST_BOX_ID=arm-linux-arm64-gha`, `ASMTEST_VIRTUALIZED=1`] + `cross-system-benchmarking.md` row; 3 existing x86 records untouched; **the box record is now COMMITTED** from the green run-29739614837 artifact: `benchmarks/boxes/arm-linux-arm64-gha/` with the live row `"tier":"native-oop","backend":"ptrace_singlestep","arch":"arm64","available":true,"complete":true,"trace_insns":3,"insns_truth":3` (math.add3, real silicon) and `virtualized:true` honored. **T4** arch-selected `ROUTINE_A64` fixtures across ALL TEN binding hwtrace suites with x86-only guards on every stealth/descent/window/movabs test; x86-64 byte-identical (`docker-hwtrace-bindings` 0 not-ok across all 9 lanes; python 26/26), qemu self-skips. **Doc correction:** dotnet self-skips wholesale on arm64 via its top-level single-step gate, so its ptrace subset is arch-corrected but running it live needs that gate relaxed [follow-on]. **T5 wip:** `hwtrace-bindings-arm64` job + the `hwtrace-arm64` python step are AUTHORED & pushed (nine docker lanes natively arm64, distinct buildx cache scope, anti-vacuity step hard-failing on the harvested per-binding ptrace-unavailability strings + per-lane banner presence; dotnet's top-level `single-step backend unavailable` wholesale skip is the one allowed line) — awaiting the first live run to declare done. T6 docs/plan/changelog flip DONE. **T5 live outcome:** the bindings job scopes to `docker-hwtrace-bindings-only` (builds each image, runs ONLY `hwtrace-<lang>-test`, NOT the x86-only base conformance suite the plain `docker-<lang>` CMD runs) — Go + Java run the ptrace tier live (their T4 A64 call_auto fixtures also arch-selected here, since the cascade's arm64 floor forks+ptraces the bytes); the singlestep-gated seven self-skip. Two rounds green. T6 flipped the plan's "one genuinely remaining front", both guides, and CHANGELOG; `make docker-docs` clean) | — |

### Scoped / managed whole-window tracing
| Document | Tasks | Status | Depends on |
|---|---|---|---|
| [zeroconfig-scoped-tracing-hardening.md](zeroconfig-scoped-tracing-hardening.md) — in-process guards, hygiene assertions, doc-tail | 9 | ✅ 9/9 (all landed & exercised green on this x86-64 Linux+Docker host — NO gates: T1-T3 whole-window deny-region/instruction-budget/SIGALRM-watchdog guards + public `begin_window_ex`/`window_guard` surface (`src/ss_backend.c`, `src/hwtrace.c`, `include/asmtest_hwtrace.h`); T4-T5 guards-fire + no-malloc/no-lock/lazy-arm hygiene assertions; T6 call-site-exact .NET auto-name; T7-T9 guide/parity-matrix/reference doc-tail. `make docker-hwtrace` **567/567, 0 failed** re-validated on merged main (+11 new: DENY/BUDGET/RING at ok323-325; WATCHDOG close/bound/no-hang + SIGALRM & ITIMER_REAL save-restore at ok326-331; malloc/mutex interposer hygiene), `make docker-hwtrace-dotnet` 191/191 (T6 `OpenScopeForNameTest:<line>` exact match; `HwTraceProgram.cs` integrated verbatim), `make check-bindings-parity` OK (128 tier symbols × 10 bindings; 2 new `ALL` exemptions for `begin_window_ex`/`window_guard`), `make docker-docs` clean (Sphinx -W), `make docker-fmt-check` clean. **Doc corrections:** spec line-anchors had drifted (resolved by symbol); one MyST slash-heading xref made page-level to satisfy -W; the AMD Zen4-floor skip-reason literal in `hw_classify` left to amd-branchsnap-lbr-docs per binding position #2. **Independently re-validated ✅ 2026-07-22 (validating agent) from clean main (9bc3b6e) on this x86-64 AMD Zen 5 box:** `make docker-hwtrace` **634 passed, 0 failed** (suite grew from 567; every zeroconfig assertion present + green — T1-T3 whole-window guards `ok 323-331`, T1 descend DENY/BUDGET/RING `ok 361-363`, T2 watchdog init/close/bounded `ok 364-366` + SIGALRM & ITIMER_REAL restore `ok 368-369`, T4/T5 hygiene `ok 389-400` incl. lazy-arm `ok 390`, no-region-slot `ok 398`, **no-malloc/no-lock under the armed SIGTRAP handler `ok 399`**, SIGTRAP restore `ok 400`); `make docker-hwtrace-dotnet` **230, 0 failed** (T6 auto-name `ok 52 OpenScopeForNameTest:<line>`, named-before-arm `ok 53`); `make docker-docs` -W clean (T7-T9); `make docker-fmt-check` clean. **Parity note:** `make check-bindings-parity` was RED on main — NOT from this doc, but from an unrelated symbol `asmtest_codeimage_read_live` added by `c7b4ef7` (Intel PT decode fix) to `asmtest_codeimage.h` without a wrapper/exemption; fixed separately in `7c4d328` (ALL exemption — it's an internal PT-decoder read primitive, consumed only by `src/pt_backend.c`), after which parity is `OK — 142 tier symbols x 10 bindings`. zeroconfig's own `begin_window_ex`/`window_guard` exemptions intact.) | — |
| [managed-wholewindow-compose.md](managed-wholewindow-compose.md) — live compose, safe managed-arm routing, ambient PT stitching | 12 | ☑ 12/12 (Track A [T1–T5] landed & Docker-validated on this x86-64 host — `make docker-hwtrace-dotnet` **196/196, 0 failed**; the compose set also runs green scoped under `make hwtrace-dotnet-unwarmed` [1..6, exit 0]. **Two re-verification findings, both from measured crashes, correct the doc's premise:** (a) forcing `GC.Collect(0)` on the single-stepped thread INSIDE an in-process TF whole-window is intermittently fatal — it trips the repo's OWN documented in-window thread-spawn footgun (SIGTRAP, exit 133) — so it's dropped; the unwarmed mid-window JIT itself supplies the live-window instruction noise the asserts need. (b) The INLINE OOP ctor `new AsmTrace(outOfProcess:true)`'s region-free `window_stop` stepper single-steps EVERY arming-thread instruction and ABORTS CoreCLR (SIGABRT, exit 134) whenever it steps a managed body — reproduced across unwarmed-first-JIT, resident, tiny-resident, byMethod on/off; a hard fail-fast that cannot be caught — so **T3** records the finding and defers the OOP compose to the RANGE-based `AsmTrace.Window` factory (`WindowLiveJitChecks`, which composes an unwarmed mid-window JIT because its stepper runs the JIT at NATIVE speed and steps only the window range), and the initially-added §E3 inline-ctor live-publish wiring was reverted. **T1** unwarmed IN-PROCESS compose (method first-JIT'd in-window; ok 180-183, JIT-noise > labelled, not truncated); **T2** mid-window re-tier decode-at-version check + new `JitMethodMap.ObservedVersions` (env-gated `ASMTEST_UNWARMED_RETIER`; runs live in the lane, SKIPs-not-fails when the runtime doesn't re-tier in-window, per the doc's never-flake rule); **T3** first suite coverage of the bare inline OOP whole-window ctor as a documented-finding gate; **T4** `hwtrace-dotnet-unwarmed` lane + docker wrapper + `make help`, scoped to the compose set via `ASMTEST_COMPOSE_ONLY` (`DOTNET_TC_AggressiveTiering` perturbs the strict byMethod/withRundown checks process-wide, so the lane runs only "the whole unwarmed compose set" the doc names); **T5** PT prong self-skips off Intel-PT silicon ("built without libipt"). `make docker-docs` clean. **Track B [T6–T9] landed & validated on this x86-64 host** — `make docker-hwtrace` **606/606** (+12: T8 `window_stop_blockstep` parity 4, T9 ss-managed-routes 8), `make docker-hwtrace-dotnet` green with the live safe-managed route resolving to `'oop'`, `make check-bindings-parity` 133 symbols, `make docker-fmt-check`/`make docker-docs` clean; host `test_hwtrace` 601/601 with the T8 block-step stream **byte-identical** to per-instruction and T9 8/8; adversarially reviewed (no correctness findings). **T6** `asmtest_hwtrace_managed_runtime_present()` (/proc/self/maps CoreCLR/JVM/Mono scan; `ASMTEST_ASSUME_MANAGED` override) + the distinct `ASMTEST_HW_EMANAGED` (-4): `ASMTEST_WHOLEWINDOW_SAFE_MANAGED=1` refuses the in-process TF arm on a managed process (default off = byte-identical). **T7** the .NET empty `AsmTrace()` routes a managed window via the unit-testable `ResolveWholeWindowRoute` → PT / §D3 OOP stepper / honest skip, never in-process TF (`ww.Route`); `ArmPtWindow`/`ArmOopWindow` factored out of the backend-keyed IntelPt + `outOfProcess` ctors and reused. **Doc correction (re-verified crash):** arming the region-free OOP window in a LIVE GC-active managed process aborts CoreCLR — the T3 finding, the ptrace stepper colliding with the runtime's thread-suspension; reproduced here at exit 134 even for an EMPTY OOP window deep in the busy suite, while the crashproof-showdown OOP leg survives only in a momentarily-quiet process — so `SafeManagedRoutingChecks` asserts the routing DECISION (route='oop') + a live in-process-TF default arm, and the live managed-body OOP capture stays proven on the range-based `AsmTrace.Window` factory. **T8** `asmtest_ptrace_trace_attached_window_stop_blockstep` (~4-10× fewer stops) wired into the stealth helper (default on; `ASMTEST_STEALTH_NO_BLOCKSTEP=1` forces per-instruction). **T9** `test_wholewindow_ss_managed_routes` (EMANAGED refusal installs no SIGTRAP disposition; default unchanged; managed probe alone inert). Also fixed a pre-existing docs-build break from the T1-T5 commit (a relative .md xref into the Sphinx-excluded `internal/` tree → GitHub URL). **Track C [T10] landed & host-validated on this Zen 5 host** — per-tid PT hop capture surface `asmtest_hwtrace_pt_hop_open`/`_hop_close` (opaque hop ctx, `src/hwtrace.c`) on the ONE shared `pt_aux_open`/`pt_aux_stop`/`pt_aux_close` arm [pid==tid per-task selector, cpu==-1, inherit=0; disable-then-drain [0,head) → `asmtest_pt_decode_window` decode-at-version → teardown]; no parallel PT open. **Doc correction:** the doc placed these in `src/pt_backend.c` — a libipt-DECODE-only TU that cannot reach the static perf-capture arm — so they landed beside the `pt_attach_*` sibling in `hwtrace.c` instead, honoring the one-PT-arm invariant the doc's own Constraints demand. `test_pt_hop_surface` proves the EINVAL/EUNAVAIL self-skip envelope AND, where libipt is built, drives `hop_close`'s decode leg through a SYNTHETIC AUX fixture (`asmtest_pt_encode_fixture`, test-only `asmtest_pt_hop_ctx_for_fixture` — NOT on the tier surface, forward-declared in the test) with zero PT silicon: offsets {0,3,6,c,11}/blocks {0,0x11}, the 0xe dec correctly skipped (the taken-TNT discriminator), exactly like `test_wholewindow_decode`. `make docker-hwtrace` **625 passed, 0 failed** (+9 pt-hop checks) + python 26/26, stable across two runs; `check-bindings-parity` OK (141 symbols; 2 new `ALL` exemptions for the hop pair, attach-sibling posture); `docker-fmt-check` clean. **Test gotcha fixed pre-land** (found via gdb — an initial docker-hwtrace SIGSEGV): the `CHECK(c,m)` macro expands `c` twice, so a side-effecting `hop_close(...)` inside a CHECK double-freed the ctx (use-after-free); both teardown checks now close ONCE into a local. Live per-tid capture hardware-gated (bare-metal Intel PT + CAP_PERFMON + per-tid access), self-skips here — validation a separate agent's job. **T11 landed & host-validated (self-skips as designed)** — `AsmAmbientStitchedTrace` (`bindings/dotnet/hwtrace/HwTrace.cs`, beside `AsmStitchedTrace`): the ZERO-call ambient escalation. A static `AsyncLocal<AmbientScope>` built with a value-changed handler opens a per-tid PT slice (T10 `hop_open`) when the flow LANDS on a thread [ThreadContextChanged && CurrentValue!=null] and decodes-at-disable (`hop_close` against the scope's `JitMethodMap` trackBytes code-image as of `ImageNow`) when it LEAVES [CurrentValue==null], parks each trace lock-free (`ConcurrentDictionary`/`ConcurrentQueue`/`Interlocked` seq, handler swallows all — the `PublishLoop` posture), and stitches via `asmtest_hwtrace_stitch_handles` at `Complete`. Capture behind an internal `IHopCapture` seam (real = T10 P/Invoke; T12 injects a stub). PT-ONLY by construction: off Intel PT the scope sets `SkipReason` and runs the body UNINSTRUMENTED (never in-process TF). New P/Invokes `asmtest_hwtrace_pt_hop_open`/`_hop_close` (dotnet now WRAPS the T10 pair — the `ALL` exemptions stay used by the other 9) + `asmtest_ss_self_tid` + scripted-fill `trace_append_insn/_block` (asmtest_trace.h, not a tier symbol). `AmbientStitchChecks` live half self-skips cleanly here (r=42 uninstrumented, reason names the Intel PT gate). `make docker-hwtrace-dotnet` **1..216, 0 failed** (ambient ok 84–85 + SKIP), stable ×2; `check-bindings-parity` OK (141); guide "Ambient stitched operations (opt-in, Intel PT)" subsection + CHANGELOG; `make docker-docs` clean. Full live chain (live managed runtime ON bare-metal Intel PT) hardware-gated — no CI coverage; the hook→tag→merge LOGIC is T12's host-testable twin. **T12 landed & host-validated on this x86-64 host** — the ambient hook→tag→merge logic twin: a scripted `IHopCapture` stub (`ScriptedHopCapture`, `HwTraceProgram.cs`) whose `Open` records the attaching tid + hands back a sentinel ctx and whose `CloseDecode` fills a fixed 3-insn/1-block pattern via `trace_append_*` — the .NET twin of `test_stitch_hops_scripted`, runs with ZERO PT hardware. `AmbientHookTwinChecks` asserts the AsyncLocal handler fires the attach/detach pair (the documented `ResetThreadPoolThread` contract — an attach with CurrentValue!=null on a pool thread, a detach with ==null), every attach parks a seq-dense slice whose tid matches the recording thread, `stitch_handles` preserves order + ascending bounds, and a synchronous no-hop scope degenerates to exactly one slice on its own ctor thread. Invariant-based (opened==closed==stitched, seq-dense) so thread-pool nondeterminism can't flake it — proven: 5-hop async runs stable ×2. Also hardened `Complete`'s open-hop drain to `TryRemove` (atomic vs a detach racing close). `make docker-hwtrace-dotnet` **1..226, 0 failed** (twin ok 86–95) AND `make docker-hwtrace-dotnet9` **1..226** (the forward-runtime drift tripwire — the contract holds on .NET 9); `check-bindings-parity` OK (141). **Doc note:** all 12 tasks are code-complete; the LIVE PT legs — T10 per-tid capture and T11's full ambient chain (a live managed runtime ON bare-metal Intel PT + CAP_PERFMON) — stay hardware-gated with no reachable box here, so ☑ (code landed), ✅ left for a validating agent on PT silicon. **UPDATE 2026-07-23 — T10/T11 have now RUN LIVE on a bare-metal Intel PT box (Core i7-8559U, `--cap-add=PERFMON`), and doing so surfaced + fixed the bug that had been blocking them ([dotnet-managed-pt-concurrency-plan.md](../plans/dotnet-managed-pt-concurrency-plan.md) T1–T5).** The first live run of the ambient chain SIGSEGV'd 7/7; `eu-stack` on the createdump cores pinned it to asm-test's own `asmtest_pt_read_codeimage` ← `pt_insn_next` ← `asmtest_pt_decode_window` ← `asmtest_hwtrace_pt_hop_close` — an unsynchronized **code-image use-after-free**: T11's `JitMethodMap` EventPipe callback calls `asmtest_codeimage_track` (which `realloc`s `img->regions`, and `r->vers` via `ci_region_add_version`) on the runtime's listener thread while every T10 hop close decodes against that same image on pool threads, walking those arrays in `asmtest_codeimage_bytes_at`. Fixed by giving `asmtest_codeimage_t` a mutex (held across the lookup/append only — never across a decode — so the header's "borrowed bytes valid until `asmtest_codeimage_free`" contract is untouched, per-version byte buffers being separately allocated), plus a companion lifetime fix in `AsmAmbientStitchedTrace`: the `_completed` check is now atomic with the `_openHops` add/remove (a hop that loses the race to `Complete` tears its PT event down instead of parking it), and `Dispose` waits in-flight `CloseHop`s out before freeing `_map`/`_parked` (bounded, leaking rather than freeing under a stalled hop). **Measured: crash 7/7 → 0/8; `make hwtrace-dotnet-test` under `--cap-add=PERFMON` 5/5 PASS (`ok=229`)** with T11 genuinely capturing (`ambient: >=2 stitched slices captured (3)` — not a self-skip) and T10's per-tid `hop_open`/`hop_close` exercised live on every iteration. New T4 regression lane `make docker-hwtrace-dotnet-ambient-stress` loops the concurrent set (`1..576`, 25 iterations, exit 0). Regressions green: `docker-hwtrace`, `docker-dataflow-attach` (incl. live `dataflow-pt-live` 1..29), PLAIN `docker-hwtrace-dotnet` `1..225`, `docker-fmt-check`, `docker-docs`. Still `☑` not `✅` only because of the README's implementer/validator split — this was the implementing agent; an independent validating agent re-running the above on PT silicon stamps `✅`. **macOS portability fix (2026-07-20, validating agent):** three defects that only surface on macOS-Intel — (1) `asmtest_hwtrace_pt_hop_open`'s non-Linux `#else` returned `ENOSYS` while the tier classifier reports Intel PT as `EUNAVAIL` off libipt, so T10's `test_pt_hop_surface` self-skip failed on macOS (fixed to `EUNAVAIL`, the same fix already applied to `pt_begin_window`/`pt_attach_begin`; the T10 hop pair reintroduced it); (2) `examples/test_hwtrace.c`'s `map_exec` helper drew an unused-function `-Werror` under `make WERROR=1 hwtrace-test` on macOS (every caller is Linux-guarded — guarded the definition to match, like `frame_insns_eq`); (3) the T10/T11 region-free §Z1 whole-window is Linux/x86-64-only (`begin_window` self-skips on macOS single-step), but the C++/Ruby/Lua/Zig/Rust binding window tests hard-asserted `w.armed` — they now guard the arming-dependent checks on `armed` and note the honest self-skip, matching node's shape and the C `test_wholewindow_singlestep` skip. `make WERROR=1 hwtrace-test` 145/145, `hwtrace-cpp-test` 58/58 + `hwtrace-ruby-test` 57/57 (the macOS CI leg's binding set) green, `docker-fmt-check` clean. **Third macOS pass (2026-07-20, validating agent):** that list omitted the Python binding — `bindings/python/tests/test_hwtrace.py`'s `test_window_region_free_whole_window` still hard-asserted `w.armed` and failed on macOS. Now guarded on `armed` with an honest self-skip note, matching node's and cpp's shape; `make hwtrace-python-test` 15 passed / 12 skipped on macOS, the Linux armed-path unchanged. See CHANGELOG [Unreleased] Fixed. | **intel-pt-whole-window-substrate** (PT prongs only; D3-stepper prongs are independent) | **macOS portability fix (2026-07-20, validating agent):** three defects that only surface on macOS-Intel — (1) `asmtest_hwtrace_pt_hop_open`'s non-Linux `#else` returned `ENOSYS` while the tier classifier reports Intel PT as `EUNAVAIL` off libipt, so T10's `test_pt_hop_surface` self-skip failed on macOS (fixed to `EUNAVAIL`, the same fix already applied to `pt_begin_window`/`pt_attach_begin`; the T10 hop pair reintroduced it); (2) `examples/test_hwtrace.c`'s `map_exec` helper drew an unused-function `-Werror` under `make WERROR=1 hwtrace-test` on macOS (every caller is Linux-guarded — guarded the definition to match, like `frame_insns_eq`); (3) the T10/T11 region-free §Z1 whole-window is Linux/x86-64-only (`begin_window` self-skips on macOS single-step), but the C++/Ruby/Lua/Zig/Rust binding window tests hard-asserted `w.armed` — they now guard the arming-dependent checks on `armed` and note the honest self-skip, matching node's shape and the C `test_wholewindow_singlestep` skip. `make WERROR=1 hwtrace-test` 145/145, `hwtrace-cpp-test` 58/58 + `hwtrace-ruby-test` 57/57 (the macOS CI leg's binding set) green, `docker-fmt-check` clean. **Third macOS pass (2026-07-20, validating agent):** that list omitted the Python binding — `bindings/python/tests/test_hwtrace.py`'s `test_window_region_free_whole_window` still hard-asserted `w.armed` and failed on macOS. Now guarded on `armed` with an honest self-skip note, matching node's and cpp's shape; `make hwtrace-python-test` 15 passed / 12 skipped on macOS, the Linux armed-path unchanged. See CHANGELOG [Unreleased] Fixed. **Independent host re-validation 2026-07-23 (validating agent), clean main `f7d43bc` on this AMD Zen 5 box (Ryzen 9 9950X):** the entire host-runnable surface is green — `make docker-hwtrace` **638/0** (T8 `window_stop_blockstep` block-step stream byte-identical to per-instruction, T9 `ss managed routes` EMANAGED refusal + distinct sentinel, T10 pt-hop `EINVAL`/`EUNAVAIL` self-skip envelope + the synthetic-libipt AUX-fixture decode twin, T11/T12 `stitch_handles` scripted-hop merge), `make docker-hwtrace-dotnet` **230/0** (T7 empty-`AsmTrace()` routing, T11 ambient self-skip naming the Intel PT gate, T12 ambient-twin `ok 86–95`: 5/5 attach↔detach pairs, seq-dense stitch, pool-thread hop; safe-managed `route=oop`), the `make docker-hwtrace-dotnet9` forward-runtime tripwire **230/0** (the ambient twin's invariants hold on .NET 9), `make docker-fmt-check` clean, and `check-bindings-parity` **142 tier symbols × 10 bindings in sync**. The two **live PT legs stay gated and self-skip here** (T10 per-tid live capture + T11 full ambient chain need bare-metal Intel PT + CAP_PERFMON), so this stays **☑** — `✅` remains a PT-silicon validating agent's call, per the note above; no drift found on the host surface. **Gotcha for the next validator:** run `check-bindings-parity` on the *host* (or any git-trusting context), NOT in a bare `docker run -v $PWD:/src asmtest-ci` — the script's `git grep` silently returns nothing under git's dubious-ownership guard there and prints a false RED flagging every symbol in every binding as missing. | **intel-pt-whole-window-substrate** (PT prongs only; D3-stepper prongs are independent) |

### macOS
| Document | Tasks | Status | Depends on |
|---|---|---|---|
| [macos-cleanroom-lanes.md](macos-cleanroom-lanes.md) — tart arm64 / Docker-OSX x86 shakedowns + sshpass containerization | 6 | ◐ 3/6 (**T6 done & live-verified GREEN 2026-07-23 on the healthy-TSC Ryzen 9 9950X Zen 5 box (`will-X870-EAGLE-WIFI7`, `current_clocksource=tsc`, clean TSC calibration, no warp — the exact unblock the 4900HS attempt lacked).** One-time Ventura **13.7.8** install ran headless to completion over VNC (`vncdotool`, no freeze at image-default SMP=4) → reusable disk `build/osx/mac_hdd_ng.img` (account `user`/`alpine`, Remote Login on); then `DOCKER_OSX_DISK=… DOCKER_OSX_CPU=Haswell-noTSX-IBRS make docker-osx-bindings` exited **rc=0, stable ×2** — `clean-room-test: OK` on `darwin-x86_64`, **ruby PASS** (the fresh gem resolved its bundled `native/darwin-x86_64/libasmtest_emu.dylib`, NOT a dev-`build/`/Homebrew/`/usr/local` leak), python/node/lua/java/dotnet/hdr SKIP (toolchain-free guest = expected green shape). **Resolved T5's `IMAGE_PATH` hedge**: the `:latest` image DOES honour `-v <disk>:/image -e IMAGE_PATH=/image` (prebuilt disk boots; no `Dockerfile.naked` fallback) and `NOPICKER=true` auto-boots the installed `macos` entry. Two runbook corrections: `systemsetup -setremotelogin on` is refused on Ventura ("requires Full Disk Access") → enable via System Settings → Sharing → Remote Login; and each fresh-container run re-downloads the ~850 MB recovery before QEMU (budget ~5 min). Only **T1–T3 remain (Apple-Silicon-gated)**. Prior attempt (context): **T6 2026-07-22/23 — BLOCKED on a warped-TSC 4900HS boot; hardening landed, lane was UNVALIDATED at that point** (commit c7b0c8c). First real execution, Ryzen 9 4900HS Linux box (KVM gate open: `/dev/kvm` user ACL, verified through Docker). Landed from the attempt: lane script hardened live — EXTRA `-display none` (gtk default is fatal in an X-less container), `-di` (a closed stdin EOFs the `-monitor stdio` monitor and QEMU quits), `DOCKER_OSX_CPU/SMP/CORES/VNC/CPUSET` passthroughs (image's Penryn default spins newer macOS userlands → `Haswell-noTSX-IBRS`; automatic core-pair `--cpuset-cpus` pin when clocksource≠tsc), the tree-copy tar now excludes the in-repo guest disk (would have shipped the guest its own live qcow2), clean-room-test.sh SKIPs the C-header check on a non-functional macOS CLT cc stub (toolchain-free guests; Linux branch unchanged) — plus new operator runbook `docs/internal/docker-osx-linux-host.md` (host-backed sparse qcow2, VNC-driven install incl. the vncdotool shift-key gotcha, UpdateBrain.zip transient, `macOS Install Data` cleanup, docker-commit recovery cache, snap-Docker AppArmor/mount quirks). The one-time Ventura install FROZE FOUR TIMES — SMP=4: late guest boot; SMP=1: ~35 min in mid-download; SMP=1+core-pair pin: ~15 min; +CPUID tsc-deadline masked: ~5 min — same measured signature each time (guest UI pixel-frozen incl. spinners, ZERO guest disk+net I/O, vCPU thread 100% split ~half user/half kernel = KVM exit storm, QEMU main loop idle). Root cause: this boot measured `4680 cycles TSC warp between CPUs` → `Marking TSC unstable`, clocksource demoted to hpet, tsc REMOVED from available_clocksource (journalctl; macOS guests are TSC-only and freeze under load on such a boot — no SMP/pin/CPUID combination survives, all measured). **Unblock (DONE in the 2026-07-23 GREEN run above): use a host whose `current_clocksource=tsc`, then redo the install and run the lane — which is exactly what the Zen 5 box did.** The interrupted prior session had pre-written GREEN status flips into the working tree before the run happened; this session REVERTED them all — plan/clean-room-testing.md/Makefile/mk/script banner state measured reality, no green claim anywhere. `make docker-docs` green post-revert; `sh -n` clean. T4 done: `Dockerfile.sshpass` + `make docker-sshpass` build `asmtest-sshpass` (sshpass 1.09-1, verified against a live noble apt-cache policy check, not just the doc's claim); `scripts/docker-osx-bindings.sh` runs every ssh call through it, no host sshpass/sudo; stdin piping through the shim verified (`echo hello \| docker run --rm -i asmtest-sshpass sh -c 'cat \| wc -c'` -> `6`); off-KVM, `make docker-osx-bindings` builds the sshpass image first then still fails fast at the existing `/dev/kvm` guard (dependency ordering confirmed live). T5 done: `DOCKER_OSX_IMAGE` defaults to `:latest` (`docker manifest inspect` confirms `:latest` resolves and `:ventura` 404s, live); added `DOCKER_OSX_DISK` prebuilt-disk support with the conditional `-v/-e IMAGE_PATH` docker-run args (both branches — disk set vs. unset vs. a nonexistent path — unit-verified in isolation since the KVM guard makes the live path unreachable on this host); one-time-install recipe condensed into the script header; docs/clean-room-testing.md Track D row + notes updated (host col drops sshpass, notes the tag reality + DOCKER_OSX_DISK requirement); `user`/`alpine` no longer promised as shipped-in credentials. T1–T3 (Apple Silicon) remain untouched — no such hardware has been available; `make docker-docs` clean) | — |
| [macos-dynamorio-port.md](macos-dynamorio-port.md) — DynamoRIO native-trace port M0–M2 (~~gated on upstream DR macOS release~~ — **unblocked 2026-07-22 via source fork**) | 11 | ✅ 11/11 (**✅ 2026-07-22 (validating agent): the doc's stated ✅ criterion — the nightly/dispatched CI lane re-running green with the T6-extended target — is MET: dispatch [run 29906179941](https://github.com/wilvk/asm-test/actions/runs/29906179941) on a fresh `macos-15-intel` runner ran M0 15/15 + the T8 `codesign -f` step + the T6 generated-bytes harness 18/18, `0 failed`, no self-skip — the second independent green dispatch after the implementer's [run 29903901986](https://github.com/wilvk/asm-test/actions/runs/29903901986), and [run 29905598418](https://github.com/wilvk/asm-test/actions/runs/29905598418) makes three. The two Apple-Silicon legs stay recorded as gated per the README legend.**) (**gated: T6 Rosetta leg + T8 arm64 acceptance — both need Apple Silicon, T8 additionally an arm64/universal DR runtime (upstream i#5383)**) (**doc complete on the implementable surface 2026-07-22, Intel-mac dev host.** The last tranche (T6/T7/T8/T9/T11) is summarized in the port doc's second UPDATE block; accounting notes: **T2 counts as superseded** — the fork-build doc recorded the runtime layout/lipo/cmake reality T2's goal asked for, no release tarball having ever existed to inspect; **T11 needed no separate implementation** — fork-build FB4's dispatch-validated `drtrace-macos` job IS that task revised for the fork reality (nightly/dispatch-only, `macos-15-intel`, fork-commit pin + script/digest cache key, self-skip-is-failure), and T6's target extension feeds the generated-bytes harness into that same lane with no YAML change. ✅ is a validating agent's call: the nightly/dispatched CI lane re-running green with the T6-extended target, plus the two Apple-Silicon-gated legs whenever that hardware exists. **CI dispatch evidence (same-day, implementer-run): [run 29903901986](https://github.com/wilvk/asm-test/actions/runs/29903901986) `drtrace (macOS x86-64, nightly)` SUCCESS on a fresh `macos-15-intel` runner — M0 `# 13 passed, 0 failed`, the T8 `codesign -f` step executed, then the T6 generated-bytes harness `# 18 passed, 0 failed`, no self-skip — the extended lane reproduces off the dev host.** **T6 done & live-verified 2026-07-22 on the Intel-mac dev host** (gated: Rosetta leg — no Apple Silicon here): `drtrace-test-macos` now also builds+runs the generated-bytes `test_drtrace` harness (`DRAPP_KEYSTONE=0`, base harness only — val/taint stays Linux-only) after the M0 harness — **18/18 ok on native Intel** (exec_alloc `PROT_NONE→RW→RX`, coverage accumulation, exact instruction stream `[0x0,0x3,0x6,0xc,0x11]`, cap-1 truncation bit, symbol mode) + 13/13 M0 in the same invocation, three consecutive runs 31 ok / 0 not-ok against the fork-built home; M1a **native-Intel PASS** + Rosetta "not run — no Apple Silicon hardware" recorded in the plan's M1a section; native-tracing.md backend row + macOS section + CHANGELOG amended; `make docker-drtrace` re-run GREEN (the edit lives inside the Darwin-only branch), `make docker-docs` clean. **T7 done & verified 2026-07-22**: `exec_alloc_platform()`/`exec_copy()` platform seam landed in src/drtrace_app.c — arm64-macOS arm = `MAP_JIT` RWX mmap + per-thread `pthread_jit_write_protect_np` toggle + clear_cache, every other platform the exact `PROT_NONE→RW→RX` dance (helpers return int and carry src_len/map_len separately so the assemble path's copy-r1.len-seal-len semantics stay byte-identical — the plan's void-return sketch would have swallowed the mprotect(RX) failure check, deviating from T7's own pure-refactor contract); both callers (`asmtest_exec_alloc`, `asmtest_asm_exec_native`) refactored onto them; `asmtest_asm_exec_native` now returns `ASMTEST_DR_ENOSYS` on non-x86-64 hosts (arch gate at the top of the Keystone-present branch). Proof: `make docker-drtrace` GREEN post-refactor (pure refactor on Linux), `cc -arch arm64` cross-compile exits 0 with `nm` showing `_pthread_jit_write_protect_np` referenced in the arm64 object and absent from the x86-64 one (the seam selects correctly), live `drtrace-test-macos` re-run 13/13+18/18 through the refactored helpers. **T8 done 2026-07-22 (gated: arm64 acceptance — needs Apple Silicon hardware AND an arm64/universal DR runtime, upstream i#5383)**: `drtrace.entitlements` (allow-jit only) + the Darwin-only `codesign -f --entitlements … -s - $@` stanza on the `$(BUILD)/test_drtrace` link rule (and only that rule — entitlements bind to the main executable; never `--options runtime`, whose one-MAP_JIT-region limit the harness's three allocations would trip; `-f` added beyond the doc's sketch because arm64 ld ad-hoc-signs at link time, so a plain `-s -` would refuse on exactly the leg T8 exists for — no-op on Intel where linked binaries come out unsigned); verified on this host: `codesign -d --entitlements :- build/test_drtrace` prints the allow-jit key and the signed harness still runs 18/18; MAP_JIT exercise extent recorded honestly in the plan's M1b RESULT block (compile+link only — no MAP_JIT allocation has ever executed); native-tracing.md gains the "macOS arm64 (limitation, not a TODO)" section (compiled-function/symbol = supported path, generated code = standalone-executable-only, hardened interpreters excluded by the OS model); CHANGELOG extended; `make docker-docs` clean. **T9 done & live-verified 2026-07-22**: `drtrace_env` + the `drtrace-python-test` env block resolve `shlib_dev` (`.dylib` on Darwin), `$(DR_CLIENT)`, and the new `DR_LOADER_PATH` var (`DYLD_LIBRARY_PATH` on Darwin / `LD_LIBRARY_PATH` else); no wrapper source needed fixing (zig/python already carry Darwin-correct fallbacks; every wrapper honors the env-var contract). Live on this host against the fork-built home: `drtrace-cpp-test` PASS, `drtrace-ruby-test` PASS, `drtrace-python-test` 3+3 passed / 1 skipped. **The skip is a NEW measured finding beyond the doc: DR signal chaining HANGS on the macOS fork build** — `test_drgate.py::test_signal_chaining` self-raises SIGUSR1 under attach and CPython's handler never fires (exactly T5 watch-item (5), which no C harness exercises); landed as a Darwin-only named skipif (Linux keeps running it), recorded in the plan STATUS block + guide + CHANGELOG as fork-side future work (i#58-adjacent, sibling of the FB2 fixes). Done-when greps hold (`libasmtest_dr*.so` in native-trace.mk: 2 hits, both comments); `make docker-drtrace-bindings` unchanged-green (the same macro expands `.so` + `LD_LIBRARY_PATH` in the Linux containers). **T11 still wip.** **T10 done & verified 2026-07-22**: every DR-tier link rule now honors `$(DRTRACE_LDFLAGS)` (harness/exec links) or the new `$(DRTRACE_SOLDFLAGS)` (the `libasmtest_drapp` shared-lib link — same libs, no `-rdynamic` in a dylib link): `test_drtrace(+_macos)`, `dr_valtrace` ×2, `dr_taint` ×2, `dr_taint_simd` ×2, `taint_attach_coop` (keeps `-lrt`); the pin-oracle lanes (`pin_taint`/`taint_oracle_diff`), `dr_valtrace_bench`, and every hwtrace-tier `-ldl` site left as-is per the doc; all 33 `DynamoRIO-Linux-<ver>` skip-message sites are now `DynamoRIO-$(UNAME_S)-<ver>` (verified live: Darwin prints `DynamoRIO-Darwin-<ver>`, the asmtest-ci container prints `DynamoRIO-Linux-<ver>`; no download URLs touched — none live in native-trace.mk); `make docker-drtrace` re-run GREEN with the changed link lines (all DR sub-lanes re-linked), `drtrace-test-macos` still 13/13; the docker-lane `.so` scoping comment landed with T4's `DRCLIENT_EXT` conditional. **T3/T4/T5 done & live-verified 2026-07-22 via the fork-build doc's FB3 — M0 = GO**: `make drtrace-test-macos DYNAMORIO_HOME=build/dynamorio-macos` 13/13 twice in a row on the macOS-14.7.5/Intel host (attach, Mach-O `dr_get_proc_address` marker resolution — T5's load-bearing unknown, RESOLVES — coverage, symbol mode, clean detach); see [macos-dynamorio-fork-build.md](macos-dynamorio-fork-build.md) for the three fork fixes + doc corrections; remaining: T6 (Rosetta, Apple-Silicon-gated), T7/T8 (arm64, Apple-Silicon + upstream i#5383 gated), T9 (bindings), T10 (-ldl cleanup), T11 (revised CI job = fork-build FB4). Earlier text follows.** T1 done — re-ran the standing upstream go/no-go 2026-07-20: **all 455 releases scanned, 0 macOS/Mac/Darwin/dylib assets, ever** (`grep -icE 'mac\|darwin\|osx\|dylib'`→0; latest `cronbuild-11.91.20651`); published set is Windows/Linux/Android only. Dated result + recheck cadence appended to [macos-drtrace-plan.md](../plans/macos-drtrace-plan.md) STATUS. **~~T2–T11 BLOCKED UPSTREAM~~ — UPSTREAM-RELEASE GATE RETIRED 2026-07-22:** a source fork (`wilvk/dynamorio`) builds `libdynamorio.dylib` on macOS x86-64 (verified live — see [macos-dynamorio-fork-build.md](macos-dynamorio-fork-build.md)), so there is now something to attach *with*. T1's release recheck is no longer the blocker; **T2 (inspect release tarball) is superseded** by the fork's confirmed `lib64/release/libdynamorio.dylib` layout, and T3/T4/T5 now proceed once the fork doc's FB2 (fix the `dr_app_setup` startup crash) is green. T6/T8 keep their Apple-Silicon gates.) | **macos-dynamorio-fork-build** (FB1–FB3 feed T3/T4/T5) |
| [macos-dynamorio-fork-build.md](macos-dynamorio-fork-build.md) — build `libdynamorio.dylib` from a pinned source fork; fix the dr_app_setup startup crash; hand off to the port doc | 4 | ✅ 4/4 (**✅ 2026-07-22 (validating agent): the FB4 lane re-ran GREEN twice on fresh `macos-15-intel` runners, independent of the implementer — dispatch [run 29905598418](https://github.com/wilvk/asm-test/actions/runs/29905598418) (M0 13/13 + generated-bytes 18/18) and dispatch [run 29906179941](https://github.com/wilvk/asm-test/actions/runs/29906179941) (M0 15/15 — the harness grew 13→15 with the signal-chaining doc's in-tree guards landing between the runs — T8 `codesign -f` step executed, generated-bytes 18/18), both `0 failed`, no self-skip, the pinned fork built from source each time. Every leg of this doc has now run live under a validating agent's dispatch. Earlier text follows.** **FB4 landed 2026-07-22**: nightly/dispatch-only `drtrace-macos` ci.yml job on `macos-15-intel` — brew nasm+lz4, `build-dynamorio-macos.sh "$HOME/dr"` (pin in script+manifest, commit-stamp build cache keyed on script+digests), `make drtrace-test-macos` with a self-skip-is-failure guard, outside the `test` matrix; actionlint 0 findings, YAML parses (47 jobs); **dispatch validation GREEN** — [run 29899563218](https://github.com/wilvk/asm-test/actions/runs/29899563218): the pinned fork built from source on `macos-15-intel` and the M0 harness ran **13/13, 0 failed** (no self-skip) — a second, independent macOS host (macOS 15/Xcode 16) reproducing the dev-host result; first dispatch failed on the runner-default newer clang (`OPTION_TYPE__Bool` token-pasting) → fixed by pinning `DEVELOPER_DIR=Xcode_16.app` exactly as upstream's ci-osx.yml does. Every leg of this doc has now run live; ✅ is a validating agent's call per the process rule. **FB2+FB3 done & live-verified 2026-07-22 on this macOS-14.7.5/Intel host — M0 = GO.** FB2: the `dr_app_setup` crash root-caused & fixed in the fork (`KERN_PROCARGS2` exec-path read; standalone baseline 10/10 SIGSEGV → 10/10 clean), **plus two more fork fixes the crash masked**: `our_environ` NULL in the dlopen embedding (dyld passes no envp to dylib initializers — the fault addr 0x8 was `NULL_env++` deref'd; `DYNAMORIO_OPTIONS` was invisible so NO CLIENT ever loaded; fixed via live `_NSGetEnviron` capture, the STATIC_LIBRARY approach) and `dr_get_proc_address` NULL on every modern binary (`LC_DYLD_EXPORTS_TRIE` unparsed + trie rebase used the ASLR slide, correct only for preferred-base-0 dylibs, never the main executable — now rebased by the mapped mach_header). Fork branch `asmtest/macos-fixes`, pin advanced to `bbbcc40b8`. **Doc corrections (measured, recorded in the doc):** the literal `api.startstop`-passes acceptance is unachievable in ANY tree — multi-thread takeover is upstream-NYI on macOS (i#58: `os_list_threads` returns 0, `thread_signal` NOT_IMPLEMENTED) and premise #4 was wrong: upstream macOS CI runs ONLY `OSX`-labeled tests (i#1815) and never runs api.startstop/api.detach, so the ignore-list absence is vacuous; re-scoped acceptance passed — both tests 10/10 exit-0 with output byte-identical to .expect after filtering exactly the two i#58 line classes. FB3: port-doc T3 (`DR_LIBNAME`) + T4 (`DRCLIENT_EXT`/`DRTRACE_LDFLAGS`/`drtrace-test-macos`; harness links `DRAPP_KEYSTONE=0`) + T5 (`examples/test_drtrace_macos.c`) landed — `make drtrace-test-macos` **13/13 twice in a row** against the pinned script-built home: attach, Mach-O marker resolution (the T5 load-bearing unknown RESOLVES), coverage accumulation, symbol mode, clean detach; + one asm-test macOS compile fix (taint-client nudge handler `uint64_t`→DR `uint64`, hard error on LP64 macOS, byte-identical on Linux); all five drclient dylibs build on macOS; M0=GO recorded in the plan + native-tracing guide macOS section + CHANGELOG. Earlier FB1 text follows.** **FB1 done & verified 2026-07-22 on this macOS-14.7.5/Intel host**: `dynamorio-fork` git-commit pin `cca42665b` in `third-party-digests.txt` + refresh emitter + vendored `licenses/DynamoRIO-fork.txt` (BSD-3) + README row; `scripts/build-dynamorio-macos.sh` (fetch-by-SHA w/ clone fallback, submodules, integrity assert, idempotent commit-stamp reuse) builds `lib64/release/libdynamorio.dylib` (lipo: x86_64) **plus `drmgr`/`drreg`/`drx`** — the drclient sub-build's extension set — via new `make dynamorio-macos` (+ `make help` line); Linux gate verified in ubuntu:24.04 (skip + exit 0); **doc correction (recorded in the doc):** copy-staging is unsound (build-tree cmake config carries 128 absolute `IMPORTED_LOCATION` paths; `cmake --install` fails on not-built targets), so the caller-named prefix IS the CMake build tree — upstream-supported as `DynamoRIO_DIR`, and it already carries the exact home shape. Earlier text follows.** freshly authored spec, 2026-07-22 — **feasibility verified live** on this macOS-14.7.5 / Intel host: fork HEAD `cca42665b` builds `lib64/release/libdynamorio.dylib` (CMake 4.3.4, Apple clang 16, submodules init'd), the tree carries real Mach-O support (`loader_macos.c`/`module_macho.c`/`signal_macos.c`/`native_macho.c`), and upstream CI-gates `api.startstop`/`api.detach` on `macos-15-intel` (**not** in the macOS ignore-failures list, unlike Windows). **FB2 is the M0 go/no-go:** `api.startstop` faults at `dr_app_setup` startup — `get_application_name_helper`, `core/unix/os.c:1192`, an exec-path-above-`envp` deref that is stack-layout-dependent (saw one clean 10-iter attach/detach run, then consistent crashes). FB1 pins the fork + adds `scripts/build-dynamorio-macos.sh`; FB2 fixes the crash in the fork; FB3 drives port-doc T3/T4/T5; FB4 adds the nightly `drtrace-macos` CI lane that builds the pinned fork on `macos-15-intel`. Intel-first; arm64/Rosetta stay Apple-Silicon-gated.) | — (produces the runtime the port doc consumes) |
| [macos-dynamorio-signal-chaining.md](macos-dynamorio-signal-chaining.md) — fix DR signal chaining under attach on macOS (the drgate hang) — a fourth fork fix | 4 | ✅ 4/4 (**✅ verified 2026-07-22 (validating agent, Intel-mac host): the second-host drgate gap is CLOSED.** The `drtrace-macos` CI lane now runs the drgate pytest gate as a standing step (cdd86fe: pytest install + `make drtrace-python-test` against the freshly built pinned fork, self-skip-is-failure — a DR-not-found skip, pytest skip, or short collection fails the job; actionlint-clean), and dispatched [run 29913042151](https://github.com/wilvk/asm-test/actions/runs/29913042151) ran it **GREEN on a fresh `macos-15-intel` runner** at pin b8785a5d8: C harnesses `# 15 passed, 0 failed` + `# 18 passed, 0 failed`, then `test_drtrace.py` 3 passed and `test_drgate.py` **4 passed incl. `test_signal_chaining PASSED`** — signal chaining under attach for a real CPython host verified on a second host (not the dev box that landed the fix), and nightly-regression-protected from here on. Pre-validated natively on the Intel-mac host the same day (`make drtrace-python-test` 3+4 passed, 0 skipped). **RESOLVED & validated on the Intel-mac dev host 2026-07-22 — signal chaining now works on macOS.** SC3 FIX landed: root cause CONFIRMED single-threaded (NOT the multi-thread i#58 first suspected) — the app handler delivers fine, but its return through libsystem `_sigtramp` → macOS 3-arg `sigreturn(uctx,infostyle,token)` needs a per-delivery kernel token DR can't forge for its SYNTHESIZED frames, so the real `sigreturn` was rejected and the thread resumed into DR gencode→`ud2`→SIGILL→terminate. Fix (fork branch `asmtest/macos-fixes`, one commit, pin `bbbcc40b8`→**`b8785a5d8`**): macOS-x86-64 `handle_sigreturn` restores the app context via `sigcontext_to_mcontext(DR_MC_ALL)` + returns `false` to skip the real `sigreturn`, exactly as the VMX86 path does (`asynch_target` is the redirect); scoped `#if MACOS && X86`, Linux/AArch64/VMX86 untouched. **Validated:** deterministic single-thread C reproducer (`sigchain_cpythonish.c`, was SIGILL 6/6) → exit 0 + handler ran 5/5; `test_drgate.py::test_signal_chaining` (was wedge) PASSES + full drgate 4/4; plain-python repro 5/5; **pin bump reproduces from a fresh fetch of `b8785a5d8`** (`make dynamorio-macos` re-fetch+rebuild) → harness 15/18 + red gate 3/3; cpp/ruby macOS lanes PASS; Linux `docker-drtrace` 4/4 green (fix is macOS-only by `#ifdef` + the fork is macOS-only). SC4: pin bumped in `third-party-digests.txt` + `build-dynamorio-macos.sh`, the Darwin `skipif` REMOVED from `test_drgate.py`, guide/plan/CHANGELOG flipped to resolved. **CI evidence: dispatched [run 29911846063](https://github.com/wilvk/asm-test/actions/runs/29911846063) — the `drtrace-macos` job rebuilt the fork from source at the new pin (`verified dynamorio fork b8785a5d8…`) on a fresh `macos-15-intel` runner and the harness ran `# 15 passed, 0 failed` + `# 18 passed, 0 failed`, no self-skip — a second independent macOS host confirms the pin builds + harness green.** (Note: the CI lane runs the C harness; the signal-chaining *behavior* gate — `test_drgate.py::test_signal_chaining` + the C reproducer — was validated locally 5/5 on this host, and a full ✅ awaits a validating agent running that drgate gate on a second host.) Earlier SC1–SC3 trail follows. **SC3 wip 2026-07-22 on the Intel-mac dev host.** Freshly authored spec; probe results measured live while authoring: python SIGUSR1-under-attach = deterministic **SIGILL on a DR-planted `ud2`** adjacent to a ret-IBL exit stub in the code cache (the pytest form wedges instead); **four minimal C repros all PASS** (at-syscall self-kill, async-mid-direct-branch, async-mid-indirect-call/ret, syscall-inside-handler + multi-registration) so the trigger is CPython-specific — ranked candidates: startup `sigaltstack`, delivery landing inside DR gencode/IBL, trace volume; fork anchors: `signal_macos.c` is 388 lines with `save_fpstate` = `ASSERT_NOT_IMPLEMENTED` (i#58, a silent release no-op on the delivery path). SC1 = in-tree C guards + repro knob; SC2 = debug fork build names the invariant; SC3 = fork fix; SC4 = pin bump + un-skip + CI dispatch at the new pin) **SC1 done & verified 2026-07-22**: two always-on signal-chaining guards in `test_drtrace_macos.c` (at-syscall SIGUSR1 self-kill; async SIGALRM landing mid-indirect-call/ret spin — CPython's plain-handler shape) — extended harness **15/15 + 18/18 twice**; the `ASMTEST_SIGCHAIN_DARWIN=1` knob force-runs the red pytest case (verified: reproduces the wedge on demand); Linux structurally untouched (harness is Darwin-only, skipif condition false off-Darwin as before, `py_compile` clean). **SC2 done 2026-07-22 — the invariant is NAMED**: `DR_MACOS_BUILD_TYPE=Debug` opt-in in `build-dynamorio-macos.sh` (separate prefix, `lib64/debug`, release flow byte-identical — stamp-reuse verified); the debug run re-attributes the crash: the `ud2` is **libsystem `_sigtramp`'s own trap after its `sigreturn` call** — the app's sigreturn for a DR-delivered frame is kernel-REJECTED and returns ("Signal 4 delivered to application as default action" at `_sigtramp+51`, atos-confirmed); `_sigtramp` passes THREE args incl. a kernel validation token that DR's `handle_sigreturn(dcontext, uctx, infostyle)` never reads (stale "2 params" NYI comment at signal.c:3292 predates the token); heap.c:1995 assert = secondary death-path noise. **SC3 diagnosed & RE-SCOPED 2026-07-22 (fork fix NOT landed — honest checkpoint):** the symbolicated debug callstack re-locates the true cause past SC2's sigreturn reading — DR takes the signal's **default action (terminate)** at delivery (`signal.c:4285`) because `info->sighand->action[SIGUSR1]` isn't recorded in time; the client returns DELIVER (C cases prove it), so it's the handler-table timing. **The bug is timing-sensitive**: under lldb `handle_sigaction(sig=30)` fires and the default path doesn't, yet it still crashes — and the pure-C race reproducer (bg-thread fragment churn + register/self-kill loop) **hangs at `pthread_create` on DR's macOS multi-thread-takeover NYI (i#58)**. So single-threaded C can't reproduce it and multi-threaded C hits the thread NYI first: SC3 is very likely **i#58-class macOS multi-thread signal delivery, a DR-core effort, not a surgical fourth fix**. Full evidence + eliminations (altstack/SA_ONSTACK, syscall-in-handler, async in-cache all PASS) in the doc's SC3 diagnosis. **SC3 diagnosis v3 2026-07-22 — CONFIRMED via full runtime instrumentation of a debug fork build; supersedes v1's multi-thread scoping AND the investigation workflow's exit-stub guess (fork fix NOT landed, honest checkpoint):** CPython is **single-threaded** at signal time (measured: `active_count==1`, `ps -M`=1) — multi-thread/i#58 REFUTED, that was a separate `pthread_create` NYI a race repro tripped. Deterministic single-threaded pure-C red gate (`sigchain_cpythonish.c`, no hang). Instrumented debug trace proves: the self-kill lands in DR `do_syscall` gencode (`get_at_syscall==1`, `asynch_target`=app PC), the handler **is delivered correctly** (`execute_handler_from_dispatch`, `next_tag==asynch_target==frame_xip`), and the crash is the **resume after the handler's `sigreturn`**: the app returns through libsystem `_sigtramp`→macOS **3-arg `sigreturn(uctx,0x1e,token)`**; DR lets the real `sigreturn` run against a *synthesized* frame it has no valid kernel token for (lldb at fault: DR gencode `ud2`, `rcx`=interrupted gencode PC, `r12`=bogus token), execution lands in gencode, macOS SIGILL→nudge path passes it to the app→default action→terminate. **Root cause: macOS `sigreturn` requires a kernel-minted per-delivery token DR cannot forge for its synthetic app frames** (Linux `sigreturn` has no token, so synthetic frames work — why all single-threaded C repros pass). A fix attempt (translate `SC_XIP`→`asynch_target` in `record_pending_signal`) *removed the crash* but caused a native escape that skipped delivery (`got=0`) — proving delivery is fine and the defect is the return/resume mechanism; reverted. **Real fix = a bounded-but-substantial macOS DR feature** (option a: capture+propagate the kernel token into the synthetic frame; option b: stop issuing the real `sigreturn` and drive the return via a direct DR context switch — implement the macOS `thread_set_self_context` arm, the `signal.c:3292` NYI). Target now precisely identified; row holds 2/4; SC3/SC4 open pending the sigreturn-context feature. | — (consumes the fork pin; SC4 bumps it) |

### Architecture ports
| Document | Tasks | Status | Depends on |
|---|---|---|---|
| [aarch64-sve-capture.md](aarch64-sve-capture.md) — AArch64 SVE wide-vector capture | 8 | ✅ 8/8 (**T8 real-silicon sign-off DONE 2026-07-22 — and the "hardware gate" was self-inflicted**: T8 was recorded as blocked on Graviton/Grace/A64FX-class silicon because "hosts are x86-64 Zen 5 and non-SVE arm64" — but the hosted **`ubuntu-24.04-arm`** runner is Azure Cobalt 100 / **Neoverse-N2**, whose HWCAP carries `sve sve2 sveaes svebitperm svesha3 svesm4 svei8mm svebf16` at `sve_default_vector_length` = **16 B**, on `Linux 6.17.0-1020-azure`. The trampoline had therefore been *executing* on real silicon in every CI run since it landed; the evidence sat unread in the `test (ubuntu-24.04-arm)` log. Measured live: `make WERROR=1 test` → `ok 5 - simd.sve_adds_doubles_at_any_vl`, **0 skipped**; `make WERROR=1 check` → **57 passed, 0 failed**. Converted from a read log into a STANDING gate: the `test` job's arm64 leg now prints kernel/`CPU part`/VL each run and asserts `^ok [0-9]* - simd\.sve_adds_doubles_at_any_vl$` — the line *without* a `# SKIP` suffix — so a runner-fleet change or a regressed `HWCAP_SVE` probe can't silently retire the only real-silicon validation this path has (`ASM_SVCALL_*` skipping is correct off-SVE, which is exactly what makes a silent skip dangerous here). Stale claims corrected in `examples/test_simd.c` ("only qemu-user under TCG exposes SVE here"), `mk/docker.mk` ("pre-hardware"), the SIMD guide, plan Track D and the analysis gate line. **Narrower gate remains, stated honestly:** a *native* VL other than 16 B (Graviton3 32 B / A64FX 64 B) — the sweep's 48/128/256 B legs are qemu-TCG. T1–T7 landed & validated live under qemu-user TCG on this x86-64 host: `svec_t`/`spred_t` + HWCAP_SVE probe (T1), `asm_call_capture_sve` AAPCS64 trampoline + GAS/NASM cross-arch stubs (T2, `nm` shows both symbols on both arches), `ASM_SVCALL_*` self-skip macros + VL-aware `ASSERT_SVEC/SPRED_EQ` (T3), `sve_addd` corpus + VL-agnostic test (T4), manifest pins `svec_t`=256/`spred_t`=32 native+win64 (T5), `docker-sve-sweep` lane (T6), user docs + CHANGELOG (T7). `make docker-test DOCKER_PLATFORM=linux/arm64` runs `simd.sve_adds_doubles_at_any_vl` as a real SVE `ptrue`/`fadd` execution (not a skip) at the default **VL=64 B (512-bit)**, and `make docker-sve-sweep` passes it at **VL 16/48/128/256 B** (VQ 1/3/8/16, incl. the non-power-of-two 48 B) by steering `QEMU_CPU=…,sve-max-vq=N,sve-default-vector-length=-1`; `make test`/`make check`/`make ASM_SYNTAX=nasm test`/`make docker-docs`/`fmt-check` all green. Built the arm64 image with `DEPS_ARGS=--pkgconfig` (the SIMD suite links no optional engine; mirrors the `docker-riscv64` light-TCG-image precedent). **T8 gated: real AArch64+SVE silicon for execution sign-off** (Graviton3/3E/4, NVIDIA Grace, or A64FX-class) — no such host in this environment and qemu TCG is not accepted as the silicon sign-off; a validating agent runs `make test && make check` + `make docker-sve-sweep` on SVE hardware to reach ✅. Source plan Track D + analysis gate line updated to match.) | — |
| [riscv-native-tier.md](riscv-native-tier.md) — native RISC-V (rv64) host tier | 7 | ✅ 7/7 (all landed; rv64 lane green: `make docker-riscv64` under qemu + the `test-riscv64` CI Actions leg, ~3m29s; `make docker-docs`/`make test`/`make check` clean. **✅ 2026-07-22 (validating agent): the `test (riscv64 container, qemu binfmt)` job ran GREEN in dispatch [run 29906179941](https://github.com/wilvk/asm-test/actions/runs/29906179941) — an independent live run of the lane's validation vehicle (`make test && make check` in the rv64 container under qemu binfmt). Local-host note: `make docker-riscv64` cannot run on the Zen 2 dev box's snap Docker (no buildx, and the classic builder resolves the locally-cached amd64 `ubuntu:24.04` tag despite `--platform linux/riscv64` — it visibly installs x86-64 toolchain packages before erroring at the first COPY); a host-Docker limitation, not a tree defect — the CI leg is the canonical vehicle there.**) | — |

### CI, distribution & infrastructure
| Document | Tasks | Status | Depends on |
|---|---|---|---|
| [distribution-packaging.md](distribution-packaging.md) — language-registry go-live + system package-manager coverage | 13 | ◐ 7/13 live + T1-T6 credential-gated (**T5's CI-gated leg CLOSED 2026-07-22 — the release pipeline is proven end to end at the current tree.** T5 had landed with only its x86_64 half locally verified; its aarch64 leg and "the clean-room tier asserts across the runner matrix" were recorded as needing a dispatch, and none had happened since 2026-07-16 — i.e. **none of T5's manylinux container legs, T6's maven job or T4's OIDC restructure had ever run in CI**. Dispatched on `main`: [run 29921538660](https://github.com/wilvk/asm-test/actions/runs/29921538660) **all 30 jobs green**, `publish to PyPI (OIDC trusted publishing)` correctly `skipped` (tag-gated, so a `main` dispatch builds and verifies without touching a registry — the dispatch-safe property T4 claimed, now observed). Both Linux wheels carry the manylinux_2_28 floor — `asmtest-1.1.0-py3-none-manylinux_2_24_x86_64.manylinux_2_28_x86_64.whl` and `…manylinux_2_24_aarch64.manylinux_2_28_aarch64.whl` — and **T5's Done-when was then closed against the artifacts themselves, not the build log**: each wheel `pip install`ed into a clean `almalinux:8` container (glibc **2.28**, `python3.12`; aarch64 under the `linux/arm64` qemu lane) and exercised live — Keystone `assemble` + Capstone `disas` per-arch (x86-64 `mov rax, 42`→`48c7c02a000000`; **arm64 `mov x0, #42`→`400580d2`, decoded back as `mov x0, #0x2a`**, so the aarch64 payload engines are real, not merely present), the vendored Unicorn tier actually **executing** on x86-64 (`Emulator.call` → `rax == 42`), and the `auditwheel --exclude` sibling libs resolving through `dladdr`: `libasmtest_hwtrace.so` on both, `libasmtest_drapp.so` present on x86-64 and absent-with-the-documented-reason on aarch64 (matching `release.yml`'s own arm64 branch). macOS legs unchanged (`macosx_15_0_x86_64` + `macosx_26_0_arm64` wheels built, both python macOS jobs green). `actionlint` re-run clean over all workflows (only the three expected custom self-hosted-label notes in `hw.yml`). Gate unchanged: T1-T6's *publish* halves stay credential-gated. system-package stream **T7-T13 COMPLETE & verified**: `make package-source` + 5 specs under `packaging/` [Homebrew/Debian/AUR/vcpkg/Conan], aggregate `make docker-syspkg` GREEN, additive `syspkg` CI job, user docs + submission runbook, `make docker-docs` clean. Registry go-live scaffolding landed where implementable: **T1** all 5 names verified free (404, no collision → manifests unchanged); **T4** OIDC trusted-publishing in `release.yml` — dedicated PyPI `pypi-publish` job (`pypa/gh-action-pypi-publish`, collects every leg's wheel), crates.io `rust-lang/crates-io-auth-action`, npm `--provenance`, job-scoped `id-token: write` (YAML-validated, tag-gated so dispatch-safe); **T5** manylinux_2_28 container wheel build LANDED & **locally verified GREEN on this host** (was: floor recorded only). New `scripts/build-manylinux-wheel.sh` + `Dockerfile.manylinux-wheel` + `make docker-python-manylinux`: inside `quay.io/pypa/manylinux_2_28_x86_64` (AlmaLinux 8, glibc 2.28) it source-builds the four engines dnf lacks (unicorn 2.1.4 / keystone 0.9.2 / capstone 5.0.1 / libipt 2.0.6, pinned — pip-pinned cmake 3.27.9 since the image's CMake 4.x rejects keystone's old policies; lib64→lib consolidation), fetches DynamoRIO, runs `make python-package`, and `auditwheel repair --plat manylinux_2_28` (release.yml's exact `--exclude` list) → `asmtest-1.1.0-…-manylinux_2_24_x86_64.manylinux_2_28_x86_64.whl` (12 MB), which **installs + imports (asm+disas) on a clean `almalinux:8`, exit 0**. Surfaced + fixed a real glibc-2.28 gap: `src/ptrace_backend.c` used `TRAP_HWBKPT` (glibc ≥2.30) → `#ifndef` guard to the kernel-UAPI value 4. `release.yml` python job restructured (the two Linux legs build in the container; macOS unchanged — actionlint-clean); `libopencsd` deliberately skipped (link-only dead dep — `cs_backend.c` calls no OpenCSD symbol, CoreSight decode board-gated). `docker-fmt-check` + `docker-docs` clean. **Still CI-gated:** confirming the aarch64 leg (emulated) + the full clean-room tier asserts across the runner matrix needs a release dispatch.; **T6** java binding now builds via a REAL `mvn package` — `make java-package` consumes `bindings/java/pom.xml` (was raw `javac`+`jar cf`), emitting jar + `-sources` + `-javadoc`; **verified GREEN on this host** by the new `make docker-java-package` lane (`asmtest-1.1.0.jar` 10.2 MB carries `Asmtest.class` + `libasmtest_emu`, sources/javadoc emitted, exit 0), Maven a version-pinned Apache 3.9.9 tarball (official SHA-512, anchored in `third-party-digests.txt`) in the java image; new tag-gated + secret-guarded `maven` job in `release.yml` (`mvn deploy` → Central staging via the pom's central-publishing plugin, actionlint-clean, dispatch-safe no-op without secrets); `releasing.md` (secrets table + Maven/LuaRocks runbooks) / `packaging.md` / `CHANGELOG` updated, `docker-docs` clean. **Live Central publish + the manual LuaRocks upload stay credential-gated** (namespace `io.github.wilvk` registration + `MAVEN_CENTRAL_TOKEN`/`MAVEN_GPG_KEY` + a LuaRocks API key — a separate credentialed agent's job). **T2/T3** no-code. GATE: PyPI/npm/crates.io/RubyGems/NuGet/Maven-Central/LuaRocks accounts+tokens, registry trusted-publisher registration, the `v1.1.0` tag push, and a release dispatch — all credentials this host lacks; do not publish. Validation of the gated legs is a separate credentialed agent's job.) | — |
| [benchmarks-ci-followups.md](benchmarks-ci-followups.md) — windows/macOS-Intel legs, nightly auto-commit, BM_MODEL_COST | 6 | ✅ 6/6 (**LIVE-VALIDATED 2026-07-22 (validating agent) via two `workflow_dispatch` runs — the T1/T3/T4 CI-gated runtime legs all ran green, after the first dispatch surfaced a REAL T1 defect:** [run 29905598418](https://github.com/wilvk/asm-test/actions/runs/29905598418) failed `benchmarks (macos-15-intel, nightly)` at `make bench-check` — dyld aborted `emu-bench` with `Library not loaded: @rpath/libcapstone.5.dylib … no LC_RPATH's found`; the K1 source-built Capstone dylib kept CMake's Darwin-default `@rpath` install name while the tree links with plain pkg-config flags. Fixed in `60ec96c` (`scripts/build-capstone.sh` now bakes `-DCMAKE_INSTALL_NAME_DIR=$PREFIX/lib`, the Homebrew convention; the K1 cache key hashes the script so CI rebuilt instead of restoring the stale dylib — see CHANGELOG Fixed). Re-dispatch [run 29906179941](https://github.com/wilvk/asm-test/actions/runs/29906179941) then went green END-TO-END: `benchmarks (macos-15-intel, nightly)` SUCCESS with `bench-golden-check: OK (7 deterministic count rows match)` AND the `model_cost` rows present (the leg's reason to exist — i7-8700B, real Intel-mac silicon), `benchmarks (windows-latest)` SUCCESS, the compare summary carries the `gh-macos-15-intel` + `gh-windows-latest` rows, and `benchmarks (nightly record commit)` SUCCESS — bot commit `e4898bb` landed EXACTLY the five `gh-*` box dirs (macos-15-intel / macos-latest / ubuntu-24.04-arm / ubuntu-latest / windows-latest) and nothing else, so the anti-launder rule held; the doc's open Constraints question is answered: `GITHUB_TOKEN` CAN push to this `main`. Earlier text follows.** (**T1/T3/T4 landed 2026-07-20** — ci.yml + mk/win64.mk + Makefile/docs/CHANGELOG only, no C: **T1** nightly `benchmarks-macos-x86` (`macos-15-intel`) leg with the K1 pinned-Capstone pair (so it carries the T6 model rows) + job-level `ASMTEST_BOX_ID=gh-${{ matrix.os }}` on the `benchmarks` matrix (the arm live-ptrace record step still overrides it inline to `arm-linux-arm64-gha`); **T3** per-push `benchmarks-windows` (`windows-latest`, MSYS2/MINGW64, +nasm for `suite_win64.exe`) running `make win64-bench-check`/`win64-bench-report`, versions logged via `pacman -Q`; both new legs wired into `benchmarks-compare`'s `needs` (the `if` still keys only off the per-push `benchmarks` matrix, package-libs-collect pattern, so per-push runs still compare with the nightly Intel leg skipped); **T4** schedule/dispatch-gated per-box record + upload on all five legs (box id = each leg's `gh-<label>`) + a `contents:write`-scoped `benchmarks-record` job that downloads `bench-boxes-*`, trims each `gh-*/perf-history.jsonl` to 200 lines, and commits **only** `benchmarks/boxes/gh-**` (`find -name 'gh-*' -print0 \| xargs -0 -r git add` — so the human-curated `arm-linux-arm64-gha`/`amd-linux` boxes and the golden file are NEVER auto-committed, the doc's anti-launder rule) as `github-actions[bot]` with a `[skip ci]` trailer + rebase-retry; new `make win64-bench-record` alias + `make help` line. **Local proofs GREEN:** `rhysd/actionlint` (docker) **exit 0, no findings**; `python3 yaml.safe_load` parses (5 benchmark jobs present); `make docker-docs` **-W clean** (ci.md + guide "In your CI" rewrites); `make -n win64-bench-record` expands, `make help` lists it. **LIVE VALIDATION GATED (GitHub-hosted Intel-mac + Windows runners, and `GITHUB_TOKEN` push to `main` — none reachable/authorized locally):** per the doc's Constraints, T1/T3/T4 runtime behavior validates only via a `workflow_dispatch`/nightly CI run — a validating agent dispatches CI on the branch and confirms the two new legs go green with `bench-golden-check: OK (7 ...)`, the compare summary gains `gh-macos-15-intel`/`gh-windows-latest` rows, and the nightly `benchmarks-record` commit lands the five `gh-*` box dirs; also unverified whether `GITHUB_TOKEN` can push to a protected `main` (recorded in the doc's Constraints). — T2, T5, T6 landed & verified on this Intel-mac host. **T2 done & live-verified (2026-07-20):** the deterministic emu-bench producer now cross-builds to a Windows PE and its golden gate runs on a real PE — `make docker-win64-bench` **GREEN** (`bench-golden-check: OK (7 deterministic count rows match)`), the mingw-cross emu-bench.exe run under Wine reproducing the committed counts (add3 x86_64/win64=4, arm64/riscv64/arm32=3, sum_to_10=32, sum_to_100=302) plus the Capstone model_cost siblings (5/5/4/4/4/43/403). `Dockerfile.win64` cross-builds **pinned** static unicorn 2.1.4 (new digest-gated manifest line + refresh emitter + version-consistency check) and capstone 5.0.1 (git-commit-gated, same immutable commit the Linux tier pins) into `/opt/win64-deps`; `mk/win64.mk` gains `WIN64_UNICORN/CAPSTONE_*` flag knobs + `win64-emu-bench`/`win64-bench-check`/`win64-bench-report` + `docker-win64-bench`, with `suite_win64.exe` refactored into a shared file target reused by `win64-runner-test` (re-verified: `make docker-win64` still 5-passed-2-failed × isolation/-jN/--no-fork/--bench, EXIT 0). `bench-report.sh` gains `TEST_BENCH`/`EMU_BENCH`/`ASMFEATURES` producer-path overrides and an absent-asmfeatures probe row (asmfeatures links the POSIX native-trace objects → not built on Windows; absence recorded as data, not a crash). **Three link fixes found & fixed by real build iterations, not guessed:** capstone's cmake `project()` needed `CMAKE_CXX_COMPILER`/`RC` for the Windows target; the static unicorn uses QEMU's POSIX thread backend → `-lpthread` (winpthread); the PE needed `-static` (libgcc+libwinpthread) to run under Wine with no DLL-not-found. `-D__USE_MINGW_ANSI_STDIO=1` so `%llu`/`%g` print C99-correctly. Regression: `make docker-bench` unchanged (features array + counts intact, EXIT 0), `sh scripts/check-thirdparty-versions.sh` OK (unicorn 2.1.4 pinned). **T3/T4 (real-Windows + Intel-mac CI legs, nightly auto-commit) remain — CI-hardware/credential-gated (no local Windows or GitHub-hosted Intel-mac runner); T1 (macos-15-intel leg) is a ci.yml-only edit, CI-dispatch-gated.** T5: `asmtest_disas_class` {OTHER/MEM/BRANCH/MULDIV} classifier (BRANCH>MULDIV>MEM>OTHER precedence; MULDIV a model-grade mnemonic match; MEM a per-arch detail walk incl. RISC-V behind the Capstone-5 API macro), `make emu-test` 55/55 native (capstone 5.0.9) + no-Capstone `docker-test` all-OTHER degrade both green, my files clang-format-18-clean. T6: `emu-bench` records insn offsets (cap 4096) and emits a `model_cost` sibling per case (weights OTHER1/MEM3/BRANCH2/MULDIV8) — hand-verified add3 x86_64/win64=5, arm64/riscv64/arm32=4, sum_to_10=43, sum_to_100=403; `bench-golden-check` filters `model_cost` from the golden compare + a new anti-vacuity check (fresh has model rows ⇒ every insns row needs a sibling — force-fail rc=1 confirmed, orphan named); `bench-compare` renders a separate `## Model cost (a MODEL, not silicon)` matrix, never mixed with counts/cycles; **also filtered the golden WRITE in bench-report.sh** (beyond the doc's file list) so a future `make bench-record` can't pollute `golden/emu-insns.json` with Capstone-version-dependent rows; CI `benchmarks` job gains the K1 pinned-Capstone build (the `benchmarks-macos-x86` half lands with T1). Verified: `make bench-check` OK(7), `make emu-bench`/`bench-report`/`bench-compare` all green with the hand values, `make docker-bench` **no-Capstone path** emits 0 model rows + all gates pass, `make docker-docs` clean, ci.yml YAML-valid. **Doc correction:** T6's "`docker-bench` carries model rows (the CI image has Capstone)" is inaccurate — `asmtest-ci` has NO Capstone, so `docker-bench` is the no-Capstone path; the real GitHub-Actions `benchmarks` job carries model rows via the added K1 step.) | — |
| [self-hosted-ci-runners.md](self-hosted-ci-runners.md) — self-hosted runner lanes for hardware-gated tiers | 6 | ◐ 5/6 (**T5 ungated half LANDED 2026-07-22; only registration is left.** `hw.yml` gains `hwtrace-pt-baremetal` (`[self-hosted, linux, x64, intel-pt]`) + the dark `hwtrace-coresight-board`, both with the required guard pair (`HW_RUNNER_*` variable + owner actor guard) so they land green with zero runners — re-verified by an all-skipped dispatch ([run 29920565410](https://github.com/wilvk/asm-test/actions/runs/29920565410), ~12 s, all three jobs skipped). **Two of the task's hedges were obsolete:** (a) step 2 says "until `hwtrace-pt-live` lands this is a build-and-run gate, leave a TODO" — [intel-pt-whole-window-substrate#T5](intel-pt-whole-window-substrate.md) shipped it, so the job is self-skip-proof from run one via new `make docker-hwtrace-pt-live` (require-mode `ASMTEST_REQUIRE_PT=1` in the hwtrace image, `--cap-add=PERFMON`, default seccomp). **Verified on this AMD host that it fails for the right reason** — `661 passed, 1 failed`, non-zero exit, `# SKIP hwtrace PT capture (Intel PT): not a GenuineIntel x86-64 host` — the fail-not-skip contract proven end to end minus the silicon. No grep-assert was written on purpose: the PT skip text has a known cosmetic misreport, so the require target is the only honest gate. (b) step 1's shakedown question ("is `intel_pt` visible INSIDE the container?") was **already answered** by the 2026-07-21 bare-metal run (`docker run --cap-add=PERFMON asmtest-hwtrace make hwtrace-pt-live` → 631/0 on the i7-8559U): visible, no `--privileged`, no `perf_event_paranoid` change — now recorded in the runbook and re-printed by the target each run. **Remaining for T5 is registration, not engineering** (runner on that known-good box with `--labels intel-pt`, flip `HW_RUNNER_INTEL_PT=1`, dispatch, approve, power down — the amd-zen flow), plus a STANDING runner for the nightly bullet. **T6 stays deliberately unwritten**: its hard precondition is [macos-cleanroom-lanes](macos-cleanroom-lanes.md) #T2/#T6 green locally first (Apple-Silicon + bare-metal KVM hosts, neither reachable), and wiring first buys a job that can only fail its own shakedown in CI. The ungated critical path landed on this x86-64 host — the doc's "lands with zero hardware" pair. **T1** runbook `docs/internal/ci/runners.md` (new `docs/internal/ci/` tree, Sphinx-excluded): registration flow [pinned runner v2.335.1 + verify-on-box SHA-256, not a fabricated digest], JIT/ephemeral re-registration loop, the 5-lane label scheme, the load-bearing security posture [separate `hw.yml` with no `pull_request`/`pull_request_target`, `environment: hw-runners`, `contents: read`, `vars.HW_RUNNER_*` guard, dedicated non-sudo runner user + the honest docker-group=root-equivalent caveat], the operator status table, and the recorded gates; **the repo-settings application half is credential-gated** [`hw-runners` environment + 5 `HW_RUNNER_*` vars all `0` + fork-PR "all external contributors" approval — needs repo admin, recorded as an operator checklist per the workflow's gated-leg rule]. **T2** `.github/workflows/hw.yml`: `workflow_dispatch` + nightly `0 5 * * *` only [**NO `push`/`pull_request`** — grep-verified], `permissions: contents: read`, static `concurrency: hw-self-hosted` `cancel-in-progress:false`, and the guarded `hwtrace-privileged-zen` job [`if: vars.HW_RUNNER_AMD_ZEN == '1'`, `runs-on: [self-hosted, linux, x64, amd-zen]`, `environment: hw-runners`, `make docker-hwtrace-privileged` + a self-skip-is-failure assert mirroring `gccanon-attach` — greps the `# SKIP AMD LBR/IBS live capture` strings + the `# call_auto escalate:` `LBR window sufficed` regression signal, keyed to the live literals in `examples/test_hwtrace.c`/`test_ibs.c`]; YAML parses (python), **actionlint-clean** [only the expected informational custom-`amd-zen`-label note, exit 0], and by construction lands green with zero runners [every job `if: vars.HW_RUNNER_* == '1'` → skips when the variable is absent/`0`]; the `gh workflow run hw.yml` dispatch on `main` [run 29688531428] completed with the Zen job **skipped** (guard `vars.HW_RUNNER_AMD_ZEN` absent → `if` false; an all-skipped run is a non-failing conclusion) — verified landing green ahead of all hardware. Honored positions: LbrExtV2 live floor = **Zen 4** (not Zen 3); `macos-15-intel`; call_auto non-escalation is a regression, not a known finding; Docker-OSX is `/dev/kvm`-gated. **T3/T5/T6 hardware-gated** [T3 AMD Zen 4/5 box; T5 bare-metal Intel PT box + the dark CoreSight placeholder awaiting [coresight-live-decode#T4](coresight-live-decode.md); T6 Apple-Silicon + `/dev/kvm` boxes, and its hard precondition — [macos-cleanroom-lanes#T2/#T6](macos-cleanroom-lanes.md) green — is unmet], so their guarded-off jobs are deferred to those tasks rather than pre-written here; **T4** [manual-checklist shrink] depends on T3 running live + coordinates with [amd-ibs-backend-honesty#T2](amd-ibs-backend-honesty.md). Validation of the gated legs is a separate credentialed/hardware agent's job. **T3 impl-slice landed 2026-07-22 on this Zen 5 box (Ryzen 9 9950X, `amd_lbr_v2` present):** step 2 local proof `make docker-hwtrace-privileged` GREEN — 666 `ok` / 0 failed, **zero** `# SKIP AMD LBR live capture`, **zero** `# SKIP IBS live capture`, escalate line `# call_auto escalate: rc=0 result=25 used.backend=3 insns=77 truncated=0 (escalated off the LBR window)`, zero `not ok` (the target on Zen silicon separated from the CI problem, per step 2); step 5 the stale hosted-lane comment at `ci.yml` K2 rewritten — `call_auto` non-escalation is FIXED (5d8e0d2) not "open", and the self-hosted counterpart is no longer "future" (it exists in `hw.yml` as `hwtrace-privileged-zen`); `docs/reference/ci.md` "What CI covers" gains the allowed-to-be-absent `hw` workflow paragraph + `macos-13`→`macos-15-intel` fix (`make docker-docs` -W clean), runbook `docs/internal/ci/runners.md` status table records the local proof; CHANGELOG Added. **Then T3 steps 3-4 completed LIVE 2026-07-22 (operator = repo owner `wilvk`, guided):** registered a pinned `v2.335.1` runner `--ephemeral --labels amd-zen` (tarball SHA-256 `4ef2f25…8cf` verified on box == GitHub's published digest), set `HW_RUNNER_AMD_ZEN=1`, `gh workflow run hw.yml` → `hwtrace-privileged-zen` ran **GREEN** on the `amd-zen` runner ([run 29897214772](https://github.com/wilvk/asm-test/actions/runs/29897214772)) with the assert step passing (`AMD LbrExtV2 + live IBS ran (no self-skip); call_auto escalated off the LBR window`, `# 667 passed, 0 failed`) — **T3 Done-when bullet 1 (green CI run on the amd-zen runner, assert passing) MET, counted 3/6**. Runner was one-shot ephemeral → de-registered after the job, `HW_RUNNER_AMD_ZEN` reset to `0` (power-down rule); the **unattended-nightly** Done-when bullet needs a STANDING runner (JIT/ephemeral loop), a deployment choice deferred. Also hardened `hw.yml` per owner request: explicit `github.actor == github.repository_owner` actor guard on the self-hosted job (belt to the no-`push`/no-`pull_request` suspenders; a convention future hw jobs must keep). **T4 done 2026-07-22 (docs-only):** `docs/internal/amd-hardware-validation.md` rewritten from "the one validation step that cannot run in CI" into the runner-uncoverable **residue** — landed AFTER amd-ibs-backend-honesty T1/T2's checklist repair (`b16f087`), reusing its repaired wording (RESOLVED call_auto, `truncated=0`=regression, `ibs_probe` real-open-vs-BLOCKED), not restating it. New three-tier table (hosted bitrot gate `ci.yml` / self-hosted Zen lane `hw.yml` / this-doc residue) + four residue items each with command + gate + owning doc: (1) Zen 2 IBS-without-LBR degradation `make docker-hwtrace-ibs` on the Ryzen 9 4900HS (only a Zen 2 box lacks `amd_lbr_v2`, so the Zen 4/5 runner can't reach it), (2) MSR-direct `make docker-hwtrace-msr` (`--privileged`+host `msr` — kept OFF the CI runner by posture policy), (3) `status` live-EPERM (root-in-container unreachable), (4) Zen 3 BRS = a link to [amd-branchsnap-lbr-docs#T8](amd-branchsnap-lbr-docs.md), not a checklist item; LBR floor phrased Zen 4 throughout; `call_auto` regression now CI-enforced (assert greps `LBR window sufficed`), not a manual eyeball; Recorded-runs gains the 2026-07-22 live-CI-green entry. Done-when met: no instruction the `hwtrace-privileged-zen` lane already performs, each residue item names command+gate(+Zen-3 owning doc), no contradiction of T2's wording. CHANGELOG `### Changed`. **T5** (Intel-PT baremetal + dark CoreSight) and **T6** (macOS tart/KVM) remain hardware-gated on non-AMD boxes. Both local proof and live run recorded in the runbook status table) | **macos-cleanroom-lanes** (soft coordinate: amd-ibs, coresight, substrate) |
| [libfuzzer-afl-shim.md](libfuzzer-afl-shim.md) — libFuzzer/AFL harness shim (demand-gated) | 5 | ✅ 5/5 (T1 emu_cover_hits seam + T2 libFuzzer + T3 AFL++ harnesses landed; all verified in a clang-18/afl++4.09c fuzz image: libFuzzer baseline exit 0/crash artifact; AFL native forkserver (`fuzz-afl`) replay exit 0 + `afl-fuzz -V 15` finds 1 crash; AFL aflpp_driver reuse (`fuzz-afl-driver`) also finds 1 crash (persistent+deferred forkserver). Two doc corrections re-verified against code: (1) clang-18 libFuzzer requires the parallel PC table; (2) this AFL++ ships NO `__sanitizer_cov_*` runtime, so both AFL paths feed `__afl_area_ptr` via a plain-compiled map helper (afl-clang-fast rewrites an in-harness `__afl_area_ptr` ref to an undefined `.2`). T4 landed: `Dockerfile.fuzz` (clang 18 + libclang-rt-18-dev + afl++ 4.09c on asmtest-bindings-base) + `docker-fuzz` (mk/docker.mk) + `fuzz-shim-test` (mk/fuzz.mk) + help lines. **`make docker-fuzz` green end-to-end**: libFuzzer baseline exit 0 (cov 11/ft 13) + crash artifact, AFL native replay exit 0, `afl-fuzz -V 15` finds 1 crash — no self-skip, both engines genuinely steer to the planted crash. T5 landed: user guide `docs/guides/fuzzing-shim.md` (in the guides toctree, cross-linked from the emulator guide's fuzzing section) + CHANGELOG `[Unreleased]` Added bullet; `make docker-docs` builds `-W`-clean. Whole doc exercised end-to-end on this x86-64 host — the shim is genuinely fuzzed, never demand-gated-away. **Independently re-validated 2026-07-20 on this x86-64 Zen 2 host by a validating agent (fresh `asmtest-fuzz` image build):** `make docker-fuzz` **EXIT 0** — libFuzzer baseline `cov: 11 ft: 13` + planted-crash artifact `crash-847be919…`, AFL native single-input replay exit 0, `afl-fuzz -V 15` found 1 crash via the emulator-coverage forkserver, and the `== fuzz-shim-test PASS: both engines steered to the planted crash ==` line printed; **zero self-skips**. The apt-resolved toolchain matches the Dockerfile.fuzz recorded pins exactly (clang `1:18.0-59~exp2` / libclang-rt-18-dev `1:18.1.3-1ubuntu1` / afl++ `4.09c-1ubuntu3`). No hardware/credential gate applies — the doc is fully verified) | — |

### Correctness & attribution
| Document | Tasks | Status | Depends on |
|---|---|---|---|
| [code-review-plausible-triage.md](code-review-plausible-triage.md) — triage & fix the 2026-07-02 review's still-present findings | 8 | ✅ 8/8 (**macOS portability fix 2026-07-20, validating agent:** T2's `tests/glob_parity.c` differential compared `asmtest_glob_match` (pinned to glibc `fnmatch`) against the *host* `fnmatch` on undefined-behavior patterns (unterminated `[`, trailing `\`), which BSD/macOS `fnmatch` resolves differently → `make check` failed 11/44 on macOS. The divergent cases now assert `glob_match`'s glibc-pinned contract directly via a new `check_div`, cross-checking the host `fnmatch` only under `__GLIBC__`; well-defined cases keep the live host differential everywhere. `glob_match` itself was unchanged and already correct (its portable contract is proven by `tests/win64/test_glob.c`). `make check` 57/57 native on the Intel-mac host, `docker-fmt-check` clean. **Independently re-validated ✅ 2026-07-22 (validating agent) on this x86-64 Linux (Zen 5) box:** `make check` **54/0** (T1 run_parallel poll-degrade via the expect.sh runner selftests; T2 `glob_match` == fnmatch parity — unterminated `[`, in-class escapes, `?`/`*`/`[abc]` all `ok`; T3 grow_overflow; T4 fuzz), `make docker-win64` EXIT 0 (T2 `test_glob.exe` green, T6 `win64-veh-scope-test: foreign-thread fault took the OS unhandled-exception path` + `VEH single-step front-end: all checks passed`; the 5-passed/**2-failed** are the runner's own INTENTIONAL negative controls `win64.hang_timed_out`/`win64.crash_contained`, i.e. the harness proving it catches a hang and a crash — the expected result, `win64 runner: … all verified`), `make docker-zig` EXIT 0 (T5 vec256/vec512 8-slot vargs; the `expected 99, found 42` on stderr is the diagnostic of the passing NEGATIVE test `conformance.zig:358` `expectError(TestExpectedEqual, assertRet(&r,99))`, not a failure), `make docker-docs` -W clean (T7).) | — |
| [native-il-bytecode-attribution.md](native-il-bytecode-attribution.md) — native trace-point → IL/bytecode/source-line attribution | 7 | ✅ 7/7 (all lanes green, no gates: `docker-hwtrace` 666/666; `-jit-jitdump`/`-jit-java-jitdump`/`-jit-dotnet-jitdump` debug tables 5/5 each; `docker-hwtrace-dotnet` IlToNativeMap 19 maps/465 entries; new `docker-hwtrace-jit-java-bci` 3/3 [16 real bcis]; `docker-docs` clean. Doc corrections: only V8 emits jitdump DEBUG_INFO [CoreCLR + HotSpot's perf agent emit none — HotSpot's bci route is java-bci]; V8 baseline positions can exceed code_size [reader keeps at-or-above-base]; .NET 8 truncates the in-proc IL-offset arrays [wiring proven, decode is out-of-proc follow-on]; java-bci traces a loop method [asmtbci] since a trivial leaf gives one bci=-1. **Independently re-validated ✅ 2026-07-22 (validating agent) on this Zen 5 box:** all lanes 0 not ok — `docker-hwtrace-jit-java-jitdump` **5/0** (real HotSpot method bytes recovered from the jitdump == jcmd perf-map, recorded==live JIT bytes; perf JVMTI agent emits no DEBUG_INFO — present-or-absent-never-error), `docker-hwtrace-jit-dotnet-jitdump` **5/0** (same for real CoreCLR JIT), `docker-hwtrace-jit-java-bci` **3/0** (real address→bytecode-index points from `CompiledMethodLoad`, strictly-ascending bci map, `asmtest_srcreg` resolves a native address→bci; 16 real bci→line points), and the IL route in `docker-hwtrace-dotnet` **`ok 211-218`** (`MethodILToNativeMap`: 17 methods / 443 entries, start addr resolves to `IlTinyProbe` at nativeOff 0, mid-body nativeOff>0); `make docker-docs` clean. **Same-day cross-box confirmation (validating agent, Ryzen 9 4900HS — Zen 2): all five lanes re-ran green there too, exit 0, 0 not ok — `docker-hwtrace-jit-jitdump` 5/5, `docker-hwtrace-jit-java-jitdump` 5/5, `docker-hwtrace-jit-dotnet-jitdump` 5/5, `docker-hwtrace-jit-java-bci` 3/3, and `hwtrace-dotnet-test` 1..229 with the IlToNativeMap assertions ok (mid-body nativeOff 96).**) | — |
| [assemble-silent-statement-drop.md](assemble-silent-statement-drop.md) — the in-line assembler silently drops statements it cannot parse | 3 | ✅ 3/3 (**Independently re-validated ✅ 2026-07-23 (validating agent) from clean main `b17d384` on this AMD Zen 5 box (Ryzen 9 9950X): all legs green.** `make asm-test` on unmodified main is **16 passed, 0 failed** — the three drop-detectors `ok 4 rejects_dropped_statement` / `ok 5 rejects_att_source_in_intel_syntax` / `ok 7 arm_hash_immediates_are_not_comments` and the anti-false-rejection control `ok 6 guard_admits_valid_separator_edge_cases` all green (the one `# SKIP RISC-V not supported by this Keystone build` is a pre-existing capability skip on an unrelated `asm_run` case, not a T1–T3 self-skip). **Adversarial teeth-check — the guard is load-bearing, not vacuous:** in an isolated `git worktree` with the guard neutralized (`if (0 && stat_count < expected)`) those three cases flip to `not ok` while control `ok 6` stays green — red-then-green reproduced independently, so the tests have teeth. `make docker-test` (`make test && make check`) exit 0, **57/57** framework self-tests (note: `test_asm` sits in `SUITE_EXCLUDES`, so the assembler suite is the separate `asm-test` leg above, *not* part of `make test`). `make docker-bindings` exit 0 — **zero false rejections from the guard**: every conformance suite that assembles through `asmtest_assemble` is green (ruby 37/0, lua 36/0, go ok, python/cpp/rust/zig/node/java/dotnet `PASSED (0 failures)`); the win64 `crash_contained`+`hang_timed_out` 2-of-7 reds are **expected-by-design** (`mk/win64.mk:211-213` asserts they must print `not ok` — the runner *containing* a crash/hang per `tests/win64/suite_win64.c:66`) and unrelated to the assembler. `make docker-docs` (Sphinx `-W`) exit 0. The header all-or-nothing contract, the residue, and the Keystone-version-sensitivity note are all present. Implementer's record follows. **landed 2026-07-23 on the Zen 5 dev box, one commit per the doc's *Task order*.** T1: three drop-detection cases + an anti-false-rejection control, all four **verified red-then-green** — against unmodified `src/assemble.c` `rejects_dropped_statement`, `rejects_att_source_in_intel_syntax` and `arm_hash_immediates_are_not_comments` each fail for the documented reason (the assemble returns `true`) while the control passes both before and after. T2: `count_statements()` + `stmt_rules()` in `src/assemble.c` only, failing the success path when `stat_count < expected` through the existing `set_last_err` shape. **Two corrections to the doc, both measured and recorded in its new Implementation-record tail:** (a) Table 2 overstates its own example — with the same Intel-syntax source ATT/GAS fail *loudly*, not silently; the syntax-independence claim holds instead via the **truncated-operand** shape (`mov rax,`), which drops silently in all five x86 dialects; (b) the doc's "truncate at `#` for every syntax" rule is **wrong for ARM**, where `#` is the immediate prefix — following it would have counted `mov x0, #1; mov x1, #2; ret` as one statement and blinded the guard on both ARM guests, where the defect also reproduces (T1 gained a case for it). T2 step 4 sweep (scratch harness, not committed) split NATIVE (every source in its own dialect — a violation blocks the fix) from CROSS (the same sources in a foreign dialect — a violation is the guard working): **0 false rejections**, 74 clean native assembles of which 31 sat at the guard's tightest bound (`want == stat`), 21 cross-dialect drops caught, and an anti-vacuity pass with **8/8 defect fixtures caught**. Corpus: the 27 literals from ~103 in-tree call sites tagged by call-site dialect, the doc's adversarial separator/comment/literal shapes, and the `examples/*.s` corpus files — the last **preprocessed the way the build preprocesses them** (`cpp -C`), since raw they are C-preprocessor sources Keystone cannot parse; 14 of those landed 9-74 counted statements against Keystone's 36-131. Every step-5 construct needed handling: `;` in a string literal, in a char literal, in `//`, in `/* */` (including across a newline) and after an escaped quote each over-count on a naive splitter. Two findings recorded: Keystone 0.9.2 **drops a whole unsupported-ISA source with `KS_ERR_OK` and zero bytes** (`examples/apx_basic.s`, Intel APX — the defect at its most extreme; the guard catches it, deliberately not test-pinned since a bump may add APX), and it **hangs** (>8s) on a large NASM-syntax source (pre-existing, inside `ks_asm`, neither caused nor fixed here). T3: header states the all-or-nothing contract + the statements-≠-instructions note, troubleshooting page names the syntax-argument symptom, CHANGELOG `Fixed`, residue recorded. Evidence: `make docker-asm` **17 tests, 16 passed 0 failed** (was 13/12), `make check` 57/57, `make test` green, `make docker-bindings` green across all ten languages, `make docker-fmt-check` clean, `make docker-docs` (Sphinx `-W`) clean. **No hardware or credential gate** — nothing here self-skips; ✅ is a validating agent's call) (**added 2026-07-22**, after the original 31 — found while closing [distribution-packaging](distribution-packaging.md) T5 in a clean AlmaLinux 8 container, not extracted from a plan. `asmtest_assemble` can return machine code with a statement missing and `ok == true`; measured in all five x86 syntaxes, and `ks_errno` is `KS_ERR_OK` on the failing path, so `stat_count` is the only witness. Reachable trigger: AT&T source under the default Intel syntax returns a bare `ret`. Core-only fix — one file, one choke point, all ten bindings. **No hardware or credential gate.**) | — |

## Parallelism & the critical path

**25 of the 31 documents are fully independent** — no shared code, no cross-doc
dependency — and can be assigned to different developers and implemented
concurrently. There are only three true ordered chains:

1. **Intel PT** — [intel-pt-whole-window-substrate.md](intel-pt-whole-window-substrate.md)
   must land first (it builds the one perf-AUX `intel_pt` capture helper and the
   `begin_window` PT arm). Then
   [intel-pt-attach-foreign-pid.md](intel-pt-attach-foreign-pid.md) extends that
   arm to foreign PIDs, and
   [dataflow-pt-replay-tier.md](dataflow-pt-replay-tier.md) consumes the
   foreign-PID capture. [managed-wholewindow-compose.md](managed-wholewindow-compose.md)'s
   PT prongs also wait on the substrate, but its managed-stepper prongs are
   Docker-testable in parallel now.
2. **Intel Pin** — [pin-xed-trace-tier.md](pin-xed-trace-tier.md) creates the
   shared `Dockerfile.pintool` + `scripts/fetch-pin.sh` substrate, so
   [pin-probe-mode-capture.md](pin-probe-mode-capture.md) and
   [pin-libdft-taint-oracle.md](pin-libdft-taint-oracle.md) build on it.
   [pin-sde-future-isa-lane.md](pin-sde-future-isa-lane.md) is independent (it
   owns the APX-capable pinned assembler the Pin lanes reuse — a soft coupling,
   not a blocker).
3. **CI** — [self-hosted-ci-runners.md](self-hosted-ci-runners.md) wires CI
   jobs that invoke the clean-room lanes, so
   [macos-cleanroom-lanes.md](macos-cleanroom-lanes.md) should be green first.

**Soft coordination (shared files, not hard dependencies — sequence to avoid
merge conflicts):** the two AMD docs share the Zen 3 story;
`dataflow-producer-correctness` and the F2 work inside it both edit
`src/dataflow_blockstep.c`; the three CI-touching docs
(`macos-dynamorio-port`, `self-hosted-ci-runners`, `benchmarks-ci-followups`)
all edit `.github/workflows/ci.yml` and must uniformly use `macos-15-intel`.
Each doc's *Task order & parallelism* section spells out its own soft couplings.

## Hardware & credential gates

Per [CLAUDE.md](../../../CLAUDE.md), a missing **installable** dependency is
never a blocker — it is added to the relevant `Dockerfile.*` + `docker-*` rule
with a pinned version. Only **hardware** (specific CPU generations, Intel PT,
CoreSight, Apple silicon) and **credentials** are legitimate self-skip gates.
Many docs implement fully in Docker and gate only their *live-validation* leg on
real silicon (e.g. the AMD docs implement and unit-test everywhere but validate
on the Zen 5 dev box; the Intel PT docs need a bare-metal PT host; the registry
publish steps in `distribution-packaging` need credentials). Each doc's
*Constraints & gates* section names its exact gate and what to record when it
blocks validation.
