# asm-test — asmspy (interactive process tracer): roadmap

> **Context (2026-07-12).** `asmspy` is the ncurses + headless out-of-process tracer in
> [cli/](../../../cli/): attach to any running Linux/x86-64 process and watch its syscalls,
> a function's assembly + call-graph, a whole-process instruction stream, an aggregated
> call graph, a live call tree, or the process/thread topology — all out of band via the
> ptrace attach seam. User guide: [docs/guides/tracing/asmspy.md](../../guides/tracing/asmspy.md).
> Sources: [asmspy.c](../../../cli/asmspy.c) (TUI + headless + main),
> [asmspy_engine.c](../../../cli/asmspy_engine.c) (the engines + detach),
> [asmspy_proc.c](../../../cli/asmspy_proc.c) (/proc + ELF/PLT resolver),
> [asmspy.h](../../../cli/asmspy.h) (contract); smoke: [cli/cli_smoke.sh](../../../cli/cli_smoke.sh).
>
> This is the first dedicated asmspy plan (every sibling tier had one; asmspy did not — the
> gap the roadmap audit flagged as **Theme G**). It records what has LANDED and the remaining
> work so the view family stays coherent rather than accreting one-off flags.

> Status legend: **landed** unless marked *(planned)*. Severity/effort on roadmap items are
> the values an adversarial audit (2026-07-12, 55 agents, 47 verified items) confirmed.

## The view family (current — all landed)

> **Complete as of 2026-07-16** — this table is the whole shipped surface: **11
> headless subcommands** (`--list`, `--syms`, `--log`, `--trace`, `--dataflow`,
> `--stream`, `--graph`, `--tree`, `--procs`, `--sample`, `--watch`) and **9 TUI
> modes** (1 Syscall log · 2 Assembly & funcs · 3 Live stream · 4 Call graph ·
> 5 Call tree · 6 Process tree · 7 Hot edges · 8 Process details · 9 Data flow),
> reached from the process picker (`asmspy` with no args). *(Correction: mode 8
> "Process details" and the `--list`/`--syms` helpers shipped but were missing from
> this table, which billed itself as the full family — added 2026-07-16.)*
>
> Cross-cutting TUI keys: `space` freezes a live snapshot; up/down·PgUp/PgDn·Home/End
> scroll; `Tab` toggles a view option or moves pane focus; `F3` refreshes; `Enter`
> drills in; `b`/ESC goes back. Headless: a **negative `n`** runs until the target
> exits or you interrupt (Ctrl-C).

| View | Headless | TUI | Engine | Mechanism | Safe on any target? |
|---|---|---|---|---|---|
| Syscall log (strace-ish, fd→path) | `--log` | mode 1 | `asmspy_engine_syscalls` | `PTRACE_SYSCALL`, all threads | **yes** |
| Function assembly + call-graph edges | `--trace` | mode 2 | `asmspy_engine_region` | `run_to` + `trace_attached_ex` (region single-step) | leader only |
| Whole-process instruction stream | `--stream` | mode 3 | `asmspy_engine_stream` | single-step, all threads | **no** (single-step) |
| Aggregated call graph (inv/calls/fanout, `[int]`/`[EXT]`/`[?]`, sortable) | `--graph` | mode 4 | `asmspy_engine_graph` | single-step, all threads | **no** (single-step) |
| Live call **tree** (indented by depth; TUI two-pane w/ assembly) | `--tree` | mode 5 | `asmspy_engine_tree` | single-step, all threads | **no** (single-step) |
| Process/thread **topology** (procs+threads+children, drill-in) | `--procs` | mode 6 | `asmspy_engine_procs` | `PTRACE_SYSCALL` **or** single-step, whole tree | **yes** in `--count=syscalls` |
| **Statistical hot edges** (IBS-Op `from→to`, named endpoints, misp/ret tags, `--json`) | `--sample` | mode 7 | `asmspy_engine_sample` | **AMD IBS-Op statistical sampling, OUT OF BAND** (no ptrace/step) | **yes** — the only *rich* view safe on any target (incl. a live JIT) |
| **Process details** / fingerprint (runtime badge — JVM/.NET/CPython/Node·V8/Go/…, thread makeup, notable modules, ELF traits) | — | mode 8 | `asmspy_fingerprint` | `/proc` + the mapped ELF only — **no ptrace attach at all** | **yes** — nothing is attached |
| **Data flow** (L0 values + L1 def-use + L2 slice nav) | `--dataflow` | mode 9 | `asmspy_engine_dataflow` | JIT-aware scoped-ptrace L0 value producer (`attach_jit`: SEIZE-all-threads-and-race + signal split), UI-side slice navigation | **no** (single-step); worker-thread + managed targets now reached |
| **Data watchpoint** (who-wrote-this-field + value) | `--watch` | — | `asmspy_engine_watch` | x86 HW data watchpoint (DR0-3, per-thread arm), native speed between hits | **yes** — near-zero perturbation |

Two attach-free helpers complete the surface (no engine — they feed the views):

| Helper | Headless | TUI | Backend | Mechanism | Safe on any target? |
|---|---|---|---|---|---|
| Process **list/picker** (`active` = recent CPU; `scan` = string-rich memory first) | `--list [active\|scan]` | the entry picker screen | `asmspy_proclist` / `asmspy_sort_t` | `/proc` walk (+ a memory probe under `ASMSPY_SORT_SCAN`) | **yes** |
| Resolved **function symbols** (ELF + PLT `name@plt`, C++-demangled; `addr`/`size`/`name`/`module`, optional substring filter) | `--syms <pid> [filter]` | feeds every view's symbol picker | `asmspy_symtab_load` | ELF symtab/dynsym + **separate debug info** (`.gnu_debuglink`/build-id, CRC- and id-verified — see Theme A) + PLT; no attach. **Static symbols only — `--syms` does *not* layer the JIT/perf-map** (`cmd_syms` calls `asmspy_symtab_load` alone; `asmspy_resolve`'s ELF→JIT→refresh chain lives in the single-step engines + `--sample`), so managed frames are absent here even when `--stream`/`--graph` name them | **yes** |

> The **Data flow** and **Data watchpoint** views landed **2026-07-15** via the
> [live-attach data-flow plan](../archive/plans/live-attach-dataflow-plan.md) (Inc 6 headless `--dataflow` +
> Inc 7 TUI mode 9) and its followup **F3** (`--watch`); `asmspy_engine_watch` per-thread-arms
> across `/proc/<pid>/task/*`. **Live-attach Increment 5 (`attach_jit` + the signal split)
> LANDED 2026-07-15**: the Data-flow view now SEIZEs every thread and races whichever one
> first enters the region — reaching a routine that runs on a worker thread (as managed
> methods almost always do), same as any native leaf — and detects/delivers the target's own
> trap (a JIT self-check, a debugger breakpoint) rather than swallowing or single-stepping
> through it, so the `runtime_is_managed` hard gate is removed from both the headless and TUI
> call sites. Two disclosed gaps remain: a genuinely W^X-enforced JIT page's entry breakpoint
> has no hardware-breakpoint fallback yet (self-skips cleanly, `DF_PTRACE_ETRACE`), and a
> method re-JIT'd/moved *mid-capture* decodes the live snapshot (the versioned-decode `img`
> plumbing isn't wired into asmspy's call site yet) — both were pre-existing, explicitly
> flagged deferrals, not new bugs. Validated by 11 new checks in
> `examples/test_dataflow_ptrace.c` (signal-detection/delivery + worker-targeting, ASan/UBSan-
> clean) and `make docker-cli`; not yet validated against a real dotnet/JVM process (no such
> asmspy-specific harness exists in-repo).

Cross-cutting, landed: **PLT stub resolution** (`name@plt`, tagged `[EXT]`); the **crash-safe
two-phase detach** (stop all threads, then release all — fixes a fatal-SIGTRAP-on-detach when
stepping a JIT); **whole-process-tree following** in `--procs` (`TRACEFORK`/`TRACEVFORK` +
a `/proc` PPid rescan for pre-existing children, `TRACEEXEC`); **Tab toggles a view option /
F3 refreshes**; headless subcommands under [cli-smoke](../../../cli/cli_smoke.sh).

## Roadmap (remaining work)

### Theme A — Symbol resolution (biggest correctness/usefulness wins)

| Item | Sev | Eff |
|---|---|---|
| ~~Resolve **JIT/perf-map** (`/tmp/perf-<pid>.map`, jitdump) in stream/graph/tree — managed frames (Node/V8 JS, **.NET**, **Java**) render as `[?]`/`0x..` today; one resolver addition names all three (they emit the same perf-map format)…must refresh during the trace~~ — **landed**: `asmspy_jitmap_t` + `asmspy_resolve` (ELF→JIT→rate-limited refresh-on-miss) in all three single-step engines; JIT frames render `name [jit]` (stream/tree) and `[JIT]` (graph); `jit_victim` smoke proves it. (`.NET`/Java need the runtime launched with the perf-map flag/agent — asmspy can't enable it from outside.) *binary jitdump reader **landed 2026-07-12**: tier-1 source (maps-discovered `jit-<pid>.dump`, `CODE_LOAD`/`CODE_MOVE`-aware, exact sizes; ASan/UBSan-fuzzed against hostile files); the text perf-map is tier 2* | **high** | L |
| ~~Separate-debug / `.gnu_debuglink` / build-id resolution for stripped distro binaries~~ — **landed 2026-07-16**: a stripped binary with separated debug info resolved **nothing** before this; now `load_module_syms` recovers it through the two keys GDB and the perf tools use, in GDB's order — **build-id** (`.note.gnu.build-id` → `<debugdir>/.build-id/ab/cdef….debug`) then **`.gnu_debuglink`** (a filename + CRC-32 → `<dir>/`, `<dir>/.debug/`, `<debugdir>/<dir>/`). `<debugdir>` defaults to `/usr/lib/debug` and is overridable via **`ASMSPY_DEBUG_DIR`** (the analogue of gdb's `set debug-file-directory`; it is also what lets the smoke test the global-tree and `.build-id` paths hermetically instead of needing root to write a system path). **Both keys are VERIFIED, never trusted** — a debuglink candidate must match the recorded CRC-32 and a build-id candidate must carry the same build-id — because for a tracer a *confidently wrong* name (the classic "-dbg package one version behind") is worse than no name. New precedence: own `.symtab` > **verified separate-debug `.symtab`** > `.dynsym`, and exactly one of the three is read — so no module contributes duplicate entries and a module carrying its own `.symtab` never pays for a debug-file search. Refactor: the symbol walk came out of `load_module_syms` into `scan_elf_syms(base, flen, want, …)` so it can run over a second mapped ELF; the bias from the **running** module applies to the debug file unchanged (it is the same ELF with the contents carved out, so section addresses and `st_value`s are preserved). Five findings worth carrying: (1) `objcopy --add-gnu-debuglink=<path>` records only the **basename** (verified against binutils) — so the search is directory+basename, and taking the basename defensively also keeps a hostile ELF's `../../` out of the candidate paths; (2) the debuglink CRC is the plain **zlib/IEEE CRC-32**, verified byte-for-byte against what objcopy records (GDB's `gnu_debuglink_crc32` is the same function); (3) `.gnu_debuglink` is **non-alloc so `strip --strip-all` removes it** — `--add-gnu-debuglink` must run *after* the strip, while `.note.gnu.build-id` is `SHF_ALLOC` and survives, which is why build-id is the key distros actually index by; (4) a stripped **executable**'s `.dynsym` does not carry its locally-called functions at all (only what dynamic linking needs), which is what makes the smoke's negative control real; (5) the CRC table is built per call — ~2 k iterations against a multi-MB file read is free, and it keeps the hash a pure function with no shared state for the UI and tracer threads to race. Guarded by **7 smoke assertions** (`cli/debuglink_victim.c` + `cli_smoke.sh`): a negative control (stripped, no debug file → resolves nothing), all three debuglink paths, a `--trace ret=51` on the recovered symbol (proving the address is the real **runtime** address — a bias bug would resolve a name that never hits), CRC-mismatch rejected, build-id resolved, foreign-build-id rejected. All three checks are **mutation-proven** to fail when the corresponding check is removed. Known limit (shared with gdb/perf, not new): the search keys off the `/proc/<pid>/maps` path, so a **deleted** or foreign-mount-namespace binary finds no debug file even though asmspy still maps the exe itself via `/proc/<pid>/exe`; **debuginfod is out of scope** (a network fetch does not belong in an attach path) | med | **M** |
| ~~C++ **demangling** via `__cxa_demangle` at the single `sym_push` chokepoint (guard on `_Z`, demangle before appending `@plt`, add `-lstdc++`) — reads through picker/graph/tree/stream at once~~ — **landed**: `demangle_dup()` at the `sym_push` chokepoint (peels/re-appends `@plt`), `-lstdc++` on the link line, `cpp_victim.cpp` smoke assertion | med | **S** |

### Theme B — Cross-engine asymmetries

| Item | Sev | Eff |
|---|---|---|
| ~~Region sampler (mode 2) attaches only the thread-group leader — silently empty for a function that runs on a worker thread; seize all threads and arm the region across them~~ — **landed 2026-07-16**: `asmspy_engine_region` now SEIZEs every thread (`PTRACE_O_TRACECLONE`, so one spawned mid-run can win a later round) and races them to the entry, sampling **whichever arrives first**; `--trace` gained `--tid=<t>` to pin one, mirroring the other engines. This closes the shape that mattered — a **managed method almost never runs on the leader**, so the view was structurally blind to the JIT'd code asmspy exists to show. Reuses the design of the data-flow tier's oracle-validated race (`dfp_seize_all`/`dfp_run_to_multi`, live-attach Increment 4) rather than inventing a second one, reimplemented over asmspy's own `thr_tab` per the standing precedent that an engine stays in `cli/` and leaves `src/ptrace_backend.c` untouched (as `asmspy_engine_watch` does). Two findings worth carrying: (1) `--tid` **cannot** narrow the SEIZE — a shared int3 would kill an unseized thread that reached it — so it pins via a **per-thread hardware execution breakpoint** (the alternative, stepping hot non-target threads back over a shared int3, was MEASURED not to converge); (2) the entry trap and the debug registers both survive `PTRACE_DETACH` and are both refused on a *running* thread, so the disarm must stop each thread first — skipping that still passes an immediate liveness check and then kills the target seconds later by SIGTRAP (exit 133) on its next arrival. `cli_smoke.sh` asserts the worker sample, both `--tid` directions, and survival past a settle; `make docker-cli` → cli-smoke PASS. Carryover: the entry breakpoint is `POKETEXT`-only in the any-thread path (no DR0 fallback for a W^X JIT page — it self-skips), and the TUI has no thread picker (mode 9's follow-on). | med | L |
| ~~No exec-stop re-resolution in stream/graph/tree — after a traced launcher `execve`s, the symtab + exebase go stale (`--procs` already sets `TRACEEXEC`; the others do not)~~ — **landed 2026-07-17**: all three engines now set `PTRACE_O_TRACEEXEC` (on the `--tid` path too — a pinned thread that execs replaces the image just as thoroughly, and by then it *is* the leader) and re-resolve at the exec-stop, the one moment where the new image is mapped but not one of its instructions has retired. A shared `exec_syms_t` (`exec_syms_init`/`_reload`/`_free`) holds the caller's table until an exec, then swaps in an engine-owned reload; the JIT map is **re-init'd, not refreshed** (a refresh merges, and a stale JIT method surviving an exec is the same wrong name in a different table); `--graph` additionally re-reads **`exebase`**, which drives the `[int]`/`[EXT]` split — a stale one silently re-labels the new binary's own functions external. `--tree` also resets each thread's depth/pending-call/focus state (the old stack is gone). A reload failure falls back to **raw addresses**, deliberately: for a tracer an unresolved `0x…` is honest and a confidently wrong name is not — the same reasoning that makes Theme A's debuglink keys verified rather than trusted. New fixtures `cli/exec_victim.c` + `cli/exec_stage2.c` (`preexec_fn` and `postexec_fn` live in **different binaries at different load biases**), and the two halves are each other's control: `preexec_fn [exec_victim]` present proves asmspy really was attached *before* the exec (so the post-exec proof isn't a late attach), `postexec_fn [exec_stage2]` present proves the reload, and both appear in **one** capture. The `--stream` leg deliberately spawns a **fresh** victim — reusing the already-exec'd one would load stage2's symbols at attach and pass with no reload at all, a vacuously green test. `exec_stage2` is linked **`-static`** purely for test runtime (ld.so relocation costs >20 k instructions before `main`, which would eat a single-step budget whether or not the fix works); the reload keys off the exec-stop and `/proc/<pid>/maps` and does not care how the image is linked. **Mutation-proven (3/3 caught, all run):** `TRACEEXEC` unset → `postexec_fn` 1→**0** (the whole post-exec image collapses to 7 nodes named from the *old* binary — silently wrong, no error); reload no-op → `postexec_fn` 1→**0**; exebase not re-read → `postexec_fn` still resolves but `kind:"internal"` 1→**0**, catching exactly the half the symtab check cannot | low | M |
| ~~Child-process following limited to `--procs` — stream/graph/tree/syscalls do **not** follow forked children (`TRACEFORK` unset there); `strace -f` parity gap~~ — **landed 2026-07-17** as **`--follow`** (opt-in, like `strace -f`) on `--log`/`--stream`/`--graph`/`--tree`. **The effort estimate (S) was wrong, and the reason is the item's real content**: setting `TRACEFORK` is one line, but a cloned THREAD shares its parent's mm and fd table while a FORKED CHILD shares *neither* — so everything the engines key off the attach-time leader `pid` (`process_vm_readv`, symbol resolution, fd→path, the exe basename, and the detach-time `at_syscall_insn` guard read) silently describes the **wrong process** for a followed child. Following forks is therefore not one more ptrace option, it is a **per-tgid re-keying**. Landed as such: `thr_t` gained `tgid` (taken from the clone/fork event — free and exact: a CLONE joins the signalling task's group, a FORK/VFORK starts its own — with a lazy `/proc` fallback), and Increment L95's `exec_syms_t` generalized into a **`psym_tab_t`**: one symtab + JIT map + exebase **per process**, borrowed from the caller for the attached pid and loaded fresh for any process we followed into, reloaded on that process's exec. `--tree` also seeds a forked child's depth from its parent's (it resumes inside `fork()` on a copy of the parent's stack, so starting it at 0 would underflow every early return and render its whole tree flat). `--tid` + `--follow` is a **usage error**, not a precedence rule — one says "exactly this task", the other "and everything it spawns". Semantics match `strace -f -p`: forks that happen **while traced**, not pre-existing children (that rescan is `--procs`'s job) — the fixture waits 2 s before forking against the smoke's 1 s settle precisely so a late attach can't make the test vacuous. New fixture `cli/fork_victim.c`, whose shape is the test: parent and child run distinct functions **and each open a different file at the same fd number 3** (opened after the fork). Opt-in gives every case a real control — the identical run without the flag must show the child **not at all**. Evidence: `--log` control parent 150/child **0**, `--follow` parent 75/child 75 across 2 pids; `--stream` control parent_fn 403/child_fn **0**, `--follow` 195/165; and `--graph --follow` on a child that **execs** names `parent_fn [fork_victim]` (PIE, `0x63e5af2142a9`) *and* `postexec_fn [exec_stage2]` (static, `0x4018b5`) in one graph — two address spaces, two symbol tables, which a single table cannot do. **Mutation-proven (2/2 caught, both run):** `TRACEFORK` unset → child lines 75→**0**; fd/strings decoded via the original leader → `write(fd=3<…child.txt>)` 74→**0** while the parent's count *inflates* 73→**147** (the child's writes attributed to the parent's file). **Carry this one:** that second mutation is a near-miss vacuous green — a naive "the child's path appears somewhere" grep still **passes** under it (1 line, from `openat`, whose path string is a `.rodata` constant at the same address in the forked image, so reading the parent's mm returns the right bytes by accident). Only the precise `write(fd=3<…child…>` assertion catches it | low | ~~S~~ **L** |

### Theme C — Signal & control-flow correctness

| Item | Sev | Eff |
|---|---|---|
| ~~App-delivered `SIGTRAP` is swallowed **and** mis-decoded as a single-step — distinguish via `PTRACE_GETSIGINFO` `si_code`~~ — **landed** (`sigtrap_is_app` + `deliver_app_sigtrap` in all four single-step engines; `int3_victim` smoke). Empirically corrected the plan's own hint: on x86-64 `TRAP_BRKPT` is **ours** (a step completing across a syscall), a real `int3` is `SI_KERNEL`, so the safe whitelist delivers only `SI_KERNEL`/`TRAP_HWBKPT`. Re-inject via `PTRACE_CONT`, never `SINGLESTEP` (re-arming TF fires a fatal `#DB` in the masked handler). Two follow-ups (both from an adversarial review): a batch `--count` run can't reach its budget on an int3-looping target (documented in `--help`); the region + syscall engines still swallow app SIGTRAPs (out of scope, lower impact) | med | M |
| ~~Indirect-call attribution slips at signal boundaries — read the CALL target from regs/mem instead of inferring from where the next step lands~~ — **landed 2026-07-17**: both stepping engines (`--tree` and `--graph`, which had the same defect) now **verify** the pending indirect call at its resolution stop — `rsp == call_site_rsp - 8` **and** `[rsp] == call_site + insn_len` — instead of assuming the next stop is the callee. This took the **cheaper route the row did not anticipate**, and it is worth recording why it is also the better one: the row prescribed reading the target from regs/mem, which needs a new operand-decoding API in `src/disasm.c` (or Capstone in `cli/`) *plus* an x86 reg-id→`user_regs_struct` map — and would still only re-derive a **prediction** from registers the same signal could have changed. Verifying the **actual post-call machine state** is strictly stronger, needed no new API (so no bindings-parity exposure), and reuses what C-L104's return-address stack already proved: what a call promises is knowable **at the call site** (`asmtest_disas_probe` already returned the insn length; the graph engine was discarding it, and now shares one decode for is_call+length). No edge is lost — `sigreturn` puts rip back on the call site and the call re-arms and resolves for real. Contrary to the scoping note in `4760d4f`, the test is **deterministic, not statistical**: the one-instruction window is forced, not raced. `ASMSPY_TEST_SIGRACE=<signo>` (a lever in the spirit of `ASMSPY_TEST_THR_OOM`) queues a signal while the tracee sits stopped **at** the call site with the pend armed, so the kernel must deliver it before the call retires — only the signal's *timing* is chosen; every line of the resolution path under test is production. `cli/sigcall_victim.c` names three functions for three independently-vacuous-able facts: `handler_helper` (handler ran ⇒ the race really happened), `indirect_target` (real edge survives ⇒ not a "fix" that drops indirect calls), `sig_handler_fn` (**never** a callee — nothing calls it, so an edge into it *is* the fabrication). In `--graph` the bogus edge is a **count, not a name** — the handler legitimately appears as a *caller* — so the assertion is `invocations == 0`. `make docker-cli` → cli-smoke PASS. Carryover: the frame the interrupted call pushed stays live while the handler runs, so handler frames render one level deep rather than zero (self-heals at `sigreturn` via the rsp rule; cosmetic, and *depth*, not attribution). | low | M |
| *(2026-07-17 note on the row above — **SUPERSEDED the same day**: the row landed via this note's own "cheaper third route", return-address **verification** at the resolution stop, so nothing here blocks attribution anymore. The operand-decoding scoping below stays only as the map for anyone who ever wants indirect-call targets for DISPLAY.)* The blocker is operand decoding: the `asmtest_disas_*` wrappers expose `is_call` / length / **direct** target only, so reading `call rax` / `call [rbx+8]` needs either **(a)** a new operand API in `src/disasm.c`, or **(b)** Capstone used directly from `cli/` — viable, since `mk/cli.mk` already *requires* Capstone (`CLI_MISSING`) and `$(CAPSTONE_CFLAGS)`/`$(CAPSTONE_DEF)` are ready to add to the `cli/` rule — plus an x86 reg-id → `user_regs_struct` mapping. **Theme C's return-address stack (landed) opens a cheaper third route**: the engine now knows the exact return address a call pushed, so the pending-call resolution can be **verified** rather than assumed — at the next stop, require `[rsp] == call_site + insn_len` before attributing; a signal frame fails that test, and after `sigreturn` the callee entry passes it. Cheaper than operand decoding and it closes the same hole, but it needs care where a handler makes its own indirect call (one `pending_call` flag per thread would be clobbered — key it off the frame stack instead). **Testing note:** the fix is deterministic but the *bug* is a race, so a test needs a signal storm (`setitimer`) against a single-stepped indirect-call loop and asserts on `--graph --json` that **no** `caller → sig_handler` edge exists. Statistical, not deterministic — budget for flake analysis | low | M |
| ~~`--tree` depth is a best-effort shadow counter that tail-calls / `longjmp` / signals drift — lift to a real per-thread return-address stack (also enables nesting-aware aggregation)~~ — **landed 2026-07-17**: `thr_t` now carries a real `frame_t{ret, sp}` stack and depth is its height. **The whole design is one rule: a frame is live exactly while the thread's rsp is at or below the rsp its CALL left behind** (`frm_unwind`) — so a RET, a `longjmp` over ten frames, and a C++ unwind all pop correctly *whether or not a `ret` ever retired*, while a signal handler (which runs BELOW the interrupted rsp) correctly leaves the frames under it intact. One comparison covers what the counter could not express at all. The frame is pushed **at the call site**, from `rip + ilen` and `rsp - 8` — `asmtest_disas_probe` already returned the instruction length and the engine was discarding it — so nothing depends on where the next step lands. `--tree` on a **forked** child now copies the parent's live stack (it resumes inside `fork()` on a copy of it); an exec clears it. Memory is bounded (`ASMSPY_STK_MAX` 4096 frames) so runaway recursion costs the *tracer* a fixed amount; stacks are freed in `thr_del` and at detach. **The drift was not cosmetic — MEASURED against the old algorithm** (`cli/longjmp_victim.c`: main setjmps, calls 3 deep, longjmps back, then calls `after_jump` from main at depth 0): `after_jump` rendered at depth **5**, then **10**, and `level_one` marched **0 → 10 → 20 columns** across three iterations. **Cumulative and permanent, on a process behaving perfectly normally.** With the stack, `after_jump` is at depth 0 every iteration. The two smoke assertions are each other's control — `jump_out` at depth 2 proves real nesting *is* still tracked (so "depth 0" is not a tracer that counts nothing), `after_jump` at depth 0 proves the discarded frames were unwound. **Mutation-proven (2/3 caught, all run):** the old pop-on-RET-only algorithm → `after_jump` at depth 0 count **2→0** and indented count **0→2**; `frm_unwind` never unwinding → `after_jump` at depth 7. **Honest note:** a third mutation (`while`→`if`, i.e. pop at most one frame per step) is **NOT** caught — many steps elapse between the longjmp and `after_jump`, each popping one frame, so the loop reaches 0 anyway. The `while` is defensive there rather than load-bearing for this fixture | low | M |
| ~~`thr_get()` OOM ignored at resume sites — an untracked clone child can escape the two-phase detach (the seize path detaches on OOM; the resume paths don't)~~ — **landed 2026-07-17**: **10 resume sites across all 5 tabled engines** (syscalls/stream/graph/tree/procs — the group-stop `LISTEN` branch and the generic resume in each) now check `thr_get()` and call **`release_untracked(tid, stepped)`** instead of resuming: disarm TF, `PTRACE_DETACH`. Losing sight of a task is the honest outcome of being out of memory; the alternative is killing the process we exist to watch out of band. `stepped` mirrors `detach_threads` so a `PTRACE_SYSCALL` engine never has step state written into it. **Testing this needed a lever**: a mid-trace allocation failure cannot be provoked from outside and is silent when it happens, so `ASMSPY_TEST_THR_OOM=<n>` (test-only, unset = disabled) caps the table at `n` tasks and fails every new one after — stickily, as a real OOM would. New fixture `cli/clone_victim.c` stays single-threaded until after the attach, then spawns short-lived threads (`spawned_fn` runs only on those). **HONEST SCOPE — the crash is NOT what is asserted.** The consequence (an untabled task left step-armed, killed later by SIGTRAP) does **not reproduce on a simple victim**: MEASURED, clone_victim survives *with and without* the fix — the same limit already recorded for the two-phase-detach tripwire (that crash reproduced reliably only on a real V8/Node JIT). So the smoke asserts the **policy** that prevents it — *untabled implies released* — which is mutation-detectable, rather than a crash that would pass either way. Under `ASMSPY_TEST_THR_OOM=1` the tabled leader keeps being stepped (`main_fn` present) while every untabled clone is gone (`spawned_fn` **0**, 1 tid). **Mutation-proven (2/2 caught, both run):** resume-the-untabled-task-anyway (the exact pre-fix code) → `spawned_fn` **0→2366**, tids **1→6** — traced but invisible to `detach_threads`; `TRACECLONE` unset → `spawned_fn` **2366→0**. **Carry this:** that second mutation makes the OOM check pass *for the wrong reason*, which is why the no-injection control (`spawned_fn` must appear) sits beside it — the two assertions are each other's guard | low | S |

### Theme D — Test coverage

| Item | Sev | Eff |
|---|---|---|
| ~~**No regression test for the crash-safe two-phase detach** — the exact bug the fix closed has zero guard, and every smoke victim is killed right after tracing, so a regression that silently kills the target looks like success. Trace a multi-threaded victim, then assert it **survives** detach~~ — **landed** as a happy-path survival tripwire (single-step via `--stream`+`--tree`, then assert alive) in `cli_smoke.sh`. Honest scope: the historical fatal SIGTRAP reproduced *reliably only on a real V8/Node JIT* (per 6aaad45's own notes — simple victims survive even the pre-fix detach), so this guards gross regressions, not that JIT crash (which needs a live JIT, an inherent limit) | med | S |
| ~~Graph sort comparator (`gnode_cmp`) ordering/tiebreaks unasserted — extract into a testable header + `test_graphsort.c`~~ — **landed 2026-07-12**: `cli/asmspy_graphsort.h` + `cli/test_graphsort.c` (ties included), wired into the smoke | med | M |
| ~~`asmspy_symtab_at` reverse-lookup edge cases~~ **(landed 2026-07-17: `cli/test_symtab.c`, 20 checks. Every view that names an address goes through this one function, and its edges are exactly where a resolver lies quietly rather than fails — one byte past a function, the GAP between two functions, a zero-SIZE symbol, an address below the first, and n==1's binary-search collapse. Each returns a plausible NAME when wrong, which is worse than nothing. **Mutation-proven 3/3:** containment `<`→`<=` → the gap at 0x1020 answers 'beta' (3 FAILs); no containment test → 7 FAILs; search boundary `<=`→`<` → every entry address misses (5 FAILs))**; ~~multi-thread `[tid]` tagging for graph/tree~~ **(landed 2026-07-17 for `--tree`: `tid_victim`'s two threads must both appear, tagged, under ≥2 distinct tids — without the prefix two threads' trees interleave into one indented column that reads like a single nonsensical call chain — plus the `--tid` control, where a single-thread trace must NOT carry it. Mutation-proven: `multi` forced 0 → tagged lines 40→**0**, distinct tids 3→**0**. `--graph` is **N/A, not skipped**: its nodes are aggregated ACROSS threads by design and carry no tid, so there is nothing to tag)**; attach-failure + `REGION_NEVER_RAN`; ~~negative-`n` "until exit"~~ (**landed 2026-07-19**, asmspy-cli-enhancements T7: `cli/exit_victim.c` — the one victim that exits — proves `--log`/`--stream <pid> -1` returns rc 0 when the target leaves, timeout-guarded so a spin-forever regression surfaces as rc 124); ~~`--tree` depth > 1~~ **(covered 2026-07-17 by the Theme C longjmp test — `jump_out` asserted at depth 2 — and by `test_treefilter`'s depth-cap/re-base cases)**; ~~post-attach clone-following~~ **(landed 2026-07-17: `cli/clone_victim.c` stays single-threaded until after the attach, so `spawned_fn` runs only on threads `seize_threads`' one-shot `/proc` scan can never have seen — 2366 lines across 6 tids; mutation-proven, `TRACECLONE` unset → 0)**; ~~job-control group-stop~~ (**landed 2026-07-19**, asmspy-cli-enhancements T6: SIGSTOP/SIGCONT honored via PTRACE_LISTEN — victim State t/T, feed paused, resumed on CONT, tracer exits 0; both `--log` and `--stream`); ~~syscall-decoder breadth~~ (**landed 2026-07-19**, asmspy-cli-enhancements T9: readv CONTENTS at exit, dup2, ftruncate's A_SIZE, a second arity-0 shape (getppid), and clock_nanosleep's 4-arg shape each get a rendered-text pin); ~~region edge aggregation~~ (**landed 2026-07-19**, asmspy-cli-enhancements T8: `--trace work 2` pins work(5)'s five helper calls to ONE `5×` line per sample — exact count + a ≤2-helper-lines merge control, each other's guard); ~~a pure TUI view-model (`test_view.c`)~~ **(landed 2026-07-15: `cli/asmspy_dataview.h` + `cli/test_view.c`, unit-testing the Data-flow annotation/slice/def-use logic, wired into `cli_smoke.sh`)** | low | S–M each |
| ~~**The `--sample` block is VACUOUS in `make docker-cli` — and always has been**~~ — **landed 2026-07-17 (`bb76680`): `make docker-cli-ibs`** reruns the same image + smoke with `--cap-add=PERFMON` under Docker's **default** seccomp profile (CAP_PERFMON gates `perf_event_open` there, and bypasses `perf_event_paranoid` — no sysctl change, no `seccomp=unconfined`), so the `--sample` and `--auto` blocks execute their else-branches for real on an AMD IBS host; the smoke's skip branch now echoes asmspy's real perf-errno reason and points at the lane instead of asserting an unmeasured host property; the `mk/docker.mk` "needs paranoid lowered" claim is corrected in place (2026-07-17). *(Original finding, for the record:)* (2026-07-17, MEASURED). `mk/cli.mk:304` runs `docker run --rm $(_docker_plat) asmtest-cli` — no `--cap-add`, no `--security-opt` — and Docker's **default seccomp profile blocks `perf_event_open`** (`mk/docker.mk:561` says so explicitly). So `--sample` always self-skips, and `cli_smoke.sh:1281`'s `if grep -q '^# SKIP --sample'; then <accept>; else <EVERY assertion> fi` takes the skip branch **every time**: the `hot_spin` naming check, the `--json` `"mode":"ibs-op"` check, the `"from_name":"hot_spin` resolution check have **never executed in the only lane that runs them**. End-to-end evidence on a Zen 5 9950X (`ibs_op` PMU + `swfilt` both present): the smoke prints **`SMOKE_RC=0`** while line 440 is `# SKIP --sample: ` and line 441 is `(IBS-Op unavailable on this host — sampler self-skipped, OK)` — **a false statement**; IBS *is* available here, `perf_event_open(ibs_op)` returns **fd=3** under `--cap-add=PERFMON --security-opt seccomp=unconfined` (measured; unprivileged it is EACCES, bare-container EPERM). So the view this plan calls *"the only rich view safe on any target"* is **untested**, and the gate is green. Same class as the format/parity gates that globbed the wrong directory. **Fix**: a `docker-cli-ibs` lane mirroring **`docker-hwtrace-amd`** (`mk/docker.mk:541-545`), which already carries exactly these two flags for exactly this reason. Per CLAUDE.md, IBS *hardware* is a legitimate gate but **a capability flag on a `docker run` line is not** — the hardware is present, only the run line is missing; `Dockerfile.cli` makes this argument verbatim for `gcc-multilib` ("a 32-bit process is not hardware, so it is not a real gate — it is an apt line"). **Correction to carry**: `mk/docker.mk:536` claims the AMD lane "still needs an AMD host **with `perf_event_paranoid` lowered**" — **MEASURED FALSE**: `CAP_PERFMON` bypasses paranoid entirely (this host is at paranoid=**4**; unprivileged EACCES, `--cap-add=PERFMON` → fd=3). Do not lower a host sysctl on this evidence | **high** | S |
| **Already landed, previously unrecorded here** (bookkeeping, no work implied) — **`cli/test_logview.c`** (`cli/asmspy_logview.h`, `mk/cli.mk:133-134`) unit-tests the TUI scrollback viewport math with no ncurses, so it runs anywhere the smoke does; **`cli/test_jitdump.c`** (`mk/cli.mk:154-157`) unit-tests the binary jitdump reader + the two-tier JIT resolve chain, linking `asmspy_proc.o` directly. Both are `cli-smoke` prereqs (`mk/cli.mk:159-165`) — so the landed view-model surface is four tests (`test_view`, `test_logview`, `test_jitdump`, `test_graphsort`), not one | — | — |

### Theme E — TUI / UX & interop

| Item | Sev | Eff |
|---|---|---|
| ~~Call-graph view (mode 4) neither scrollable nor pausable — only the top `rows-3` visible, re-sorts every frame~~ — **landed**: `space` freezes the snapshot (a `gpaused` flag stops the sink overwriting it) and up/down·PgUp/PgDn·Home/End scroll a `gtop` window over the frozen, sorted nodes; a finished tracer is scrollable too, and `space` resumes the live top-anchored re-sort. Verified end-to-end through the ncurses TUI (pty-driven). Mirrors the log-view pause/scroll idiom | med | M |
| ~~Region view (mode 2) disasm/functions panes don't scroll — overflow off-screen~~ — **landed**: `space` freezes the sample (an `rpaused` flag stops the sink re-sampling) and up/down·PgUp/PgDn·Home/End scroll the focused pane; `Tab` moves focus between the ASSEMBLY and FUNCTIONS panes (each with its own `atop`/`ftop` offset and a `(scroll)` overflow hint), a finished sample stays scrollable, and `space` resumes the live top-anchored re-sample. Verified end-to-end through the ncurses TUI (pty-driven). Same idiom as the call-graph pause/scroll | med | M |
| ~~Call tree has no depth cap / symbol focus / module filter — a firehose on busy processes~~ — **landed 2026-07-17**: `--tree --depth=<d>` (`tree -L`: d levels), `--focus=<sym>` (root the tree at a symbol's **subtree** and re-base its depth to 0), `--module=<m>` (keep only that module's callees) — all substring-matched, composing as AND with `focus` applied first because it is the only one that establishes *scope*: `--focus=work --module=libc` means "the libc calls work() makes", not "libc calls, and separately work()". The decision came out into **`cli/asmspy_treefilter.h`** (pure, header-only, the `asmspy_graphsort.h`/`asmspy_dataview.h` precedent) + **`cli/test_treefilter.c`** (19 checks). The governing invariant, and the thing every review should re-check: **filtering bounds what is EMITTED, never what is TRACKED** — the engine pushes/pops the per-thread depth on every call and return regardless of the verdict, and `max` therefore counts *surviving* lines. A filter fed only the surviving calls drifts from its first suppression on (mutation M6 below demonstrates exactly this: the cap suppresses `helper` at depth 1, the un-pushed counter falls back to 0, and the *next* `helper` escapes the cap at depth 0). `--depth=0` is a **usage error, not "unlimited"** (it can only ever print nothing; omitting the flag is how you ask for unlimited). Guarded end-to-end in `cli_smoke.sh` against `spy_victim`, whose shape supplies a real control for each case — `usleep@plt` runs at the **same real depth 0** as `work`, so "`--focus=work` dropped it" cannot pass by cutting deep lines, and `helper` unfiltered only ever appears indented two columns, so "printed at column 0" cannot pass without a true re-base. **Mutation-proven (6/6 caught, all run):** cap ignored → unit 3 FAILs + `helper` leaks the cap; no re-base → unit 5 FAILs + `helper` renders indented; focus never closes → unit 6 FAILs + `usleep@plt` leaks into `--focus=work`; module ignored → unit 4 FAILs + `[spy_victim]` leaks into `--module=libc`; `max` counting raw calls → `--depth=1 8` yields **2** lines and `--module=libc` yields **0 bytes**; depth pushed only on emit → `helper` leaks the cap. The two engine-level mutations are caught only by the e2e, correctly — they are outside the pure filter's scope. **TUI mode 5 is deliberately unchanged** (passes `filter = NULL`, asserted identical to a zeroed filter): it has no filter widget, and a text-entry ncurses prompt is a separate, CI-undrivable piece of work — the headless flags carry the feature | med | M |
| ~~No per-thread (tid) filter for stream/tree/graph — can't isolate one worker or cut single-step slowdown on others~~ — **landed**: `--stream`/`--tree`/`--graph --tid=<t>` seizes/steps only that thread (`seize_one`/`seize_for_engine`), leaving the rest of the process at full speed; `tid_victim` (distinct alpha/beta functions) smoke proves the other thread's code never appears | med | M |
| **DOT/JSON export** — *landed for `--graph`*: `--graph --json` emits nodes (addr/name/module/kind/counts) **and edges** (address-keyed caller→callee + count — `gedge_t` now crosses the sink via `asmspy_gedge_t`/`graph_emit`), and `--graph --dot` emits a Graphviz digraph (kind-coloured nodes, count-labelled edges; `dot -Tsvg`-validated in the smoke). `--tree --json/--dot` **landed 2026-07-12** (mirrors the `--graph` exporters). ~~Still open: `--json`/`--dot` for `--procs`~~ — **landed 2026-07-17, closing the row**: `--procs --json` emits the flat **task** list (`tid`/`tgid`/`ppid`/`leader`/`comm`/`exe`/`inv`) — the forest the human view draws is derivable from `tgid`+`ppid`, so exporting a *rendered* tree would throw information away **and** force a consumer to re-parse box-drawing glyphs. It also exports **`"count"`**: `inv` means something different per mode, and a bare number that silently switches meaning is exactly what an exporter must not emit. `--procs --dot` renders the forest with **processes as boxes** (solid parent→child) and **threads as dashed-edged ellipses**, so the two kinds of "child" the human view stacks into one glyph column stay distinguishable; threads are drawn only where a process has more than its lone leader — the same rule the human view uses, so the two renderings agree. Guarded by the `--graph`/`--tree` exporters' own conventions (well-formedness via `python3 json.load`, `dot -Tsvg` where graphviz is present, and a *human output must not leak into export mode* check) plus task invariants asserted against the multi-threaded victim: exactly **1 leader**, `tid == tgid` for it, ≥2 tasks, all one `tgid`, total `inv > 0`. **Mutation-proven (2/2, both run):** exports ignored → JSON unparseable + every DOT assertion 4→**0**; JSON emits only leader tasks → still perfectly well-formed JSON with a plausible 1-task list, caught **only** by the ≥2-tasks/threads-exported invariant — the reason those invariants are asserted rather than just "it parsed" | low | M |
| ~~Syscall arg decoding is a hardcoded subset (flags/structs/vectors/sigsets → hex); the always-3-hex-slots default is a small standalone blemish~~ — **partially landed 2026-07-17** (scoped deliberately; the rest is listed below as a follow-on, not silently dropped). A per-syscall **argument-shape table** now gives ~40 syscalls exact arity and per-arg classes: `open`/`openat` flags+octal mode, `mmap`/`mprotect` prot+flags, `clone` flags, signal numbers, sigset **bitmasks** (`rt_sigprocmask`'s `how` too), `iovec` **contents** (`readv`/`writev`), `timespec`, `lseek` whence, and the socket family incl. the `sendto` case `4760d4f` flagged (its fd now resolves to an endpoint). **The roadmap's question — can arity come from the generated source? — was investigated and the answer is NO**, MEASURED in the image: 741 `__NR_*` macros, **zero** arity/nargs macros, no `syscall_64.tbl` (kernel-source-only), no man2 pages. Every `__NR_` is a *number*; there is no `__NR_x_NARGS`. And arg *semantics* ("this word is an open-flags mask") is stated by no header even in principle — strace curates these tables for the same reason. So the table is hand-maintained but **rot-guarded**: every entry is `#ifdef __NR_x`, so a kernel lacking a syscall cannot break the build (the protection the generated name list already had), and a *wrong* entry is caught by a smoke that drives `cli/argdecode_victim.c` through known calls with known arguments and asserts the **rendered text**. The **always-3-hex blemish is fixed at the root**: an undescribed syscall now prints its three words followed by `...` — it no longer *asserts* an arity it never established, which is precisely how it rendered a write as `stat("", 0xf20f0e34, 0x1) = 1` and looked confident doing it. Arity 0 (`getpid()`) and 6 (`mmap`) are now expressible, and open's mode is **conditional** (dropped unless a creating flag is set). Found + fixed en route: an `int` arg arrives zero-extended, so `mmap`'s `-1` fd rendered as `fd=4294967295`. `make docker-cli` → cli-smoke PASS. **NOT covered** (the honest remainder): ~~`stat`/`statx` struct contents (the *path* decodes; the buffer stays a pointer)~~ (**landed 2026-07-19**, asmspy-cli-enhancements T4: `{st_mode=…, st_size=…}` on success, mask-honoring `statx`, raw pointer on failure), ~~`sockaddr` contents for connect/bind/sendto~~ (**landed 2026-07-18**, asmspy-cli-enhancements T1: `{AF_INET, ip:port}`/`{AF_INET6, [ip]:port}`/`{AF_UNIX, "path"}` for connect/bind/sendto IN + accept/accept4/recvfrom OUT, `socket()` domain named), ~~`ioctl` request decoding, `fcntl` cmd decoding~~ (**landed 2026-07-19**, asmspy-cli-enhancements T2: named requests + `_IOC(dir,type,nr,size)` for unknowns; `F_*` names with conditional arity), ~~`futex` op decoding~~ (**landed 2026-07-19**, asmspy-cli-enhancements T3: `FUTEX_*` op names with the private/clock-realtime flags folded in), `execve` argv/envp vectors (deliberate — formatting happens at syscall EXIT, when a successful exec has already replaced the address space the strings lived in), and the ~700 syscalls with no descriptor, which keep the `...` default. | low | L |
| *(2026-07-17 note on the row above — **NOT done**, but two concrete findings from adjacent work.)* **(1)** The fd→endpoint work (Theme E) hit this directly: `sendto` is not in the decoded set, so a socket send renders `sendto(0x6, 0x6422497c607c, 0x1)` — three raw hex slots, **no fd to enrich**, which is why `cli/sock_victim.c` had to use a self-connected DGRAM socket and plain `write()` instead. The socket-syscall family (`sendto`/`recvfrom`/`connect`/`accept`/`bind`) is where the always-3-hex default is most visible now that fds resolve to real endpoints. **(landed 2026-07-18, asmspy-cli-enhancements T1: the sockaddr now decodes, and `cli/sock_victim.c` uses `sendto` directly.)** **(2)** The i386 gate (Theme F3) measured what a *wrong* decode looks like — `stat("", 0xf20f0e34, 0x1) = 1` for a `write` — a useful reference for how confidently this decoder renders nonsense when its inputs are wrong | low | L |
| ~~fd→path shows `socket:[inode]`/`pipe:[inode]`, not the endpoint; graph node/edge lookups are O(n) per call; `usage()` accepts the `functions-called` sort synonym but omits it from the `--sort=` `bad_arg` text~~ — **all three landed 2026-07-17.** **(1) fd→ENDPOINT**: `fd_endpoint()` resolves `socket:[<ino>]` through **`/proc/<TARGET-pid>/net`** — the target's pid deliberately, so a container's socket is looked up in *its* netns, not ours — across `tcp`/`tcp6`/`udp`/`udp6`/`unix`, rendering `TCP 127.0.0.1:40730->127.0.0.1:54681`, `TCP LISTEN 127.0.0.1:54681`, `unix:/tmp/foo.sock`. **`pipe:[inode]` is left alone deliberately**: its peer is not in `/proc/<pid>/net` at all, and naming it would mean scanning every process's fds for the same inode — not something an attach path should do per syscall. New fixture `cli/sock_victim.c` binds to **port 0 and prints the port the kernel picked**, so the expected strings are *derived from the run*, not hardcoded, and cannot be satisfied by echoing the inode back; it holds both ends of one loopback connection, so each must show its own local→remote direction. Two fixture findings: `sendto` is **not** one of the hand-decoded syscalls (it renders as raw hex with no fd to enrich, i.e. would have tested nothing — hence a self-connected DGRAM socket and plain `write()`); and `write()` to a **listening** TCP socket raises **SIGPIPE**, not just EPIPE (`tcp_sendmsg`→`sk_stream_error`), which killed the victim outright (exit 141) until it ignored SIGPIPE. **Mutation-proven (2/2):** always-decline → raw `socket:[inode]` **0→64** and all four endpoint assertions **16→0**; enrichment-swallows-everything → endpoints→0 **and** the downstream regular-file `fd=<path>` assertion **4→0**, catching the additivity regression the endpoint checks alone cannot see. **(2) graph hash index**: open-addressed (`ghash_t`, splitmix64, slot = index+1 so 0 is empty, power-of-two cap, linear probing, grown at 70%) over both the node table (by addr) and the edge table (by caller,callee). The two endpoints are mixed **separately** before combining — a plain xor of two small indices collides trivially (`a→b` vs `b→a`), degrading the probe chain into the very scan it replaces. On index-allocation OOM the index is **dropped entirely** (`cap=0`) rather than left stale, so lookups fall back to the linear scan: a half-populated index would MISS and silently duplicate a node, which is wrong, whereas the scan is merely slow. Guarded by a **node/edge uniqueness** assertion on `--graph --json` — both lookups are "find it, else append", so a broken index does not error, it appends a second node for an address it already has and splits the counts across the duplicates. **Mutation-proven:** `gh_put` no-op → nodes **7→90** (unique still 7), edges **5→60**; assertion fires. **⚠ Honest gap:** the uniqueness check catches the index MISSING hits (what a `gh_put`/`gh_grow` bug produces) but **cannot** catch a hash-vs-key confusion — MEASURED, a mutant that accepts the first probe slot without comparing the key emits **byte-identical** output, because the smoke's graphs (≤7 nodes in a 128-slot table) never collide, so the first probe slot is always the right one. That failure over-merges rather than duplicating (on a larger graph it silently dropped an edge, 4→3). Closing it properly means extracting `ghash_t` into a header + a collision-forcing unit test, the `asmspy_graphsort.h` treatment — **DONE 2026-07-17**: `cli/asmspy_ghash.h` (find/put/grow; the probe loop is now owned by `asmspy_gh_find`, whose eq callback the engine's node/edge lookups supply — "the hash ROUTES, the eq callback DECIDES") + `cli/test_ghash.c` (9 checks, in `cli-smoke` before the ptrace blocks). The test brute-forces three alignment-shaped keys into ONE slot of the 128-slot table — the collision the smoke's ≤7-node graphs can never produce — then asserts the chain survives 303 entries / two doublings, an absent key that routes into the chain answers -1, and the cap==0 / idx+1 contracts hold. **Mutation-proven 3/3 (measured)**: accept-first-probe-slot-without-eq → 5 FAILs (exactly the byte-identical-in-the-smoke mutant this row recorded); slot stores idx not idx+1 → 3 FAILs; grow-without-rehash → 1 FAIL. Engine behavior unchanged (`make docker-cli` cli-smoke PASS, --graph uniqueness e2e included). **(3) `--sort=` synonym**: `functions-called` now appears in **both** the `bad_arg` text and `usage()` (marked as a synonym for `fanout`) — omitting it told a user who typed a near-miss that their working spelling does not exist. The `<sym|0xADDR[:LEN]>` half was already done | low | S–M |

| ~~**`# SKIP --sample: ` prints an EMPTY reason on exactly the host where the reason matters**~~ — **landed for asmspy 2026-07-17 (`bb76680`)**: `asmtest_ibs_unavail_reason()` (new public API, `include/asmtest_ibs.h`) carries the `perf_event_open` errno out of `ibs_chan_open` and answers the substrate-present-but-blocked path — EACCES names paranoid/CAP_PERFMON, EPERM names seccomp — and `cmd_sample` + `--dataflow --auto` print it. **REMAINING (the two adjacent instances, deferred in `bb76680` as "now cheap given the new API")**: `examples/ibs_probe.c` still prints "AVAILABLE"/"a survey is buildable" from the substrate probe alone without attempting an open, and `make ibs-test`'s six live paths still self-skip vacuously green on a locked-down host — both should consume `asmtest_ibs_unavail_reason` (or attempt a real open) before claiming availability. *(Original finding:)* (2026-07-17, MEASURED). `ibs_probe()` (`src/ibs_backend.c:319-358`) is **CPUID + sysfs only — it never calls `perf_event_open`**, so `asmtest_ibs_available() == 1` does **not** mean sampling will work; it means the *substrate* is present. On this Zen 5 all six detect checks pass (AMD, IBS caps, OpSam, BrnTrgt, `ibs_op` PMU, `swfilt`) → `g_avail = 1`, `g_reason = ""`. Then `perf_event_open` is refused, the engine returns `ASMSPY_SAMPLE_UNAVAIL`, and `cmd_sample` prints `asmtest_ibs_skip_reason()`, which is `return g_avail == 1 ? "" : g_reason` (`:366`) — **the empty string, by construction, on precisely this path**. `cli/asmspy.c:1177` even *names* it: `/* substrate present but perf_open blocked */`, then prints the passing detect chain's reason. MEASURED: `./build/asmspy --sample <pid> 200` on this host emits `# SKIP --sample: ` and nothing else — an operator on their own AMD box gets no reason at all. **Root cause**: `ibs_chan_open` (`:745-747`) does `long fd = perf_open(...); if (fd < 0) return -1;` — **errno is discarded**, so EACCES (permission), EINVAL (bad attr), EMFILE (fd limit), ENOENT (bad PMU type) and ESRCH (thread exited) all collapse into one indistinguishable code. Fix: carry the errno out of the open and report it on the UNAVAIL path (paranoid/CAP_PERFMON for EACCES). This is exactly the operator-one-liner discipline Theme F3's i386 gate was built for, inverted. **Two adjacent instances of the same root cause, both MEASURED**: `examples/ibs_probe.c` *actively misreports* this host — it prints "IBS-Op statistical edge sampling: AVAILABLE" and "A live, out-of-band from→to edge survey is buildable on this host" when no survey can open; and **`make ibs-test` is vacuously green here** — all six live paths self-skip on `EUNAVAIL` while printing substrate-present messages, so only the pure-decode tests actually run. A confidently-wrong "AVAILABLE" is the same defect this tracer rejects everywhere else (an honest `0x…` beats a confident wrong name) | med | S |

### Theme F — Portability & build

| Item | Sev | Eff |
|---|---|---|
| ~~**`--trace`'s entry bound is not a bound — `rgn_race_to_entry` can hang forever**~~ — **landed 2026-07-17**: a `CLOCK_MONOTONIC` **wall** bound (`RGN_ENTRY_WALL_MS`, 30 s) now sits alongside the idle window and is checked **unconditionally**, including on the paths that reset `idle_us`. Both are kept because they answer different questions and the honest answer differs — idle ⇒ "the region is quiet" (the fast, common case), wall ⇒ "we waited this long and it never came" — and 30 s is deliberately ~10× the idle window: a backstop for the pathological target, not a second opinion on the quiet one. Same shape as `DFP_ENTRY_WAIT_MS`, which had the identical hazard. *(Original finding, for the record:)* The loop polls `WNOHANG` and gives up after `RGN_ENTRY_WAIT_MS` (3000) of accumulated `idle_us` — but `idle_us = 0` executes on **every** waitpid event (`asmspy_engine.c:2288`), *before* every handler: clone, group-stop, a forwarded signal, an app SIGTRAP. So it ceilings **one uninterrupted quiet spell**, not the wait. Any target that stops more often than the window — a 1 Hz timer signal, a chatty clone, a JIT's own traps — resets the budget forever and `--trace` blocks indefinitely on a region that is never arriving. The comment above the constant claims it bounds "FREE-RUNNING time", which is exactly the distinction that makes it not a bound on the wait. The only escapes are `stop` or the target dying; the outer `idle > 20` counter does **not** cover it (it counts rounds where the race *succeeded* but produced nothing — an rc==2 breaks out first). Fix: a `CLOCK_MONOTONIC` deadline alongside the idle rule, as `DFP_ENTRY_WAIT_MS` now uses (`src/dataflow_ptrace.c`) — a bound whose job is to bound must not be resettable by the thing it bounds. **Note the numbers are not transferable**: 3000 ms of resettable idleness ≫ 3000 ms of wall time; picking a wall default needs the slow-managed-arrival argument the data-flow row makes | med | S |
| ~~**`asmspy_engine_region` discards its own diagnosis — three different facts collapse into `REGION_NEVER_RAN`**~~ — **landed 2026-07-17**: the race's outcomes are now NAMED (`RGN_RACE_CAUGHT/QUIT/IDLE/EGONE/EUNARMABLE` — they were bare integers, which is how they came to be discarded), the loop KEEPS why it ended, and zero samples now maps each to its own answer: EUNARMABLE→`ASMTEST_PTRACE_ETRACE`, EGONE→`ASMTEST_PTRACE_ENOENT`, IDLE→`NEVER_RAN`. Any sample at all still returns 0 — how the last round ended is then just "it went quiet", which is not news. *(Original finding:)* `rgn_race_to_entry` carefully returns **0** = entrant caught, **1** = user quit, **2** = idle for the window, **−1** = target gone / entry unarmable — and the caller does a bare `if (rc != 0) break;` (`asmspy_engine.c:2492`), never re-reads `rc`, then decides purely on `sample == 0` (`:2534`). So "the region did not run", "the target exited", and "we could not arm the breakpoint" all render as *"never executed"*. The information exists and is thrown away one line later; plumbing it through is small | low | S |
| ~~**A documented contract is FALSE: the W^X/JIT self-skip never happens**~~ — **landed 2026-07-17**, unblocked by the row above: an arm failure now returns a DISTINCT code all the way out, so `asmspy.h:245-248`'s promised ETRACE self-skip is real. VERIFIED deterministically without a W^X page — an explicit range at an UNMAPPED address cannot be POKETEXT'd, which is the same failure the JIT page produces: it now reports *"ptrace/attach failure … possibly a W^X JIT page refusing the entry breakpoint"* where it used to report *"never executed"*. The header text is corrected too, since it described behaviour that never existed. Also fixed the `ENOENT` rendering: the library defines that code as "region/symbol not found", which is actively misleading for a target that EXITED (the symbol resolved fine — the process stopped existing), so `cmd_trace` now says *"pid N exited before <sym> was seen executing"*. *(Original finding:)* `asmspy.h:245-248` promises that a genuinely W^X-enforced JIT page refusing the POKETEXT entry breakpoint "self-skips via `ASMTEST_PTRACE_ETRACE` … rather than being traced wrong". It does not: `rgn_plant_bp` failing returns −1 from the race → the bare `break` above → `sample == 0` → **`ASMSPY_REGION_NEVER_RAN`**. The only `ETRACE` returns are on the *seize* path (`:2443`, `:2465`), **before** any breakpoint is planted. So the exact case the contract was written for reports *"the region never executed"* — a confidently-wrong answer about a region that may be executing constantly, which is the failure mode this tracer rejects everywhere else (an honest `0x…` beats a confident wrong name). Depends on the row above: once the race's `−1` is distinguishable from its `2`, this is the same fix | med | S |
| Single-step engines are **x86-64-hardcoded** (`rip`/`eflags`-TF/`orig_rax`, `ASMTEST_ARCH_X86_64`) though the disassembler already does ARM64 — needs a reg/single-step/detach abstraction + `PSTATE.SS` | med | L |
| ~~No architecture gate — on arm64 `make cli` emits raw `SYS_open undeclared` / `no member rip` instead of self-skipping like other tiers~~ — **landed 2026-07-17**: `CLI_ARCH := $(shell uname -m)` gates `cli` AND `cli-smoke` (each exits 0 with a `# SKIP` naming the reason and pointing at the Theme F abstraction row above), checked BEFORE `CLI_MISSING` deliberately — falling through would tell an arm64 user to install libncurses-dev, which cannot fix an architecture. Also covers `make docker-cli` on an arm64 HOST (`_docker_plat` follows the host unless `DOCKER_PLATFORM` is set, so the in-container make sees aarch64 and hits the same gate). MEASURED in a real linux/arm64 container (qemu binfmt, the lane the arm64 memory note documents): `make cli` → the skip, rc 0, where it previously failed; `make cli-smoke` → same. x86-64 behavior unchanged | low | S |
| ~~32-bit/i386 tracees silently mishandled — pragmatic fix: read `/proc/<pid>/exe` `EI_CLASS` at attach and refuse with a clear message~~ — **landed 2026-07-17**: `asmspy_elf_class(pid)` (reads only the 16-byte `e_ident`, which is identical in ELF32/ELF64 — so, uniquely in that file, it must **not** go through `elf_ok()`, whose job is to reject the very files this identifies) gates **all 8 ptrace engines** before they attach, returning `ASMSPY_ETRACEE_I386`. **Why this is not cosmetic** — MEASURED with the gate removed, against a victim that only ever calls `write(2)` and `nanosleep`: `read(fd=4291765908, 4291765908) = 0` / `stat("", 0xf20f0e34, 0x1) = 1` / `readlinkat(-3201388, …)`, **rc=0**. Every syscall named wrong (the tables overlap and disagree — i386 4 = `write`, x86-64 4 = `stat`), every fd garbage (a 32-bit pointer read through the 64-bit ABI), and a **success exit**. Textbook confidently-wrong. With the gate: rc=**1**, *"32-bit (i386) tracee — unsupported (asmspy is x86-64-only and would MISDECODE it). Trace a 64-bit process"*, and the victim is untouched (refused **before** attach). The message was shortened after measuring that the first draft **truncated mid-sentence** at the callers' `char[128]` — the detail lives in `asmspy.h`, the string is the operator's one-liner. **Dependency added, not skipped** (CLAUDE.md): `gcc-multilib` on `Dockerfile.cli`'s existing apt line — a 32-bit process is not hardware, so it is an apt line, not a gate — and `cli/i386_victim.c` is a real `-m32` tracee. `make docker-cli` therefore **hard-fails** if the victim is missing rather than skipping, and the smoke first verifies the fixture really is `ELFCLASS32` (a 64-bit "i386_victim" would make every assertion pass for the wrong reason). Controls: the identical `--log` against the 64-bit victim must succeed, and the victim must still be alive afterwards (a refusal, not a failed attach). **Mutation-proven:** gate removed → rc 1→**0** on all five subcommands. **⚠ Outside this lane** — CI runs `make cli-smoke` on a **bare GitHub runner**, not in docker (`.github/workflows/ci.yml:565`), so its toolchain has no multilib and this block does not run there. `mk/cli.mk` carries a **parse-time** `-m32` probe (not a recipe-line `|| exit 0`, which would only exit that line's shell) feeding a sentinel, so the runner build does not hard-fail. **One-word fix, not made here because that file is outside this change's ownership: add `gcc-multilib` to the `cli` job's apt line.** | low | S |
| ~~CI tests asmspy only against apt Capstone 4, never the pinned 5.0.1 that docs/docker ship; no documented kernel-version floor (a too-old-kernel `SEIZE` failure misreports as a permission problem)~~ — **landed (both halves; marker corrected 2026-07-16 — this row had gone stale)**: the `cli` job builds the **pinned Capstone 5.0.1 from source** (`.github/workflows/ci.yml`, cached per the K1 pattern), explicitly *"NOT apt's Capstone 4 — the older library silently degrades some disassembly, so the smoke must run against what users actually get"*; the **kernel floor is documented** in the user guide ([asmspy.md](../../guides/tracing/asmspy.md) — `PTRACE_SEIZE`+`PTRACE_INTERRUPT` need **Linux 3.4+**, and the guide names the too-old-kernel failure so it isn't misread as a permission problem) | low | S |

### Theme G — Process/docs

- ~~No dedicated asmspy plan/roadmap doc~~ — **this document** closes it.

### Theme H — Auto-targeting: `--dataflow <pid>` with no symbol *(planned)*

> **The ask**: identify what a process is *actually doing right now* and trace that, without
> the operator naming a function. Today `--dataflow <pid> <sym|0xADDR[:LEN]>` requires the
> answer as input — you must already know what you want to look at.

**This needs no engine change.** `asmspy_engine_dataflow(pid, only_tid, base, len, …)`
(`asmspy.h:303`) takes an **address range**; the symbol requirement lives entirely in
`cmd_dataflow` (`asmspy.c:1829`) → `resolve_region`. Auto-targeting is a **resolution-layer**
feature. `asmspy_engine_sample` already produces the input out of band (no ptrace, no
single-step, target unperturbed, endpoints resolved through **both** the ELF symtab and the
JIT perf-map), and `asmspy_sample_edge_t` already carries **`uint64_t from_addr, to_addr`**
plus `is_return` (`asmspy.h:474-481`) — everything the rule needs.

**The rule: rank the hottest ENTRY edge, not the hottest edge and not the hottest PC.**
An IBS-Op edge whose `to_addr` equals a symbol's *start* is a call/tail-jump to a function
**entry**; its count is a statistical proxy for **invocation rate**. Aggregate per entry
symbol (multiple call sites sum), drop `is_return` edges, prefer the target's own module,
pick the max → `(sym->addr, sym->size)` → the existing engine.

**MEASURED 2026-07-17** (Zen 5 9950X, `--cap-add=PERFMON`, a 3-level victim: `main` →
`outer_cold` → `mid_warm` ×2 → `leaf_hot` ×8):

```
3903  mid_warm+0x17   -> leaf_hot            <- ENTRY edge (renders with NO +0x)
 249  outer_cold+0x15 -> mid_warm            <- ENTRY, call site 1
 238  outer_cold+0x9  -> mid_warm            <- ENTRY, call site 2 (sums to 487)
 237  main+0x36       -> outer_cold          <- ENTRY
3744  leaf_hot+0x11   -> mid_warm+0x1c [ret] <- return: tagged, lands mid-function
3377  mid_warm+0x23   -> mid_warm+0x14       <- loop back-edge: mid-function
```

The entry-edge counts **reproduce the true invocation ratios**: 3903/487 = **8.0×** (the exact
loop trip count), 487/237 = **2.05×** (the exact call-site count). The proxy is quantitative,
not hand-wavy. Because IBS samples on a *period*, one entry sample stands for ~16 k arrivals
(95% one-sided Poisson lower bound ≈ 820 in a 300 ms window ≈ 2.7 calls/ms) — and the engine
needs exactly **one** arrival, so the expected wait is sub-millisecond.

**Why this is the rule that (mostly) cannot hang.** `asmspy.h`'s own contract: *"A region that
never executes on ANY thread **blocks the producer at its entry breakpoint**"* — a wrong pick
does not error, it **hangs** (which is why `cli_smoke.sh:130` timeout-guards it). Type-match
the evidence to the event: the engine blocks on exactly one event — a thread's RIP reaching
`base` — and an edge with `to_addr == sym->addr` **is a direct observation of that same
event**. Everything else is a different event wearing the same clothes. In the capture above
**`main` has no entry edge at all**, because it was entered once, before we attached — *an
entry edge is evidence of re-entry by construction*, so "hottest entry edge" and "the
breakpoint fires promptly" are nearly the same predicate. A **PC histogram picks `main`** and
blocks forever; that contrast is the test's control (the rival rule's answer **hangs**). An
independent 3-way design panel converged on this rule **3/3 — including the agent assigned to
argue the PC-histogram case**, which reported back that residency-ranking is wrong "and the
failure is not an edge case — it is the modal case. Every event loop, every `main`".

**⚠ Honest scope — "cannot hang" is overstated, and the real fix is elsewhere.** The evidence
is gathered in a window ending *milliseconds before* the attach. A target that changes phase
in that gap (the winner's entry stops arriving at `t0 + W·ms + 1`) still blocks forever.
`--auto` reduces P(hang); it cannot **bound** it, and no resolution-layer rule can — the hole
is non-stationarity, not the ranking. The actual elimination is a **bounded entry wait inside
`asmspy_engine_dataflow`**, and there is precedent in this very tree: **`--trace` already has
one** (`ASMSPY_REGION_NEVER_RAN`; `cli_smoke.sh:1755` asserts a non-runner `--tid` reports
"never executed" promptly rather than hanging). `--dataflow` should grow the same bounded wait
— arguably before `--auto`, since it makes *every* pick safe, not just the automatic one.

**⚠ The vacuity trap this rule walks into** — `sym_at`'s zero-`size` branch is
`query == s->addr`, **exact-start-only**. So for any zero-size symbol (hand-written asm, some
PLT/JIT entries) resolution succeeds *only* at the entry, and `to_addr == sym->addr` is
**100% true by construction, not by measurement** — every such symbol looks like a pure entry
and wins. The rule must **require `sym->size > 0`** (it needs a real `len` for the region
anyway) and the unit test must include a zero-size symbol as a negative control. Related:
`asmspy.h:137-139`'s doc comment claims a "nearest addr<=" fallback in `asmspy_symtab_at`
that **does not exist** in `sym_at` — do not design around it (docs fix, separately —
**fixed 2026-07-17**: the comment now states containment-only + the zero-size
exact-start rule).

**Module filtering is load-bearing, not polish.** A raw hot endpoint lands in
`__memmove_avx_unaligned` or a JIT stub far more often than in the operator's code; without a
filter the feature reliably picks libc and reads as broken. Reuse `--module=`'s substring
idiom (`asmspy_treefilter.h:45`) — but **not** `--graph`'s `[int]`/`[EXT]` split as a module
predicate: `external == 0` means "internal **OR unknown OR jit**" (an unresolved address takes
the same branch), `exebase` is a **name**, not a load address (`char[64]`, silently truncated
>63 chars), and PLT stubs live in the exe yet are `[EXT]` **by design**. Those two questions
("is this address in the exe" vs "is this node internal") are different.

**Shape**: a pure header-only decision module + unit test, per the
`asmspy_graphsort.h` / `asmspy_treefilter.h` / `asmspy_dataview.h` precedent — the ranking is
pure over `(edges, symtab)` and must be testable without IBS hardware, which is what keeps the
feature's core covered on **every** host while the live leg gates on AMD.

**⚠ But the pure module is not the risky half — the ADAPTER is** (adversarial review, 2/3
lenses raised it independently). The ranking is easy to unit-test and easy to get right. The
part that can actually be wrong is the **accumulation sink**: the callback that drains
`asmspy_engine_sample`'s per-window edges, resolves each `to_addr`, applies the entry test,
and sums per symbol. It lives *outside* the pure header (it needs the symtab and the engine),
so a `docker-cli`-only lane tests **neither** half — the unit test does not reach it and the
live block self-skips. Its most likely mutant (drop the accumulation, keep the last window)
**keeps every block green**. So the pure module must be given the widest possible seam — take
`(edges[], n, symtab)` and return the ranked list, so the adapter shrinks to "call the engine,
hand it the edges" — and the `docker-cli-ibs` lane must assert the *summed* count, not just
the winning name. Note the two-call-site sum in the measurement above (`mid_warm` = 249 + 238)
is exactly the property a last-window-only mutant loses.

**Fixture**: `cli/sample_victim.c` exists and carries the two things any new victim needs —
`noinline` (a real, resolvable ELF symbol) and the **`prctl(PR_SET_PTRACER, PR_SET_PTRACER_ANY)`**
opt-in every victim here carries. **Do not omit that prctl**: Yama `ptrace_scope=1` is the
Ubuntu default and is *not* namespaced, so without it a sibling attach is denied and the
failure reads exactly like a tracer bug (cost me a wrong diagnosis — I briefly concluded
`docker-cli` was missing `--cap-add=SYS_PTRACE`; it is not, the victims opt in instead).

But **`sample_victim` cannot test the happy path — MEASURED 2026-07-17**, in the new lane, it
yields **3 edges, ALL of them `hot_spin`→`hot_spin` back-edges, and NO entry edge whatsoever**:

```
5340  hot_spin+0x56 -> hot_spin+0x1e      2663  hot_spin+0x32 -> hot_spin+0x4d
2631  hot_spin+0x25 -> hot_spin+0x34      (main -> hot_spin: ABSENT)
```

because `hot_spin(2000000)` is entered ~once per 2 M iterations — far too rare to sample. So
it is the **refusal control**, and a good one: the rule must find zero candidates and say so
honestly. It also shows what the naive rule does — raw-hottest-edge picks `hot_spin+0x56`, a
**mid-function** address, which is not an entry the producer can ever break on cleanly. The
happy path needs a **new** victim with a genuinely re-entered function (the measured 3-level
shape above: `leaf_hot` at 3903 entry samples).

**`--auto` + `--tid` is a usage error (rc=2), not a precedence rule** — the same call the
Theme B `--tid`/`--follow` row made, for a harder reason: `asmspy_sample_edge_t` carries **no
tid**, and neither does `asmtest_ibs_edge_t` (`ibs_drain` reads the record's `pid`,
`ibs_backend.c:509`, and drops the tid). The sampler physically cannot say *which thread*
arrived at an entry, so `--auto --tid=<t>` could only pin the capture to a thread that may
never enter the region — i.e. it is a **hang generator**. Refuse it up front.

**Blocked on Theme D/E above**: built today, this feature's live test would self-skip in
`docker-cli` forever — a **fourth** vacuously-green block in a file that already has one. Fix
the lane first. *(And it was: `docker-cli-ibs` landed first (`bb76680`), so `--auto`'s live
blocks have run for real from their first commit — this never bit.)*

| Item | Sev | Eff |
|---|---|---|
| ~~**Bounded entry wait in `asmspy_engine_dataflow`**~~ — **landed 2026-07-17**. `--dataflow <pid> <a symbol that is not running>` did not error, it **HUNG**. MEASURED against `--trace` on the identical target/function/thread: `--trace` rc=1 in **4 s** ("never executed"); `--dataflow` rc=**124** at 25 s, killed by `timeout`. The producer's `DFP_STEP_BACKSTOP` **looked** like a bound and was not: it counts single-steps, and a region that never arrives burns **zero** steps, so `dfp_run_to_multi`'s blocking `waitpid(-1, __WALL)` never advanced it. Now `WNOHANG` + nap + a **`CLOCK_MONOTONIC` deadline** → `DF_PTRACE_NEVER` → `ASMSPY_REGION_NEVER_RAN` → "not seen entering (waited N ms)". **Deliberately NOT the region engine's shape**: `rgn_race_to_entry` resets `idle_us` on EVERY event, so it ceilings one quiet spell, not the wait (see the §F row below). Default **10 s, not 3000** — 3000 ms of *resettable idleness* and 3000 ms of *wall time* are not the same posture, so matching the number would have been strictly more aggressive and turned a slow-but-real managed arrival into a confidently-wrong "never ran"; `ASMTEST_DF_ENTRY_WAIT_MS` overrides (0 = the old unbounded wait). **The unwind was the hard part, and the kernel gives you nothing**: MEASURED, `PTRACE_INTERRUPT` on an already-ptrace-stopped tracee **returns 0** (indistinguishable from success) and queues **no** event, so the next blocking `waitpid` never returns — a standalone probe died only to `SIGALRM`. That is why the region engine *tracks* a `stopped` flag and `dfp_thrset` (a bare `pid_t` array) cannot. So `dfp_remove_bp_any` now restores the byte, **rewinds `rip` if it sits at `base+1`** (a blind `CONT` resumes one byte into the restored instruction — both judges flagged it independently), and `CONT`s, re-establishing the all-running invariant `dfp_detach_others` assumes. This also fixes a **latent hang on the pre-existing `DFP_STEP_BACKSTOP` path**, which had the same disarm-then-detach sequence and was simply never exercised (it needs 1 M non-target int3 hits). `DF_PTRACE_NEVER` is a **positive** (2), like FAULT: an outcome, not a failure — folding it into ETRACE would say "we could not trace you" when the truth is "your code is not running", sending an operator to ptrace_scope/seccomp instead of to the symbol. Re-declared at all **three** hand-duplicated sites (`cli/asmspy_engine.c`, `examples/test_dataflow_ptrace.c`, `examples/gccanon_attach/gccanon_tracer.c` — the last is a **caller** that only skipped on ENOSYS/ETRACE and would have drained an empty feed as success). **Mutation-proven:** `ASMTEST_DF_ENTRY_WAIT_MS=0` → rc 1→**124**, hangs again, proving the *bound* is what fires and not an incidental error; the same-victim `hotfn` control still captures (rc=0, ret=57, 83 steps); the victim survives a settle (a leaked int3 does not fail loudly — it kills the target by SIGTRAP on its next arrival, seconds later, looking unrelated). Fixture is `attach_victim`'s **`main`** — entered exactly once, *before* the attach, never re-entered: the precise shape that blocks, and the same "hot by residency, never re-entered" case §H's ranking exists to reject. Found + fixed en route: my own **use-after-free** printing the region name (`dc.func` borrows into the symtab, which is freed before the message formats — it printed plausible garbage rather than crashing; the smoke now asserts the literal name) | med | M |
| ~~`--dataflow <pid>` with no symbol → sample, rank hottest **entry** edge, capture it~~ — **LANDED 2026-07-17** as `asmspy --dataflow <pid> --auto [--module=<m>] [--json] [--max=<n>]`. Verified live on `auto_victim`: *"--auto: entered_often [auto_victim] — 60 entry samples from 1 call site (of 1 candidate in 400 ms)"* → captured 10 steps / 24 records, **no symbol named by the operator**. The design above landed unchanged; what the build added: **the picker is `cli/asmspy_autoregion.h`** (pure, header-only, the `asmspy_graphsort.h`/`asmspy_treefilter.h` precedent) + **`cli/test_autoregion.c` (19 checks)**, and that split is the feature's spine — the sampler beneath is AMD-IBS **hardware** that self-skips on every other host (every GitHub runner included), so a rule verified only end-to-end would be verified almost nowhere. The pure test therefore carries the burden and runs everywhere; the live block covers only the wiring. **Mutation-proven**: deleting the `size > 0` guard fires exactly the zero-size vacuity check (`to == start` is true *by construction* for unsized symbols, so one would win outright on a technicality). **The fixture is the argument**: `auto_victim`'s `grind_forever()` is entered ONCE, never returns, and burns the CPU — the **residency** winner a PC histogram picks, whose entry breakpoint can never fire again; `entered_often()` is called from its inner loop. The two rules *disagree*, so "picked entered_often" cannot pass by accident, and the smoke asserts `grind_forever` is **absent** (the rival's answer doesn't merely differ — it hangs). `quiet_helper()` is never called and must never be named. **The measured refusal matters as much as the pick**: `--auto` on `attach_victim` reports *"no function was observed being ENTERED (400 ms, 0 edges)"* — its `hotfn` runs at 5 Hz, far too rare for IBS's period, and **most processes are idle most of the time**, so refusing honestly is the common path, not an edge case. `--auto --tid` and `--module=` without `--auto` are both rc=2. Adapter kept deliberately thin (drain the window → call the picker) per the review that flagged it, not the pure module, as the half that can be wrong. **Two adapter defects found + fixed 2026-07-17 (review of this branch)** — both in that thin adapter, vindicating the flag: **(1) the pick was ELF-only** — `auto_resolve_sym` wrapped `asmspy_symtab_at` alone and the jitmap was freed *before* ranking, so a JIT'd/managed method could never win despite a comment claiming it could (on a JIT-heavy target every hot entry edge failed to resolve → a misleading refusal). Now the resolver layers ELF → JIT via the pure `_at` lookups — deliberately NOT `asmspy_resolve`, whose refresh-on-miss can realloc the map mid-rank and dangle the name/module pointers already folded into candidates; instead one refresh runs after the window (so mid-window emissions are named), and the winner's name is COPIED out so `cmd_dataflow` can name a capture the symtab cannot (`data flow — jit_hot_loop`, not `?`). Live leg added to the smoke: `jit_victim`'s `jit_hot_loop` — an anonymous-mapping function invisible to the ELF symtab, re-entered ~1M/s — must be picked, traced, and NAMED. **Mutation-proven (measured in `docker-cli-ibs`)**: reverting the resolver to ELF-only (the exact pre-fix shape) fails the leg — every entry edge lands in code no ELF symbol covers, so the picker refuses and the pick assertion fires. **(2) `AUTO_MAX_CANDS=16` silently dropped candidates** under a comment claiming a full table's newcomer "cannot win anyway" — false: accumulation is streaming, so a symbol first seen late could out-SUM an admitted one (and the drop violated the standing no-silent-caps rule). The adapter now sizes the table by the window's **edge count** (each edge introduces ≤1 candidate, so nothing can drop; OOM degrades to the capped stack table), and `test_autoregion` #10 (21 checks now) pins both the degradation and the sizing rule | med | M |
| ~~**`--dataflow --max=<n>` is broken for every n below the region's step count — and lies about why**~~ — **landed 2026-07-17**, a one-constant fix whose intent the code already stated. `asmspy.h` documents `max` as "bounds the in-region steps captured"; it did not truncate, it **failed**. MEASURED on the pre-change tree against `attach_victim`'s `hotfn` (83 steps): `--max=3` → **rc=1**, `--max=5` → **rc=1**, no `--max` → rc=0, `--max=200` → rc=0 — **the flag worked only when it did nothing**. The exhaustion branch (`dataflow_ptrace.c:948`) already did everything right — appended the partial trace, set `c->vt->truncated = true`, and its comment reads *"Bounded scope reached: append what we have … flag truncated, and leave the tracee"* — then returned **`DF_PTRACE_ETRACE`** ("fork/ptrace/wait failure (seccomp)"). So a valid `--max` surfaced as *"data-flow capture failed: ptrace/attach failure (permission? ptrace_scope? … possibly a W^X JIT page)"*, sending the operator to Yama/seccomp/JIT pages when the truth was "your cap was smaller than the function". Now `DF_PTRACE_OK`: the engine already reads `truncated` ("vt->truncated tells the story"), so nothing else changed — `dfp_dirty_exit`'s foreign path still leaves the tracee stopped for the caller to DETACH, and the target still survives. No `*result` is set, deliberately: the region has not returned, so there is no return value, and `truncated` is what says so. **Found by a CONTROL**: it broke the bounded-entry-wait smoke's happy-path leg, which is the argument for controls — the bug had shipped through every prior review. Undetected for the same reason it was harmless-looking: the suite only ever asserted `expect_badarg --max=0`, never a *valid* cap. **The assertion is the EXACT step count, not rc=0** — "it did not fail" would also pass if `--max` were ignored entirely, which is the other way this breaks; `n` steps for `--max=n` can only come from a cap that caps. Guarded by: `--max=3` → **exactly 3 steps**, `--max=5` → **exactly 5** + `--json` `"truncated":true`, and a control that `--max=200` (> 83) is indistinguishable from no cap (83 steps, `ret=57`) — proving it truncates *at the cap* rather than just capping everything short. **Mutation-proven**: reverting the constant → the smoke fails | med | ~~M~~ **S** |
| ~~**The sibling truncation site tells the same lie, and conflates two different facts**~~ (`dataflow_ptrace.c:988`, found 2026-07-17 next door to the row above) — **landed 2026-07-17**. The CALL-OUT step-over's failure branch fused two facts into one `||` and one `DF_PTRACE_ETRACE`: `++total > DFP_STEP_BACKSTOP` (a **bound** — an honest outcome, the same shape `--max` just got right) and `dfp_run_to(...) != 0` (a real **failure** — the callee exited, faulted, or its return byte could not be trapped on a W^X page). Split into two branches: the bound now returns `DF_PTRACE_OK` + `truncated=true`; the real failure keeps `DF_PTRACE_ETRACE`. The backstop itself is now overridable (`ASMTEST_DF_STEP_BACKSTOP`, mirroring `ASMTEST_DF_ENTRY_WAIT_MS`) so a test can shrink the 2^20-step bound instead of driving 2^20 real steps — the same "needs 1M hits, never exercised" gap the bounded-entry-wait row above named. **Mutation-proven** (`test_callout_step_backstop`, the ATTACH path so `total` starts at a known 0 rather than an unknown fork-prologue count): reverting the split turns the check back into `DF_PTRACE_ETRACE` | low | S |
| ~~Portable IP sampler so auto-targeting is not AMD-only~~ — **landed 2026-07-17** as `asmtest_swclock_survey_process` (`src/ibs_backend.c`: `PERF_COUNT_SW_TASK_CLOCK` + `PERF_SAMPLE_IP`, per-tid channels + the mid-window `/proc` rescan + ring drain mirrored from the IBS survey; availability probed BY DOING with `asmtest_swclock_unavail_reason()` carrying the real errno — the `--sample` lesson applied from birth) + the pure `asmspy_autoregion_rank_ip` (containment attribution, residency ranking, same size>0/module/tie rules; 9 new checks in `test_autoregion`, 31 total) + `--sampler=ibs\|sw` on `--dataflow --auto` (AUTO picks by substrate). **The carry was honoured structurally**: the sw attr is built fresh, never via `ibs_cfg`/`ibs_fill_attr`, so config bit 19 (IbsOpCntCtl) cannot leak into a software event's `config` (which IS the event id). **The re-opened blocking hazard is owned out loud, not hoped away**: residency's winner is often the entered-once-never-returns shape (test #15 pins the two rules DISAGREEING on the same behaviour — the entry rule picks the hot callee, the residency rule the looping caller), so the sw path returns up to `AUTO_SW_TRIES`=3 ranked candidates and `cmd_dataflow` WALKS them on `NEVER_RAN`, reporting each honest refusal. **Proven live in `docker-cli-ibs`** on `auto_victim` — the built-to-disagree fixture: sw picks `grind_forever` (the residency winner), the bounded entry wait refuses it, the walk lands + captures `entered_often`; the smoke asserts the refusal-then-capture sequence, that captured-grind is impossible, and that a locked-down host's `# SKIP` carries a REAL perf reason (in plain `docker-cli`, measured, it does). `make docker-cli` + `make docker-cli-ibs` both PASS | med | L |
| ~~TUI: mode 7 (hot edges) → `Enter` → mode 9 (data flow) scoped to that endpoint — the existing "`Enter` drills in" idiom, no new UI concept; doubles as mode 9's missing thread picker~~ — **landed 2026-07-19** (asmspy-cli-enhancements T5): freeze (`space`), select (arrows), `Enter` drills into a data-flow capture of the edge's function (`to_addr` then `from_addr`), reusing the call-graph `Enter`-drill idiom. The decision is the pure `asmspy_edge_drill` (`cli/asmspy_autoregion.h`) — tries `to_addr` then `from_addr`, requires `size > 0` (the vacuity rule), accepts a mid-function `to_addr` (drill ≠ rank) — unit-tested in `cli/test_autoregion.c` (6 checks) so it is covered on every host, not just an AMD IBS box. Honest correction to "doubles as the thread picker": `asmspy_sample_edge_t` carries **no tid** (the IBS drain drops it), so the drill-in cannot and does not pick a THREAD; it provides the effect a thread picker was wanted for — `asmspy_engine_dataflow` races every thread to the chosen region, capturing whichever worker runs it. The ncurses wiring is pty-driven, so its interactive leg is manual-only (the plan's standing pattern for TUI rows); its decision logic runs in CI. | low | M |

## Top priorities

1. ~~**Resolve JIT/perf-map symbols in stream/graph/tree** *(A, high, L)* — the crash-safe
   detach was built to whole-process single-step V8/Node, yet on that flagship target every
   dominant JIT'd JS frame renders `[?]`/`0x..`; the same fix names **.NET** and **Java**
   managed frames too.~~ — **landed**: `asmspy_resolve` layers a refreshed perf-map over the
   ELF symtab in all three single-step engines (see Theme A). **All three original Top-3 have
   now landed.** Next candidates: ~~jitdump (bytes-accurate, handles tiered recompilation)~~ (**landed 2026-07-12**) and
   the remaining Theme E items (call-tree depth cap / focus / module filter; the
   call-graph and region-pane pause+scroll have since landed).
2. ~~**Add the crash-safe two-phase-detach regression test** *(D, med, S)*~~ — **landed**
   (a happy-path survival tripwire across `--stream`/`--tree`; see Theme D for the honest
   scope note on why a simple victim can't deterministically reproduce the V8 crash).
3. ~~**C++ symbol demangling at `sym_push`** *(A, med, S)*~~ — **landed**: one chokepoint
   change (`demangle_dup()`) makes the picker, graph, tree, and stream readable on any C++
   target at once; guarded by the `cpp_victim` smoke assertion.

## Known issues / open investigations

- **Fatal `SIGTRAP` single-stepping a real V8 process (VS Code / Claude Code extension host).**
  The two-phase detach fixed the reproducible detach-ordering crash, and a spawned Node
  (up to 10 threads, 49 s of single-stepping, worker threads + heavy churn) now survives
  `--graph`/`--stream`/`--tree`/`--procs --count=calls` every time. But a real V8 host was
  reported to still die with `SIGTRAP` — **not reproduced** with a spawned Node; more crash
  detail pending. Likely the inherent managed-runtime hazard below (V8 `IMMEDIATE_CRASH`/int3
  under perturbation) rather than a remaining asmspy bug. The `si_code` split (Theme C) — the
  named next diagnostic lever — has **landed** and sharpens this: V8's `IMMEDIATE_CRASH` int3
  reports `si_code == SI_KERNEL`, so asmspy now *faithfully delivers* it (via `PTRACE_CONT`)
  instead of swallowing it. A `SIGTRAP` death on a real V8 is therefore now confirmable as V8's
  *own* int3 delivered as it would be untraced — not an asmspy-injected signal. The split cannot
  separate a genuine V8 self-check from a step-perturbation-induced one (identical `SI_KERNEL`
  int3); that ambiguity is inherent (below). A detect-and-warn guard remains deliberately unbuilt.

## Managed runtimes (Node/V8, .NET, Java)

The safe views — **syscall log** and **`--procs --count=syscalls`** — already work on any
managed runtime today (they show the runtime's GC/JIT/finalizer/threadpool threads and child
processes with counts, no single-stepping). The **single-step** views (stream/graph/tree,
`--count=calls`, drill-in) run but (a) carry the crash risk below and (b) leave managed
methods unnamed until the **perf-map resolver** (Theme A) lands — one addition covers Node
(`--perf-basic-prof`), .NET (`DOTNET_PerfMapEnabled=1`), and Java (`perf-map-agent`), which
all emit `/tmp/perf-<pid>.map`. The runtime must be launched with that flag/agent; asmspy
cannot enable it from outside.

## Inherent limitations (record, do not schedule)

- **Single-stepping a live JIT/managed runtime (V8/Node, .NET/CoreCLR, JVM, Mono) can crash
  it.** They self-check and `int3`/abort when perturbed, and JIT/GC/deopt run under the
  stepper. The crash-safe detach removes one failure mode; it cannot make stepping a live JIT
  safe in general. Prefer the syscall-based views on these targets.
- **macOS / Windows unsupported.** ptrace `SEIZE` + `/proc` + `process_vm_readv` are
  Linux-kernel-specific; a macOS port is a full `mach_vm_read`/`task_for_pid`/`thread_get_state`
  rewrite. Out of scope by design.
- **`SIGTRAP` ambiguity.** A ptrace `#DB`/int3 and a genuine app `SIGTRAP` are fundamentally
  indistinguishable at the tracer; `si_code` heuristics reduce but cannot fully eliminate this.
